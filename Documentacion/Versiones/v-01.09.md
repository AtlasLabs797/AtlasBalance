# V-01.09

## Estado

Version actual activa.

## Base

- Version anterior publicada: `V-01.07`.
- Rama base local: `origin/main`.
- Rama de trabajo actual: `V-01.09`.
- Nota: no existe apertura documental de `V-01.08`; la nueva linea solicitada pasa directamente a `V-01.09`.

## Alcance

- Rama de trabajo para nuevas funciones y correccion de errores posteriores a `V-01.07`.
- Aplicacion Atlas Balance empaquetable para Windows x64.
- Backend ASP.NET Core 8, frontend React/Vite y Watchdog.
- Paquetes de release en `Atlas Balance/Atlas Balance Release`.
- Documentacion centralizada en `Documentacion`.

## Prioridad

- Corregir bugs antes de sumar funciones vistosas.
- Mantener cada cambio trazado en documentacion tecnica, bugs o bitacora.
- No publicar release sin build limpio y verificaciones relevantes.
- No meter artefactos de release en commits normales salvo que el objetivo sea publicar paquete.

## Instrucciones para cambios

- Asociar cualquier cambio nuevo a `V-01.09`.
- Actualizar `Documentacion/DOCUMENTACION_CAMBIOS.md` en cada sesion.
- Actualizar `Documentacion/DOCUMENTACION_TECNICA.md` si cambia codigo, estructura, scripts o configuracion.
- Actualizar `Documentacion/DOCUMENTACION_USUARIO.md` si cambia instalacion, uso, configuracion o ubicacion de paquetes.
- Consultar `Documentacion/LOG_ERRORES_INCIDENCIAS.md` antes de resolver errores.
- Mantener `Documentacion/REGISTRO_BUGS.md` al dia.
- No documentar contrasenas, tokens ni datos privados.

## Cambios registrados

### 2026-06-01 - Fix update antiguo sin owner persistido en Watchdog

- El primer fix de backup cubria instalaciones sin `MigrationConnection` pero con credenciales owner en Watchdog; no cubria instalaciones antiguas/manuales sin ambas cosas.
- `Actualizar-AtlasBalance.ps1` ahora puede usar `ATLAS_DB_MIGRATION_CONNECTION`, `ATLAS_DB_OWNER_USER`/`ATLAS_DB_OWNER_PASSWORD`, el archivo local `INSTALL_CREDENTIALS_ONCE.txt` si sigue existiendo, o prompt seguro con `-PromptForDbOwnerCredentials`.
- `update.ps1` propaga esos parametros al elevar por UAC.
- La plantilla productiva del Watchdog vuelve a declarar `DbOwnerUser` y `DbOwnerPassword`.
- Verificacion: parser PowerShell OK; fallbacks estaticos por archivo y variables de entorno OK; paquete local firmado con `SIGNATURE_OK`.
- Paquete local corregido: SHA-256 ZIP `4E3256141498450775AB581FC5DFF38F066867592D38F3123CAEED8940B38128`.
- Pendiente: republicar paquete firmado en GitHub y reintentar en la instalacion afectada. Publicacion bloqueada localmente porque `gh` no esta instalado y no hay token GitHub disponible.

### 2026-06-01 - Fix update desde V-01.06 sin MigrationConnection

- Una instalacion `V-01.06` sin `ConnectionStrings:MigrationConnection` fallaba en `pg_dump` antes de actualizar por RLS/FORCE RLS en `AUDITORIAS`.
- `Actualizar-AtlasBalance.ps1` ahora usa `MigrationConnection` si existe; si no, usa `WatchdogSettings.DbOwnerUser`/`DbOwnerPassword` para el backup pre-update.
- Si solo queda `DefaultConnection`, el actualizador conserva el fallo cerrado pero devuelve un mensaje claro sobre credenciales owner/migracion y RLS.
- Verificacion: parser PowerShell OK; paquete `V-01.09-win-x64` regenerado, firmado, verificado como `SIGNATURE_OK` y republicado como GitHub Release `latest`. SHA-256 ZIP `A1F6D5A6BBEFAD7C05C8CBFBB09046A5B9C9F5DBCE5E5E1FB0D7DA41DC7E8061`.

### 2026-06-01 - Rotacion de clave de firma de releases

- No se encontro la clave privada historica `ATLAS_RELEASE_SIGNING_PRIVATE_KEY_PEM` en repo, entorno ni rutas locales razonables.
- Se genero un par RSA 4096 nuevo para firmar releases `V-01.09+`.
- La clave publica nueva reemplaza la anterior en `Instalar-AtlasBalance.ps1` y `appsettings.Production.json.template`.
- La clave privada queda fuera de Git en `tmp-release-signing-key/atlas-release-private.pem`; debe copiarse a GitHub Secret `ATLAS_RELEASE_SIGNING_PRIVATE_KEY_PEM` y guardarse en gestor de secretos.
- Huella SHA-256 de la clave publica nueva: `1762B5DFD784A0947EC0F191D38BC28D3AC7ED6EA7BA63902CEB31C0616242B4`.
- `Build-Release.ps1` empaqueta el frontend desde una carpeta temporal y lo inyecta en el `api/wwwroot` publicado, sin tocar `frontend/dist` ni el `wwwroot` fuente bloqueable.
- Paquete local generado y firmado: `AtlasBalance-V-01.09-win-x64.zip`; SHA-256 `A1F6D5A6BBEFAD7C05C8CBFBB09046A5B9C9F5DBCE5E5E1FB0D7DA41DC7E8061`; firma verificada localmente como `SIGNATURE_OK`.
- Publicado GitHub Release `V-01.09-win-x64` como `latest`: `https://github.com/AtlasLabs797/AtlasBalance/releases/tag/V-01.09-win-x64`.
- Nota operativa: instalaciones con clave publica vieja no verificaran paquetes firmados con la nueva hasta actualizar su `UpdateSecurity:ReleaseSigningPublicKeyPem`.

### 2026-06-01 - Auditoria profunda de seguridad, bugs y release gate

- Autenticacion: `REFRESH_TOKENS` guarda `security_stamp`; refresh y cambio de password rechazan tokens emitidos antes de una rotacion de seguridad/MFA/password reset.
- MFA: el cambio de password exige garantia MFA de la sesion actual para cualquier usuario sujeto a MFA, incluso si todavia esta en setup.
- RLS: nueva migracion reasegura `REVISION_EXTRACTO_ESTADOS` con RLS forzado y cierra lecturas de integraciones `escritura` sobre titulares/cuentas.
- Tipos de cambio: una conversion sin tasa ya falla de forma explicita con `409`; antes devolvia el importe 1:1 y podia falsear dashboards/OpenClaw.
- Importacion: la huella de deduplicacion ignora columnas extra no financieras; importaciones y movimientos de plazo fijo reevalúan alertas de saldo bajo tras guardar.
- IA: las preguntas por `titulares` agrupan por titular/divisa, no por cuenta escondida bajo etiqueta de titular.
- Release/update: `Build-Release.ps1` usa el SDK local `.dotnet` si existe; `Actualizar-AtlasBalance.ps1` restaura binarios automaticamente si falla `/api/health` y backfillea configuracion no secreta en instalaciones antiguas.
- CI/CD: agregado workflow manual `Release` que ejecuta verify, exige secreto de firma y publica/actualiza GitHub Release como `latest`.
- Verificacion: tests focalizados 136/136 OK; suite backend sin Docker/Testcontainers 276/276 OK; backend Release build OK; frontend lint/build OK; scripts PowerShell parse OK; secret scan OK.
- Estado de publicacion: paquete `V-01.09-win-x64` generado, firmado y publicado como GitHub Release `latest`. Tests PostgreSQL reales siguen bloqueados por Docker daemon no disponible.

### 2026-05-22 - Actualizacion one-click de paquete completo

- `ActualizacionService` mantiene GitHub `releases/latest` como origen y pasa al Watchdog la raiz del paquete validado, no la subcarpeta `api`.
- Watchdog valida paquete completo y aplica `api`, `watchdog`, scripts, wrappers, `VERSION` y `atlas-balance.runtime.json`.
- Si Watchdog corre desde la instalacion real en Windows, lanza un helper PowerShell no interactivo que ejecuta `scripts/Actualizar-AtlasBalance.ps1`; eso evita intentar copiar sobre su propio binario vivo.
- `ConfiguracionPage` tolera la caida temporal de API durante el reinicio y espera el estado final del Watchdog.
- Verificacion: bloque update/watchdog 26/26 OK; frontend lint/build OK; backend sin Docker/Testcontainers 270/270 OK.
- Estado: GitHub `latest` ya apunta a `V-01.09-win-x64`; Docker/Testcontainers sigue no disponible; falta validacion Windows real del helper externo antes de venderlo como upgrade 100% probado desde instalaciones antiguas.

### 2026-05-22 - Verificacion de actualizacion one-click desde GitHub

Nota: diagnostico previo al fix de paquete completo. El estado actual queda en la entrada anterior de esta misma version.

- Se comprobo la ruta `Configuracion > Sistema > Actualizar ahora` contra codigo, tests focalizados y el release real de GitHub.
- En el diagnostico original, GitHub `releases/latest` respondia con `V-01.06-win-x64`; esto quedo corregido el 2026-06-01 al publicar `V-01.09-win-x64` como `latest`.
- El flujo in-app descarga y valida el ZIP completo, pero pasa al Watchdog solo la carpeta `api`; no actualiza Watchdog, scripts instalados, wrappers, `VERSION` raiz ni `atlas-balance.runtime.json`.
- Tests focalizados `ActualizacionServiceTests|AutoUpdateJobTests|WatchdogClientServiceTests|WatchdogOperationsServiceTests`: 23/23 OK. Cobertura util para seguridad del paquete y sincronizacion parcial, insuficiente para prometer update completo.
- Estado actualizado: el redisenio de update online completo ya esta implementado y publicado como `latest`; siguen pendientes validar Windows real y resolver bootstrap para Watchdog antiguos.

### 2026-05-22 - Verificacion threat model con subagentes

- Se revisaron autenticacion/sesion, autorizacion/aislamiento, OpenClaw, ficheros/admin/watchdog y frontend/configuracion con subagentes.
- Hallazgo corregido: cambio de contrasena podia emitir un refresh token con `mfa_verified_at` falso si la sesion actual era pre-MFA.
- `CambiarPassword` pasa la cookie `refresh_token`; `AuthService` exige y preserva la garantia MFA existente cuando MFA es obligatorio.
- Nueva migracion `20260522103000_HardenRlsSoftDeleteBackstop` endurece RLS frente a soft-delete para usuario/integracion y datos dependientes de cuenta/titular.
- Exportaciones/backups validan ruta absoluta, extension y raiz permitida antes de tocar disco o invocar restauracion por Watchdog.
- Verificacion: auth/controladores 27/27 OK; bloque seguridad no-Docker 88/88 OK; suite backend sin Docker/Testcontainers 269/269 OK.
- Pendiente de release: `RowLevelSecurityTests` y concurrencia PostgreSQL siguen bloqueados hasta tener Docker/Testcontainers operativo.

### 2026-05-20 - Revision threat model e import/export soft-delete

- El threat model recibido se reviso como checklist de seguridad; venia duplicado y no era una lista atomica de bugs.
- Hallazgo corregido: importacion y exportacion podian operar sobre cuentas activas cuyo titular estaba soft-deleted si se llamaba con `cuentaId` directo.
- `ImportacionService`, exportacion manual y exportacion mensual exigen ahora titular activo.
- `ActualizacionService` corta descargas de paquetes durante streaming si superan el limite; `UpdateSecurity:MaxUpdatePackageBytes` permite bajarlo por instalacion sin superar el maximo productivo de 300 MB.
- Verificacion: `ImportacionServiceTests|ExportacionServiceTests|ActualizacionServiceTests` 63/63 OK con SDK local.
- Pendiente de release: Docker/Testcontainers y E2E autenticado real.

### 2026-05-20 - Correccion fuga de diagnosticos internos IA

- Confirmado y corregido el hallazgo `AI provider network errors leak internal diagnostics`.
- Los errores de red de OpenRouter/OpenAI ya no devuelven `Detalle tecnico` ni mensajes crudos de transporte al usuario.
- La auditoria IA conserva solo codigos seguros de transporte (`tls_certificate`, `proxy_unavailable`, `dns_resolution_failed`, `connection_refused`, `network_error`).
- `Directory.Build.props` excluye `.local-build`/`.codex-build` de MSBuild y `.gitignore` ignora `.local-build` anidados.
- Verificacion: regresion focalizada 1/1 OK, `AtlasAiServiceTests` 62/62 OK y `git diff --check` OK.
- Pendiente de release: suite completa, Docker/Testcontainers y E2E autenticado real.

### 2026-05-20 - Cierre bypass MFA en refresh token

- `REFRESH_TOKENS` incluye `mfa_verified_at` para distinguir sesiones que completaron MFA de sesiones legacy/pre-MFA.
- `RefreshTokenAsync` revoca y rechaza con `401` los refresh tokens sin garantia MFA cuando `RequireMfaForWebUsers` esta activo.
- La rotacion conserva `mfa_verified_at`, por lo que el refresh legitimo tras MFA sigue funcionando aunque el usuario no marque `Recordar dispositivo`.
- Verificacion: reproduccion previa fallida del exploit, `AuthServiceTests` 18/18 OK y suite backend sin Docker/Testcontainers 261/261 OK.
- Pendiente de release: ejecutar Testcontainers y E2E autenticado real.

### 2026-05-20 - Hardening de login contra DoS por IP compartida

- Confirmado y corregido el hallazgo `Per-client login throttle enables unauthenticated DoS`.
- El contador cliente/IP ya no bloquea credenciales validas antes de verificar password.
- Los intentos invalidos siguen limitados por email+cliente y cliente/IP.
- Un login correcto limpia tambien el contador cliente/IP.
- `ForwardedHeaders` queda activado con `KnownProxies`/`KnownNetworks` configurables para despliegues con proxy inverso.
- Regresiones agregadas en `AuthServiceTests`: DoS de IP compartida y limpieza de contador post-login.
- Verificacion: `AuthServiceTests` 20/20 OK con SDK local; suite backend sin Docker/Testcontainers 267/267 OK; `git diff --check` OK en archivos tocados.
- Pendiente general: suite completa, Docker/Testcontainers y E2E autenticado antes de release final.

### 2026-05-20 - Logout limpia MFA recordado

- `POST /api/auth/logout` vuelve a borrar `mfa_trusted`.
- El dispositivo MFA recordado baja de 90 a 62 dias.
- El administrador decide si se permite recordar dispositivos mediante `mfa_remember_device_enabled`; por defecto queda apagado.
- Si la politica esta apagada, el backend ignora `mfa_trusted`, exige TOTP y limpia la cookie.
- Tests focalizados Auth/Configuracion: 29/29 OK; frontend lint/build OK.
- Pendiente de empaquetado: la sincronizacion local de `wwwroot` quedo bloqueada por `Access denied`; regenerar assets con permisos antes de publicar release.
- El fallo independiente `RefreshToken_Should_Reject_PreMfa_Token_When_Mfa_Becomes_Required` queda cerrado por el bloque de bypass MFA de esta misma version.

### 2026-05-20 - Fix de compatibilidad ZIP en actualizador

- `ActualizacionService.TryExtractPackageSafely` acepta entradas raiz inocuas (`.` / `./`) sin aflojar Zip Slip.
- Regresiones anadidas:
  - Paquete firmado con entrada raiz `.`.
  - Paquete firmado con entrada raiz `./`.
  - Paquete firmado con `../evil.txt`, que debe rechazarse sin llamar al Watchdog.
- Verificacion:
  - Reproduccion previa: `ActualizacionServiceTests` fallaba con `rootDirectoryEntry: "."`.
  - Tras el fix: `ActualizacionServiceTests` 13/13 OK.
  - Bloque actualizacion/watchdog 20/20 OK.
- Pendiente no relacionado en ese momento: suite backend sin Docker/Testcontainers quedaba en 256/258 por fallos ajenos de IA/Auth; el fallo Auth queda cerrado por el bloque de bypass MFA.

### 2026-05-20 - Apertura de version V-01.09

- `version_actual.md` pasa a declarar `V-01.09` como version activa.
- Fuentes runtime actualizadas a `V-01.09` / `1.9.0`.
- `V-01.07` queda documentada como base anterior.
- Rama de trabajo creada: `V-01.09`.
- Verificacion: barrido de referencias activas confirma `V-01.09` / `1.9.0` en fuentes runtime, scripts y documentacion viva.
