# V-01.06

## Estado

Version actual activa.

## Estado actual de publicacion

- Runtime y versionado apuntan a `V-01.06`.
- Gate backend Docker/Testcontainers cerrado el 2026-05-12: suite completa 225/225 OK.
- Release final sigue condicionado al E2E autenticado con PostgreSQL real y datos de volumen registrado en `Documentacion/REGISTRO_BUGS.md`.
- Si ese E2E no se ejecuta, publicar solo como pre-release/RC. Venderlo como final seria humo.

## Base

- Version anterior publicada: `V-01.05`.
- Rama base local: `V-01.05`.
- Rama de trabajo actual: `V-01.06`.

## Alcance

- Rama de trabajo para nuevas funciones y correccion de errores posteriores a `V-01.05`.
- Aplicacion Atlas Balance empaquetable para Windows x64.
- Backend ASP.NET Core 8, frontend React/Vite y Watchdog.
- Paquetes de release en `Atlas Balance/Atlas Balance Release`.
- Documentacion centralizada en `Documentacion`.

## Prioridad

- Corregir bugs antes de sumar funciones vistosas.
- Mantener cada cambio trazado en tickets, bugs o documentacion tecnica.
- No publicar release sin build limpio y verificaciones relevantes.
- No meter artefactos de release en commits normales salvo que el objetivo sea publicar paquete.

## Instrucciones para cambios

- Asociar cualquier cambio nuevo a `V-01.06`.
- Actualizar `Documentacion/DOCUMENTACION_CAMBIOS.md` en cada sesion.
- Actualizar `Documentacion/DOCUMENTACION_TECNICA.md` si cambia codigo, estructura, scripts o configuracion.
- Actualizar `Documentacion/DOCUMENTACION_USUARIO.md` si cambia instalacion, uso, configuracion o ubicacion de paquetes.
- Consultar `Documentacion/LOG_ERRORES_INCIDENCIAS.md` antes de resolver errores.
- Mantener `Documentacion/REGISTRO_BUGS.md` al dia.
- No documentar contrasenas, tokens ni datos privados.

## Cambios registrados

### 2026-05-13 - Preparacion de paquete local V-01.06

- Se usan subagentes para revisar versionado, release, estado Git, exclusiones y riesgos de secretos antes de empaquetar.
- `.gitignore` raiz y `Atlas Balance/.gitignore` excluyen artefactos locales `.codex-verify`, `.learnings`, `backend_log.txt` y `.codex-test-obj`.
- Se genera `Atlas Balance/Atlas Balance Release/AtlasBalance-V-01.06-win-x64.zip` con `Build-Release.ps1 -Version V-01.06 -AllowUnsignedLocal`.
- SHA256 del ZIP local: `443E6066E669EC3050161FC7D73B54A2CFBAEBEBCF5FCA83651594A1E86A6F43`.
- No se genera `.zip.sig` porque falta `ATLAS_RELEASE_SIGNING_PRIVATE_KEY_PEM`; por tanto el asset no es publicable como release online.
- Publicacion final sigue bloqueada por firma obligatoria y por el E2E autenticado con PostgreSQL real/datos de volumen si se pretende no marcarlo como RC/pre-release.

### 2026-05-12 - Saneado de datos para entrega

- Se eliminan seeds locales con datos demo de titulares, cuentas, extractos y usuarios.
- Se anaden `scripts/purge-delivery-data.sql` y `scripts/Purge-DeliveryData.ps1` para dejar una base local sin datos operativos antes de publicar.
- La purga borra usuarios, titulares, cuentas, extractos, tokens, auditorias, uso IA, backups/exportaciones registradas y datos dependientes; conserva configuracion base, formatos, divisas, tipos de cambio y migraciones.
- SMTP, claves API externas, OpenRouter/OpenAI, modelo IA y contadores de uso IA quedan vacios o reseteados.
- `.gitignore` bloquea seeds demo y el artefacto local anidado `Atlas Balance/Atlas Balance/`.
- Verificacion: BD local `atlas_balance` saneada; 21 tablas sensibles en `0`, configuracion sensible vacia/reseteada, parser PowerShell OK y barrido sin nombres/hashes demo en rutas publicables.
- Pendiente real: E2E autenticado con PostgreSQL real y ZIP firmado `V-01.06`.

### 2026-05-12 - Microinteracciones emil prepublicacion

- Se aplica `emil-design-eng` con subagentes, limitado a microinteracciones y detalles finos sin rehacer pantallas.
- Se retiran animaciones globales de pagina/nav repetidas, el shell deja de animar layout al colapsar sidebar, `AppSelect` no anima cuando se abre por teclado y los hover con movimiento quedan limitados a puntero fino.
- Los toasts pausan por hover/foco y pestana oculta; su entrada pasa de keyframes a transicion con `@starting-style`.
- Checkbox, chat IA y estados reduced-motion reciben ajustes de tacto y accesibilidad.
- `frontend/dist` queda reconstruido y `wwwroot` sincronizado 65/65, tras podar 27 assets obsoletos de `wwwroot/assets`.
- Verificacion: frontend lint OK, build frontend OK fuera del sandbox, `index_match=True`, `stale_assets=0` y barrido estatico sin `page-surface-in`, `nav-link-pop`, `toast-slide-in` ni `scale(0)`.
- Pendiente real: E2E autenticado con PostgreSQL real/datos de volumen, ZIP firmado `V-01.06` y estado Git local fiable.

### 2026-05-12 - Clarify de errores, labels y estados vacios

- Se aplica la skill local `clarify` con subagentes para revisar errores, labels, empty states y copy confuso.
- Frontend deja de mostrar errores crudos tipo `Network Error`, `Request failed with status code`, `N/A`, `SUCCESS`, `FAILED`, `AUTO`, `MANUAL`, `Flag`, `Check`, `Primer login` o `API key`.
- Backups pasan a mostrarse como `Copias de seguridad`; exportaciones, tokens, alertas, usuarios, cuentas, titulares, formatos, dashboards, extractos y auditoria reciben copy mas claro y accionable.
- Acciones destructivas de alertas/tokens/copias/cuentas/titulares/formatos tienen confirmaciones y labels de carga especificos.
- Backend sanea respuestas `Request invalido`, mensajes que mandaban a revisar logs y errores tecnicos de IA/proveedor.
- Verificacion: frontend lint OK, TypeScript OK, API build OK con salida aislada, build frontend OK fuera del sandbox y barridos estaticos OK.
- Incidencia documentada: `AtlasBalance.API.dll` bloqueado por `.NET Host (9632)` y limpieza de `.codex-build` bloqueada por `Access denied`; carpeta ignorada en Git.
- Pendiente real: E2E autenticado con PostgreSQL real/datos de volumen.

### 2026-05-12 - Polish final prepublicacion UI

- Se aplica la skill local `polish` con apoyo de `impeccable` y subagentes.
- Se limpian trazas de consola productiva y se corrigen copy, acentos, labels accesibles, estados de foco y contraste.
- `DatePickerField` restaura foco correctamente; sidebar colapsada conserva badges accesibles; toasts largos no rompen layout.
- Chat IA mejora contraste del mensaje de usuario y overlays dejan de usar blur caro.
- Tokens de diseno documentados quedan alineados con los tokens runtime.
- `frontend/dist` queda reconstruido y `wwwroot` sincronizado 65/65, con poda acotada de chunks JS obsoletos.
- Verificacion: frontend lint OK, TypeScript OK, build frontend OK fuera del sandbox y barridos estaticos OK sobre bundle servido.
- Pendiente real: E2E autenticado con PostgreSQL real/datos de volumen y paquete firmado `V-01.06`.

### 2026-05-12 - Optimize prepublicacion de pantallas pesadas

- Se aplica la skill local `optimize` con subagentes para frontend, backend/API y readiness de publicacion.
- `Dashboard` deja de cargar su modulo pesado si el usuario no tiene permiso de acceso.
- `Cuentas` y `Titulares` cargan graficas bajo demanda y comparten `TitularSaldoBarChart`.
- Busquedas de cuentas/titulares quedan debounceadas para no disparar llamadas por cada tecla.
- `Importacion` limita previsualizacion/deteccion de separador a muestras pequenas, pagina la validacion en bloques de 200 filas y usa `Set` para seleccion.
- Auditoria OpenClaw evita materializar todos los extractos del scope antes de paginar.
- Totales mensuales por cuenta se agrupan en SQL.
- Revision recibe indice trigram GIN sobre `lower(concepto)` para busquedas textuales.
- `frontend/dist` queda sincronizado con `backend/src/AtlasBalance.API/wwwroot` (`65/65` archivos).
- Verificacion: frontend lint OK, TypeScript OK, build frontend OK fuera del sandbox, build API OK, tests focalizados OpenClaw/Revision 8/8 OK y `wwwroot` sincronizado.
- Pendiente real: E2E autenticado con PostgreSQL real/datos de volumen y paquete firmado `V-01.06`; en esta sesion no hay `ATLAS_RELEASE_SIGNING_PRIVATE_KEY_PEM`.

### 2026-05-12 - Adapt responsive desktop/tablet/mobile prepublicacion

- Se aplica la skill local `adapt` con subagentes sobre desktop/tablet, mobile/touch targets y scroll horizontal accidental.
- Breakpoints alineados: mobile `<768px`, tablet `768-1023.98px`, desktop `>=1024px`.
- `DatePickerField` evita colision horizontal y en touch pasa a superficie tipo bottom sheet con controles de 44px.
- Chat IA, Revision, login, extractos, importacion, usuarios y tablas de cuenta elevan targets tactiles compactos.
- `TitularDetailPage` y `FormatosImportacionPage` corrigen tablas sin scroll local.
- Hoja de cuenta reduce anchuras por breakpoint y mantiene scroll interno, no scroll global de pagina.
- `frontend/dist` queda sincronizado con `backend/src/AtlasBalance.API/wwwroot`.
- Verificacion: `npm.cmd run lint` OK, TypeScript OK, build frontend OK fuera del sandbox, `wwwroot` 61/61, Playwright `setContent` en 1366/1024/768/390/320 sin overflow global; en tablet/mobile touch, sin targets bajo 44px.
- Pendiente: E2E autenticado con datos reales/volumen sigue abierto en `REGISTRO_BUGS.md`.

### 2026-05-12 - Hardening de estados borde y gate backend completo

- Se aplica la skill local `harden` con subagentes sobre estados vacios, errores API, permisos, textos largos, overflow, datasets grandes y casos feos.
- Frontend unifica mensajes API con `errorMessage.ts`, cubriendo validaciones ASP.NET, errores sin respuesta, 401/403/404/409/413/429/5xx y truncado de textos largos.
- Importacion deja de mostrar "sin cuentas" cuando falla la API de contexto y bloquea doble confirmacion.
- Extractos muestra fallos reales de filas/resumen/preferencias/auditoria, revierte preferencias de columnas fallidas y evita ocultar la ultima columna visible.
- Cuenta detalle elimina el limite silencioso de 500 movimientos y anade paginacion real.
- IA y permisos muestran estados vacios utiles en vez de paneles vacios o redirecciones silenciosas.
- Revision oculta acciones de escritura si el usuario no puede editar la cuenta/titular de la fila; backend expone `titular_id` para calcularlo.
- CSS endurece tablas, modales, selectores, errores e importes grandes contra overflow y textos largos.
- Backend anade manejador global de excepciones JSON saneado.
- Se registra correctamente la migracion `20260512110000_HardenReleaseSecurityPermissions` con `.Designer.cs`; antes EF no la descubria.
- Se cierra el test obsoleto de importacion que esperaba `Nombre de columna extra duplicado`.
- Verificacion: frontend lint OK, TypeScript OK, build frontend OK, build backend OK, `RevisionServiceTests` 5/5 OK, backend no Docker 223/223 OK, suite backend completa con Docker/Testcontainers 225/225 OK y `wwwroot` sincronizado 61/61 archivos.
- Pendiente real: E2E autenticado/visual con datos reales.

### 2026-05-12 - Auditoria UI prepublicacion

- Auditoria con skill `audit` y subagentes para accesibilidad/responsive, rendimiento y theming/anti-patrones.
- Modales/sheets criticos pasan a foco controlado con `useDialogFocus`: foco inicial, trap, Escape y restauracion.
- Formularios y controles de importacion/login/cambio de password/alta de lineas incorporan labels y estados ARIA.
- Disponibilidad de IA centralizada en `iaAvailabilityStore`; se elimina polling duplicado y `AiChatPanel` queda en chunk diferido.
- `DashboardPrincipal` incluye `saldos_por_cuenta`; `Cuentas` elimina el N+1 por titular y la carga auxiliar de 2000 cuentas.
- QR MFA se carga bajo demanda; el shell deja de re-renderizar cada segundo por timeout de sesion.
- Graficas usan tokens de tema y el render traduce colores legacy a tokens para mantener contraste.
- CSS corrige tokens inexistentes, touch targets, radios/sombras excesivas y blur caro en shell/auth.
- `Build-Release.ps1` aborta si no puede limpiar `wwwroot`; `dist` queda sincronizado con `wwwroot`.
- Verificacion: frontend lint OK, TypeScript OK, API build OK con salida aislada, build frontend OK fuera del sandbox, `wwwroot` 62/62 archivos y sin assets obsoletos/N+1 detectados.
- Pendiente de release global: el gate Docker/Testcontainers quedo cerrado en el hardening posterior de la misma fecha; E2E autenticado sigue abierto.

### 2026-05-12 - Auditoria de seguridad prepublicacion

- Se corrigen fugas de JWT/cookies en logs frontend y se limpia el log local afectado.
- MFA recordado pasa a opt-in explicito y 30 dias.
- Login suma rate limiting por IP para password spraying.
- `/api/health` deja de exponer metadatos operativos.
- Lectura financiera queda ligada a `PuedeVerCuentas`; `PuedeImportar` y permisos de escritura ya no abren GETs de cuentas/extractos.
- Exportacion manual exige lectura; revision de comisiones/seguros exige `PuedeEditarLineas`.
- RLS se endurece con scopes firmados y politicas especificas de exportacion/revision.
- Release ahora exige `npm ci`, lockfiles NuGet, `dotnet restore --locked-mode` y firma salvo prueba local explicita.
- CI fija runtimes y amplia secret scan high-confidence.
- Instalador endurece ACL de secretos y valida identificadores PostgreSQL.
- Verificacion: secret scan 0 findings, frontend lint OK, frontend build OK fuera del sandbox, `npm audit` 0 vulnerabilidades, NuGet vulnerable audit 0 vulnerabilidades, restore locked OK, build .NET OK, tests no Docker 34/34 OK.
- Estado posterior: `RowLevelSecurityTests` y `ExtractosConcurrencyTests` quedaron cubiertos por la suite completa 225/225 con Docker/Testcontainers fuera del sandbox.

### 2026-05-12 - Revision: Todas excluye descartadas

- `Todas/Todos` deja de mostrar filas `DESCARTADA`.
- En comisiones, `Todas` muestra pendientes y devueltas.
- En seguros, `Todos` muestra pendientes y correctos.
- Las descartadas quedan solo en el filtro `Descartadas/Descartados`.
- El boton de descarte queda como una cruz sin texto visible, manteniendo `aria-label` y `title`.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd exec tsc -- --noEmit` OK, `RevisionServiceTests` 5/5 OK, `npm.cmd run build` OK fuera del sandbox. API local saludable PID `9632`.

### 2026-05-12 - Revision descarta falsos positivos

- `Revision` permite marcar una deteccion automatica como `No es comision` o `No es seguro`.
- Backend y frontend anaden el estado persistido `DESCARTADA` para `COMISION` y `SEGURO`.
- El filtro de estado incorpora `Descartadas/Descartados`.
- Las filas descartadas se pueden restaurar a `PENDIENTE`.
- El bundle frontend se recompila y se sincroniza con `wwwroot`.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd exec tsc -- --noEmit` OK, `npm.cmd run build` OK fuera del sandbox, `RevisionServiceTests` 5/5 OK fuera del sandbox con `-p:OutDir=C:\tmp\atlas-revision-discard-test-out\`. API local saludable PID `32520`.

### 2026-05-12 - Dashboard principal con porcentajes KPI compactos

- Los KPIs del dashboard principal dejan de mostrar `vs inicio` y `vs anterior` bajo el porcentaje de variacion.
- El calculo, signo y color de la variacion se mantienen sin cambios.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd exec tsc -- --noEmit` OK y busqueda estatica sin restos en `DashboardPage.tsx`.

### 2026-05-12 - Fix 500 en Revision al cargar comisiones

- `/api/revision/comisiones` deja de fallar con HTTP 500 por una query no traducible de EF/Npgsql.
- `RevisionService` reemplaza el record posicional intermedio `RevisionRawRow(...)` por una clase con propiedades `init` y proyeccion por inicializador.
- El filtro por importe de comisiones sigue ejecutandose en SQL junto con la paginacion.
- Se anade una regresion Npgsql que genera SQL con `ToQueryString()` para cubrir el caso que InMemory no detectaba.
- Verificacion: `RevisionServiceTests` 5/5 OK fuera del sandbox con `-p:OutDir=C:\tmp\atlas-revision-test-out\`. API local saludable tras reinicio, PID `42848`.

### 2026-05-12 - Fix etiquetas laterales del grafico Evolucion

- `EvolucionChart` reserva mas anchura para el eje Y y evita que importes compactos como `15,6 M EUR` queden cortados.
- El ancho del eje se calcula con los valores de las series, los extremos del dominio y cero, cubriendo ticks generados por Recharts.
- Los ticks del grafico usan estilo explicito con fuente monoespaciada/tabular para mantener lectura y estimacion estable.
- Ajuste posterior: el eje pasa a una reserva adaptativa por anchura estimada de etiqueta, con margen izquierdo reducido para no dejar hueco excesivo cuando los datos no lo necesitan.
- Verificacion: `npm.cmd run lint` OK y `npm.cmd exec tsc -- --noEmit` OK. No se arranco Vite ni servidor temporal por incidencia conocida de `spawn EPERM`.

### 2026-05-11 - Cierre del mensaje recurrente `respuesta malformada`

- `AtlasAiService` deja de devolver al usuario el mensaje generico `El proveedor de IA devolvio una respuesta malformada`.
- Las respuestas no compatibles se muestran como `respuesta de chat compatible (categoria)` para que el fallo sea accionable y no parezca el mismo error opaco de siempre.
- El `catch (JsonException)` global ya no se atribuye sin mas al proveedor; queda registrado como `provider_response_processing_error` con `json_processing_error`.
- El parser acepta payloads `data:` tipo SSE si el proveedor ignora `stream=false`, `choices[0].delta.content`, `output_text` top-level y partes de texto anidadas (`text.value`, `output_text`, `content`).
- Verificacion: `AtlasAiServiceTests|ConfiguracionControllerTests` 68/68 OK fuera del sandbox con salida aislada en `C:\tmp\atlas-ai-test-bin-provider-parser-loop`.

### 2026-05-11 - IA financiera: rankings por cuenta calculados en backend

- `AtlasAiService` ya no delega en OpenRouter los rankings soportados por cuenta/titular/divisa.
- Se anade un resolvedor determinista para preguntas de ranking de gastos, ingresos o neto por cuenta en mes actual, ultimos 30 dias, mes anterior, trimestre actual y ano actual.
- Para `Que cuentas han tenido mas gastos este trimestre?`, el backend aplica `ApplyCuentaScope`, agrupa movimientos por titular/cuenta/divisa, calcula `gastos = -SUM(monto < 0)`, cuenta movimientos de gasto y devuelve el ranking sin llamada HTTP al proveedor.
- La respuesta incluye periodo exacto, secciones por divisa, cuenta, titular, importe y numero de movimientos. Si no hay datos, devuelve un mensaje claro.
- Las consultas deterministas registran auditoria `IA_CONSULTA` con coste/tokens `0`, sin prompt, sin respuesta completa y sin datos financieros crudos.
- La ruta LLM restante rechaza contenido visible de analisis interno en ingles y el prompt recuerda no recalcular rankings/agregados ya calculados.
- Verificacion: `AtlasAiServiceTests|ConfiguracionControllerTests` 66/66 OK fuera del sandbox con salida aislada en `C:\tmp\atlas-ai-test-bin-financial-ranking`.
- Incidencia: el primer test fallo por `AtlasBalance.API.dll` en uso; `C:\tmp` fallo dentro del sandbox por `Access denied`; se ejecuto con aprobacion fuera del sandbox.

### 2026-05-11 - Fix parser OpenRouter para respuestas 200 sin `message.content` string

- `AtlasAiService` deja de llamar `malformada` a cualquier respuesta 200 que no sea `choices[0].message.content` string.
- El parser acepta `content` string, arrays de partes `{ type/text }` y fallback `choices[0].text`.
- Se detectan y explican aparte: error embebido con HTTP 200, `refusal`, `finish_reason=content_filter`, `finish_reason=length`, tool calls sin texto, `choices` vacio y JSON/shape realmente malformado.
- Las peticiones IA declaran `stream=false`, `Accept: application/json` y `X-OpenRouter-Title: Atlas Balance`.
- HTTP 429/503 usa `Retry-After` para decir al usuario cuando reintentar, sin bloquear el hilo esperando.
- Auditoria IA anade `provider_response_error_kind`, `finish_reason`, cliente HTTP, fallback y detalle saneado sin guardar prompt, respuesta completa ni API key.
- Verificacion: `AtlasAiServiceTests|ConfiguracionControllerTests` 61/61 OK fuera del sandbox con salida aislada en `C:\tmp\atlas-ai-test-bin-openrouter-parser`.
- Incidencia: test directo bloqueado por `AtlasBalance.API.dll` en uso y salida `C:\tmp` denegada dentro del sandbox; se ejecuto una vez fuera del sandbox.

### 2026-05-11 - Fix recurrente OpenRouter `Authentication failed`

- El error `Authentication failed, see inner exception` del chat IA queda tratado como fallo de transporte HTTPS/proxy, no como API key mala.
- `Program.cs` no usa proxy automatico como fallback por defecto para IA; OpenRouter/OpenAI salen directo salvo `Ia:UseSystemProxy=true` o `Ia:ProxyUrl` explicito.
- `AtlasAiService` desenvuelve excepciones anidadas y clasifica TLS/certificado, proxy roto, DNS y conexion rechazada; auditoria y mensaje visible usan detalle saneado.
- `appsettings` y el instalador documentan `Ia:ProxyUrl`.
- `Start-BackendDev.ps1` corrige el choque con la variable automatica `$PID` de PowerShell durante el reinicio seguro.
- Verificacion: `AtlasAiServiceTests` 42/42 OK fuera del sandbox con salida aislada en `C:\tmp\atlas-ai-test-bin`.

### 2026-05-11 - Fix Network Error en login y arranque backend

- El frontend deja de compilar una API absoluta con `VITE_API_URL`; todas las llamadas usan `/api` same-origin.
- `frontend/.env.local` queda con aviso de no fijar `localhost`.
- Build frontend recompilado y `wwwroot` sincronizado; los bundles servidos no contienen `localhost:5000`.
- `Start-BackendDev.ps1` arranca la API en desarrollo con limpieza de proxies, PID/logs y healthcheck.
- `Start-Dev.ps1`, `Launch-AtlasBalance.ps1` y BATs dejan de vender un backend vivo sin comprobar `/api/health`.
- `/api/health` devuelve version, PID, entorno y `started_at`.
- Verificacion: lint frontend OK; build frontend OK fuera del sandbox; build API OK; `localhost:5000/api/health` y `localhost:5173/api/health` OK; busqueda sin URLs absolutas en `dist`/`wwwroot`.

### 2026-05-11 - Chat IA sin razonamiento interno visible

- OpenRouter recibe `reasoning.exclude=true` en todas las rutas del proveedor.
- El prompt de sistema fuerza respuesta final en espanol, sin analisis interno, sin frases tipo `we need to answer` y sin placeholders.
- `AtlasAiService` limpia `<think>`, prefacios de razonamiento, etiquetas `Final:`/`Respuesta final:` y placeholders antes de devolver `Respuesta`.
- Verificacion: `AtlasAiServiceTests` 41/41 OK; `AtlasAiServiceTests|ConfiguracionControllerTests` 47/47 OK. Primer intento bloqueado por API local en PID `25776`, parada y reintentada sin repetir la misma via.
- Pendiente operativo: la API local no quedo reiniciada desde el agente porque el launcher local falla por duplicidad `Path/PATH` o permisos de log; `localhost:5000` queda sin listener.

### 2026-05-11 - Fix OpenRouter 400 por limite de `models`

- OpenRouter rechazo `Auto (gratis permitido)` con `400` porque `models` llevaba seis candidatos y la API solo acepta 3.
- `OpenRouterAutoFallbackModels` queda limitado por `OpenRouterMaxFallbackModels = 3` y envia Nemotron, Gemma y MiniMax.
- gpt-oss, GLM y Qwen siguen en la allowlist y seleccion manual, pero no se envian todos en Auto.
- Se agrega mensaje especifico para el 400 de limite de `models`.
- Verificacion: `AtlasAiServiceTests|ConfiguracionControllerTests` 46/46 OK fuera del sandbox; el test parsea el JSON y valida `models.Count == 3`; API local saludable en `localhost:5000` con PID `25776`.

### 2026-05-11 - Fix red OpenRouter por proxy local roto

- El error de red de OpenRouter venia de variables de entorno `HTTP_PROXY`, `HTTPS_PROXY` y `ALL_PROXY` apuntando a `http://127.0.0.1:9`.
- WinHTTP estaba directo y `openrouter.ai:443` era alcanzable; el problema no era OpenRouter caido.
- Se reinicia la API limpiando esas variables de proxy y dejando logs en `C:\tmp\atlas-api-openrouter.*.log`.
- Verificacion: OpenRouter `/api/v1/models` responde HTTP 200 por salida directa fuera del sandbox; `/api/health` OK; `localhost:5000` escucha en PID `40704`.

### 2026-05-11 - Revalidacion OpenRouter Auto y anti-encallamiento backend

- Se comprobo que el mensaje antiguo de OpenRouter venia de un backend viejo en ejecucion.
- El backend activo en `localhost:5000` queda saludable con PID `20880`.
- El DLL compilado ya no contiene `Se normalizaron modelos obsoletos`; contiene el mensaje nuevo de restricciones y la ruta `models`.
- Se cierra un proceso `dotnet` huerfano de tests.
- `AGENTS.md` y `CLAUDE.md` raiz/subproyecto incorporan regla especifica: no reiniciar backend/API desde `shell_command` con `Start-Process` o `[Diagnostics.Process]` si puede heredar stdout/stderr y dejar la herramienta colgada.
- Verificacion: `AtlasAiServiceTests|ConfiguracionControllerTests` 45/45 OK fuera del sandbox; `/api/health` OK.

### 2026-05-11 - Fix Auto OpenRouter con modelos gratis restringidos

- `Auto` mantiene `openrouter/auto` como valor guardado, pero la llamada ya no usa el plugin `auto-router`.
- Para evitar el 404 `No models match your request and model restrictions`, `AtlasAiService` envia `models` con modelos gratis permitidos como fallback acotado. Ajuste posterior: OpenRouter solo acepta 3 elementos en `models`, asi que Auto queda limitado a tres candidatos.
- El selector muestra `Auto (gratis permitido)` para no vender una eleccion inteligente que OpenRouter no puede hacer con una interseccion vacia de restricciones.
- Los 404 de restricciones de modelo ahora tienen mensaje especifico.
- Verificacion: frontend lint OK; TypeScript OK; `AtlasAiServiceTests|ConfiguracionControllerTests` 45/45 OK fuera del sandbox; build frontend OK fuera del sandbox; `wwwroot` sincronizado.

### 2026-05-11 - Selector IA mas discreto en el chat

- El proveedor y el selector de modelo del chat IA pasan a la cabecera como metadato secundario.
- El chat muestra etiquetas compactas (`Auto`, sin sufijo `(free)`) sin cambiar las etiquetas completas de `Configuracion`.
- El selector baja peso visual: fondo transparente, texto tenue, 32px de alto y foco accesible.
- Verificacion: frontend lint OK; build frontend OK fuera del sandbox; `wwwroot` sincronizado; Playwright estatico OK sin overflow y con selector dentro de cabecera.

### 2026-05-11 - OpenRouter Auto acotado a seis modelos gratis

- `Auto` vuelve a ser la opcion por defecto para OpenRouter y usa `openrouter/auto`.
- En ese intento, la peticion Auto se enviaba con `plugins.auto-router.allowed_models` limitado a `nvidia/nemotron-3-super-120b-a12b:free`, `google/gemma-4-31b-it:free`, `minimax/minimax-m2.5:free`, `openai/gpt-oss-120b:free`, `z-ai/glm-4.5-air:free` y `qwen/qwen3-coder:free`. Queda sustituido por `models` con maximo 3 candidatos.
- El selector de Configuracion y el selector del chat muestran `Auto (elige el mejor)` mas esos seis modelos manuales.
- La auditoria registra el `runtime_model` real cuando OpenRouter lo devuelve en la respuesta.
- Verificacion: frontend lint OK; TypeScript OK; `AtlasAiServiceTests|ConfiguracionControllerTests` 44/44 OK fuera del sandbox; build frontend OK fuera del sandbox; `wwwroot` sincronizado.

### 2026-05-11 - Enter y selector de modelo en chat IA

- `Enter` envia la pregunta desde el textarea del chat IA; `Shift+Enter` mantiene salto de linea.
- El chat muestra un selector de modelo propio, limitado al proveedor configurado.
- El modelo seleccionado se envia por consulta a `/api/ia/chat` y no cambia `ai_model` global.
- El backend valida el modelo solicitado contra la allowlist y bloquea modelos arbitrarios antes de llamar al proveedor.
- La lista de modelos queda centralizada en `frontend/src/utils/aiModels.ts` para Configuracion y chat.
- Verificacion: frontend lint OK; TypeScript OK; `AtlasAiServiceTests` 35/35 OK fuera del sandbox; build frontend OK fuera del sandbox; `wwwroot` sincronizado; Playwright estatico OK con selector visible y sin overflow.

### 2026-05-11 - IA mas permisiva para consultas financieras

- El chat IA permite consultas financieras administrativas sobre gastos globales, ingresos, importes, montos, impuestos, Seguridad Social, comisiones, seguros, recibos, facturas, nominas, cuotas, cargos y cobros.
- El prompt ya no rechaza en bloque vocabulario fiscal/legal cuando la pregunta trata sobre datos financieros propios; solo rechaza asesoramiento legal externo y asuntos fuera de Atlas Balance.
- El contexto IA suma periodos `ultimo mes`/`ultimos 30 dias`, `mes pasado`/`mes anterior` y categorias de impuestos/Seguridad Social y recibos/facturas.
- Verificacion: `AtlasAiServiceTests` 33/33 OK con regresiones para la frase exacta `cual ha sido los gastos globales del ultimo mes`.

### 2026-05-11 - Fix visual y legibilidad del chat IA

- El chat IA deja de mostrar Markdown crudo del proveedor: `**negritas**` y tablas con pipes se transforman en contenido legible.
- Las tablas Markdown se presentan como pares dato/valor, adecuadas para respuestas financieras compactas.
- Los metadatos de uso (`movimientos`, `modelo`, `tokens`, `coste`) quedan en `Detalles de IA` plegable y ya no ensucian la respuesta.
- El panel cambia a layout flexible para que el historial de mensajes use la altura disponible y no parezca cortado.
- El prompt de sistema pide respuestas sin tablas Markdown, pipes ni asteriscos de negrita.
- Verificacion: frontend lint OK; TypeScript OK; `AtlasAiServiceTests` 33/33 OK fuera del sandbox; build frontend OK fuera del sandbox; `wwwroot` sincronizado; Playwright estatico OK sin Markdown crudo ni overflow.

### 2026-05-11 - Fix OpenRouter 404 por allowlist de modelos gratis

- La allowlist de OpenRouter se alinea con los modelos gratis permitidos en la cuenta del usuario.
- `openrouter/auto` queda acotado a la allowlist gratis; la ruta actual usa `models` con maximo 3 candidatos, no Auto Router abierto.
- Las peticiones a modelos gratis se pinchan al proveedor exacto (`open-inference/int8` o `google-ai-studio`) con fallback desactivado para no salir de la allowlist.
- Se retira `provider.zdr=true` para esos modelos gratis porque OpenRouter no los lista como endpoints ZDR; la auditoria registra `zero_data_retention=false`.
- El selector de `Configuracion > Revision e IA` muestra `Auto` y los modelos gratis permitidos.
- Verificacion: API publica de OpenRouter revisada; build API OK; `AtlasAiServiceTests` 29/29 OK; frontend lint/build OK; `wwwroot` sincronizado; API reiniciada y `/api/health` 200 `healthy`.

### 2026-05-10 - Fix OpenRouter 404 por modelo obsoleto

- `openrouter/auto` deja de enviar `anthropic/claude-3.5-sonnet` en `allowed_models`; ese slug ya no existe en OpenRouter.
- Auto Router usa patrones actuales para reducir roturas por cambios de slug.
- La allowlist directa de OpenRouter y el selector frontend se actualizan con modelos vigentes.
- El runtime normaliza el slug obsoleto conocido a `openrouter/auto`, pero mantiene bloqueo para modelos desconocidos.
- Los errores HTTP 404 del proveedor ahora se explican como modelo no encontrado y el detalle del proveedor se redacta antes de auditarse o mostrarse.
- Verificacion: OpenRouter `/api/v1/models` confirma nuevos slugs; build API OK; `AtlasAiServiceTests` 25/25 OK; frontend lint/build OK; `wwwroot` sincronizado; API reiniciada y `/api/health` 200 `healthy`.

### 2026-05-10 - Fix error de red del proveedor IA

- `/api/ia/chat` deja de tratar todos los fallos de transporte como una caja negra.
- `AtlasAiService` prueba el cliente HTTP principal y reintenta con `openrouter-fallback`/`openai-fallback`. En el ajuste inicial se uso modo proxy opuesto; queda sustituido por salida directa por defecto y proxy solo explicito.
- La auditoria IA registra cliente usado, si hubo fallback y detalle tecnico sanitizado, sin prompt, respuesta completa ni API key.
- El error visible ahora indica proveedor, host y detalle tecnico corto para diferenciar proxy, sandbox, DNS/TLS o bloqueo de red.
- Verificacion: conectividad HTTPS real fuera del sandbox OK (`OpenRouter` 200, `OpenAI` 401 esperado sin token); build API OK; `AtlasAiServiceTests` 24/24 OK; API reiniciada y `/api/health` 200 `healthy`.

### 2026-05-10 - Protocolo anti-encallamiento de agentes

- Se revisa el patron de atascos recientes y se documenta que la causa principal era repetir vias ya fallidas del entorno.
- `CLAUDE.md`, `AGENTS.md`, `Atlas Balance/CLAUDE.md` y `Atlas Balance/AGENTS.md` incorporan reglas operativas: maximo dos intentos por via, comandos finitos con timeout, alternativas estaticas y bloqueo documentado.
- Quedan catalogados como fallos conocidos: Vite/Rolldown/Chromium con `spawn EPERM`, servidores temporales vivos, `robocopy /MIR`, `wwwroot` bloqueado, `apphost.exe` en uso, Docker/Testcontainers sin daemon y limpiezas con `Access denied`.
- Verificacion: busqueda documental de la seccion `Protocolo anti-encallamiento`; no aplica build ni tests de runtime.

### 2026-05-10 - Fix definitivo del 500 en chat IA

- `/api/ia/chat` deja de fallar en resumenes mensuales, ingresos/gastos, comisiones, seguros y movimientos relevantes.
- `AtlasAiService` reemplaza las consultas sobre `AiExtractoRow` proyectado por consultas traducibles sobre `Extractos`/`Cuentas`.
- Se anade regresion para construir contexto de periodo y categorias sin romper en proveedor SQL real.
- Se refuerza la regla de trabajo para cortar limpiezas/verificaciones con errores repetitivos de permisos o salida masiva.
- Verificacion: `AtlasAiServiceTests` 22/22 OK; build API OK; verificador temporal contra PostgreSQL real OK con rollback; `/api/health` responde `healthy`.

### 2026-05-10 - Alineacion simetrica de Extractos

- `Extractos` usa un ancho propio de `1600px` para que la hoja financiera no quede encajonada en el max-width generico de paginas.
- Header, formulario manual y tabla comparten el mismo borde izquierdo/derecho.
- El titulo queda centrado verticalmente contra el bloque de filtros.
- Se evita que la tabla o columnas extra fuercen overflow del contenedor mediante `min-width: 0`.
- Verificacion: frontend lint OK; build OK fuera del sandbox por `spawn EPERM` conocido de Vite dentro del sandbox; `wwwroot` sincronizado; Playwright confirma margenes simetricos `98px/98px` y sin overflow horizontal.

### 2026-05-10 - Alineacion del header en detalle de cuenta

- El header del dashboard de cuenta pasa a una grilla especifica de dos filas.
- El titulo ocupa la fila superior izquierda y el panel de acciones `Periodo/Volver/Extractos/Importar` arranca en esa misma fila para quedar mas alto.
- El panel de acciones se estira sobre la altura combinada de titulo y ficha para que el borde inferior tambien quede alineado.
- La ficha `Titular/Banco/IBAN` queda debajo del titulo en la columna izquierda.
- En pantallas estrechas la grilla vuelve a una sola columna para conservar lectura vertical.
- Verificacion: frontend lint OK; build OK fuera del sandbox por `spawn EPERM` conocido de Vite dentro del sandbox; Playwright headless con CSS compilado confirma `topDelta=0`, `bottomDelta=0.01` y panel de acciones `75.44px` por encima de la ficha; `wwwroot` sincronizado.

### 2026-05-10 - UX de seleccion, insercion y flag en desglose de cuenta

- El desglose de cuenta elimina la columna visual `Flag` y mueve el marcado con flag a un boton superior con icono de banderola.
- La seleccion por checkbox pasa a ser la primera columna visible de la tabla.
- La insercion intermedia deja de mostrarse como boton textual por fila: aparece un icono `+` discreto al pasar el cursor entre lineas.
- El icono `+` queda anclado a la columna de numero de fila, no a la columna de seleccion, para no tapar ni dificultar el click del checkbox.
- La accion de eliminar por fila se retira de la tabla; el borrado se hace desde la accion superior de papelera sobre filas seleccionadas.
- Check, seleccion, flag, insercion y borrado actualizan estado local sin navegacion, submit ni recarga completa de la pagina.
- Se refuerza la regla operativa: no arrancar servidores Node/Vite/HTTP de larga duracion desde `shell_command` para validar UI; usar comandos finitos o Playwright `setContent`.
- Verificacion: frontend lint OK; build OK fuera del sandbox por incidencia conocida `spawn EPERM` de Vite dentro del sandbox; `wwwroot` sincronizado con copia no destructiva.

### 2026-05-10 - Restriccion tematica del chat IA

- El chat IA queda limitado a Atlas Balance, funcionamiento de la app y datos financieros disponibles.
- `AtlasAiService` bloquea preguntas claramente fuera de ambito antes de llamar al proveedor externo y audita `IA_CONSULTA_BLOQUEADA` con motivo `out_of_scope`.
- El prompt de sistema ordena rechazar recetas, cocina, programacion, noticias, ocio, salud, legal y cualquier tema externo.
- Verificacion: `AtlasAiServiceTests` 21/21 OK con `-p:UseAppHost=false` por bloqueo local del apphost `.exe`.

### 2026-05-10 - Cierres de UI con icono X

- Los botones visibles `Cerrar` usados para cerrar modales, toast y sheet movil pasan a una X mediante `CloseIconButton`.
- El cierre mantiene `aria-label`/`title`, target tactil de control y estilos coherentes con el sistema Atlas.
- Se mantienen `Cerrar sesion` y `Cancelar` como texto porque son acciones con significado propio, no cierres decorativos.
- `TokenCreatedModal` mueve el cierre al header y deja `Copiar` como unica accion del footer.
- `wwwroot` queda sincronizado con el build frontend actualizado.
- Verificacion: frontend lint OK; build OK fuera del sandbox; Playwright headless confirma cierres sin texto visible y `43x43` en mobile; busqueda sin botones textuales `Cerrar`.

### 2026-05-10 - Fix 500 en primer mensaje IA

- `/api/ia/chat` ya no devuelve HTTP 500 al construir el primer contexto financiero.
- `AtlasAiService` calcula los saldos actuales por cuenta con un agregado SQL sobre columnas escalares y proyecta a `AiExtractoRow` solo al final.
- Causa corregida: Npgsql no podia traducir el join anterior porque agrupaba sobre un record proyectado.
- Verificacion: `AtlasAiServiceTests` 20/20 OK; API dev reiniciada; `/api/health` responde `healthy`.

### 2026-05-10 - Fix visual del chat flotante IA

- El chat flotante de IA ya no queda por debajo de los selectores del dashboard principal.
- `.app-topbar` crea un plano de apilado propio (`position: relative` + `z-index: var(--z-sticky)`) para que el panel IA se pinte por encima del contenido normal.
- No cambia el contrato de IA ni la configuracion de proveedores; es una correccion visual de shell.
- Verificacion: frontend lint OK; build OK fuera del sandbox; `wwwroot` sincronizado; Playwright headless confirma que el panel IA queda por encima de los filtros (`insideChat=true`).

### 2026-05-10 - Fix guardado OpenRouter y modelo auto

- `Configuracion > Revision e IA` permite dejar OpenRouter en `Auto (OpenRouter)` y ya no bloquea el guardado de la API key por modelo vacio o desfasado.
- Backend normaliza modelos vacios o no permitidos a default seguro del proveedor; en OpenRouter cae a `openrouter/auto` para que un valor desfasado no bloquee guardar la API key.
- `AtlasAiService` conserva `openrouter/auto` como valor guardado y usa `models` con maximo 3 candidatos de la allowlist interna. Desde el ajuste posterior de modelos gratis, esa ruta no fuerza `provider.zdr=true`.
- Frontend normaliza el proveedor/modelo antes de guardar y refresca la configuracion sin conservar la key en memoria.
- Verificacion: tests focalizados IA/configuracion 25/25 OK; frontend lint OK; build frontend OK fuera del sandbox; `wwwroot` actualizado; backend dev saludable en `http://localhost:5000/api/health`.

### 2026-05-10 - Retirada del inicio de sesion ChatGPT externo

- Eliminado el flujo experimental para que ChatGPT iniciara sesion contra Atlas Balance.
- Eliminados endpoints, controlador, tests dedicados, migracion, entidad temporal, claves de configuracion, DTOs, mensajes de estado y esquema OpenAPI de ejemplo de ese flujo.
- Anadida migracion defensiva para limpiar restos de base de datos en entornos donde la migracion experimental se hubiera aplicado.
- `Configuracion > Integraciones` ya no muestra boton ni formulario para iniciar sesion con ChatGPT.
- `Configuracion > Revision e IA` queda centrado en proveedor, modelo y API key protegida en backend.
- `LoginPage` ya no conserva retornos especiales hacia rutas externas de autorizacion.
- Corregida la metadata de la migracion de OpenAI para que EF la detecte junto a las migraciones pendientes.
- Verificacion: busqueda sin restos del flujo retirado; backend Release build OK; tests focalizados IA/config/integracion 30/30 OK; frontend lint OK; frontend build OK fuera del sandbox; `wwwroot` actualizado y assets antiguos limpiados.

### 2026-05-10 - Proveedor IA OpenAI con API key de servidor

- `Configuracion > Revision e IA` permite elegir `OpenRouter` u `OpenAI`.
- OpenAI usa API key guardada en servidor (`openai_api_key`) y `AtlasAiService` llama a `https://api.openai.com/v1/chat/completions`.
- Se elimina el intento de usar la sesion web de ChatGPT como credencial de API. OpenAI queda solo por API key de servidor.
- Se mantienen los controles previos de IA: permiso por usuario, interruptor global, allowlist de modelos, limites de requests/tokens, presupuesto y auditoria sin prompts/respuestas completas.
- `wwwroot` se sincronizo con el build frontend usando copia acotada y elevada; se evita `robocopy /MIR` porque quedo colgado en esta maquina.
- Verificacion: backend Release build OK; tests `AtlasAiServiceTests|ConfiguracionControllerTests` 22/22 OK; frontend lint OK; frontend build OK fuera del sandbox.

### 2026-05-10 - Ajuste visual de identidad de cuenta

- La ficha superior `Titular / Banco / IBAN` del dashboard por cuenta pasa a un panel flexible coherente con el resto del dashboard.
- `Titular` queda como dato principal; `Banco` e `IBAN` se muestran como datos secundarios sin separadores verticales ni espacio muerto.
- `dashboard-toolbar-main` reclama ancho en desktop para evitar que la ficha se aplaste junto a las acciones.
- Verificacion: frontend lint OK, build OK fuera del sandbox y Playwright con fixture CSS real confirma desktop 2048x900 y movil 390x844 sin overflow.

### 2026-05-10 - Alineacion de Cuentas con pantallas phase2

- `Cuentas` deja de ocupar el ancho completo y pasa a compartir el contenedor centrado de `Titulares`.
- La coherencia se resuelve en `system-coherence.css` aplicando el ancho comun a `.phase2-page`, no con un parche local de una sola pantalla.
- Verificacion: frontend lint OK, build OK fuera del sandbox y Playwright desktop confirma `deltaLeft=0`, `deltaWidth=0` entre `Titulares` y `Cuentas`, sin errores de consola.

### 2026-05-10 - Ajuste visual del modal de usuarios

- El bloque `Emails de notificación` del modal de usuarios se alinea con el resto del formulario.
- El textarea pasa a ocupar el ancho útil de la ventana emergente y deja de partir emails en una caja estrecha.
- Se añade etiqueta `Destinatarios` y ayuda asociada accesible.
- Verificación: frontend lint OK, build OK fuera del sandbox, Playwright desktop/móvil OK sin overflow ni errores de consola.

### 2026-05-10 - Cierre parcial de pendientes altos de auditoria release

- Suite backend recuperada: el bloqueo MSBuild anterior queda resuelto regenerando restore tras el renombrado y desactivando build paralelo en el proyecto de tests.
- Importacion idempotente: fingerprint persistido por fila/lote, indice unico filtrado y trazabilidad de archivo/fila/fecha de importacion.
- Parser europeo ampliado con pruebas para miles, coma decimal, espacios, simbolo euro, negativos y parentesis.
- Altas/ediciones manuales frontend de importes pasan a parser europeo compartido y dejan de depender de `Number(...)`.
- Revision bancaria paginada en backend con `PaginatedResponse`, filtros antes de paginar y `Skip/Take` en EF.
- Exportaciones XLSX protegidas con limite `export_max_rows`; las exportaciones demasiado grandes se bloquean con auditoria y HTTP 413 en manual.
- Plazos fijos ya no marcan `FechaUltimaNotificacion` si falla SMTP o no hay destinatarios admin; se permite reintento sin duplicar la notificacion interna.
- IA reforzada con tests de modelo fuera de allowlist, rate limit, presupuesto mensual, contexto no confiable y auditoria sin prompt/API key.
- Accesibilidad puntual: el chat flotante IA cierra con Escape y enfoca el campo de pregunta.
- Verificacion: backend no Docker 163/163 OK; frontend lint/build OK; npm audit 0; NuGet vulnerable 0; secret scan sin claves.
- Estado de release: no apta hasta ejecutar y pasar los 2 tests PostgreSQL con Docker/Testcontainers (`RowLevelSecurityTests` y `ExtractosConcurrencyTests`).

### 2026-05-10 - Revision bancaria, IA y remate de validaciones

- Nuevas secciones `Revision` e `IA` en el menu lateral.
- `Revision` detecta comisiones y seguros en extractos visibles, con estados persistidos y filtros.
- `Configuracion` incorpora umbral minimo de comisiones, ventana antiduplicados de saldo bajo y ajustes de IA/OpenRouter.
- Boton superior de IA abre chat flotante sin cambiar de pantalla.
- `AtlasAiService` responde con contexto financiero real y falla claro si falta configurar proveedor/modelo/API key.
- Exportaciones de extractos respetan `fila_numero desc`, fecha `dd/mm/yyyy` y formato numerico con miles.
- Barras de formula de tablas tipo Excel muestran el texto completo de la celda seleccionada.
- `REVISION_EXTRACTO_ESTADOS` queda protegida con RLS.
- Auditoria final general con 6 subagentes especializados.
- Correcciones post-auditoria: IA con interruptor global, permiso por usuario, limites/coste/tokens/auditoria, modelo IA validado en backend, contexto IA minimizado y marcado como no confiable, estados de `Revision` y exportacion manual protegidos por permiso de escritura, saldo bajo sin cooldown si falla el email y ultimo saldo por `fila_numero`.
- Verificacion: backend API build OK, frontend lint/build OK, carga browser local OK en login. Tests backend completos bloqueados por fallo abierto de `AtlasBalance.Watchdog`.
- Estado de cierre: no apta para release final hasta resolver los pendientes altos de auditoria restantes y recuperar la suite backend.

### 2026-05-10 - Hardening especifico del modulo IA

- La IA queda gobernada por interruptor global `ai_enabled` y permiso persistente por usuario `puede_usar_ia`.
- `POST /api/ia/chat` valida autenticacion, usuario autorizado, IA global activa, modelo permitido, API key en servidor, limites de requests, presupuesto estimado y tokens antes de llamar al proveedor.
- Se elimina la cuota fija en memoria y se pasa a limites configurables por minuto/hora/dia, limite global diario, presupuesto mensual/total, costes estimados por modelo y caps de contexto/respuesta.
- El coste acumulado de IA se guarda en contadores persistidos (`ai_usage_*`) independientes de la limpieza de auditoria de 28 dias; el presupuesto total ya no se recalcula desde logs caducables.
- Auditoria IA registra metadatos de uso, bloqueos, errores y avisos de presupuesto sin guardar prompts ni respuestas completas.
- Las llamadas OpenRouter no gratuitas pueden exigir `provider.zdr=true`; los modelos gratis permitidos no lo fuerzan porque OpenRouter no los publica como endpoints ZDR.
- El frontend oculta menu/boton IA si falta permiso o la IA esta desactivada, y la ruta directa muestra bloqueo claro sin formulario usable.
- `Usuarios` permite activar/desactivar IA por usuario sin reiniciar la app; los cambios rotan sesiones como el resto de permisos sensibles.
- Migracion `20260510123000_HardenAiGovernance` agrega `USUARIOS.puede_usar_ia`, indice y claves de configuracion IA.
- Verificacion: API build OK, frontend lint OK, frontend build OK. Tests backend siguen bloqueados por fallo MSBuild sin errores detallados.

### 2026-05-10 - Cierre de riesgos pendientes IA

- `AtlasAiService` ya no carga todos los extractos accesibles para construir contexto: aplica rango maximo defensivo, scope del usuario, agregados SQL y limite de movimientos relevantes antes de enviar datos al proveedor.
- Se agrega presupuesto mensual por usuario con tabla `IA_USO_USUARIOS`, migracion `20260510124158_AddIaUserUsageTableAndBudget`, contador de requests/tokens/coste por usuario y bloqueo `user_monthly_budget_exceeded`.
- `Configuracion > Revision e IA` permite configurar `Presupuesto mensual usuario EUR` y muestra consumo mensual del usuario.
- El parser de respuesta del proveedor valida `choices/message/content` y trata JSON invalido o payload inesperado como `IA_CONSULTA_ERROR` sin exponer prompts ni claves.
- El consumo OpenAI sigue limitado a API key de servidor; no se usa la sesion de ChatGPT como credencial.
- Verificacion: `AtlasAiServiceTests` 18/18 OK; backend sin tests Docker 173/173 OK; frontend lint/build OK; npm audit 0; NuGet vulnerable 0.
- Estado de release: NO apta. La suite completa falla 2/175 por Testcontainers porque Docker Desktop no esta arrancado o no expone `dockerDesktopLinuxEngine`.

### 2026-05-10 - Renombrado tecnico a AtlasBalance

- Solucion, proyectos, carpetas, namespaces, tests, scripts, CI y referencias de build/deploy pasan a `AtlasBalance.*`.
- Los textos visibles mantienen `Atlas Balance`.
- `Actualizar-AtlasBalance.ps1` reconfigura el `binPath` de los servicios tras copiar ejecutables nuevos para que una instalacion existente no quede apuntando a binarios obsoletos.
- Se mantiene sin cambios el nombre de BD `atlas_balance`, rutas HTTP `/api/*` y `/watchdog/*`, y claves de Data Protection (`SetApplicationName("AtlasBalance")`) para no romper compatibilidad.
- Verificacion: API y Watchdog build OK, frontend lint/build OK, busqueda final sin referencias activas al nombre anterior. Tests/backend solution siguen bloqueados por el bug MSBuild ya registrado.

### 2026-05-02 - Reticula tipo Excel en Extractos

- La tabla de `Extractos` elimina la cuadricula falsa de fondo y usa bordes reales por celda.
- `--extracto-sheet-width` nace en el viewport para que cabecera, cuerpo, espaciador y filas compartan el mismo ancho.
- Las filas virtualizadas quedan con altura fija exacta y las celdas ocupan toda la altura de fila.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK, Playwright headless OK con 13 columnas alineadas y `wwwroot` sincronizado.

### 2026-05-02 - Insercion intermedia en desglose de cuenta

- El dashboard de cuenta permite `Insertar debajo` en cada linea del desglose si el usuario puede agregar lineas.
- `POST /api/extractos` acepta `insert_before_fila_numero` y desplaza las filas posteriores dentro de una transaccion.
- El orden queda persistido mediante `fila_numero`, no solo reordenado en frontend.
- El desglose de cuenta carga por `fila_numero desc` para respetar la secuencia guardada.
- Verificacion: `ExtractosControllerTests` 11/11 OK, `npm.cmd run lint` OK, `npm.cmd run build` OK y `wwwroot` sincronizado.

### 2026-05-02 - Filtro de periodo en Extractos

- `Extractos` permite seleccionar un rango libre con fechas `Desde` y `Hasta`.
- El rango se conserva en la URL con `fechaDesde` y `fechaHasta`.
- La pantalla envia esos parametros a `GET /api/extractos`, reutilizando el filtrado backend existente.
- Se bloquea el rango invertido (`Desde` posterior a `Hasta`) con mensaje claro.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK y `wwwroot` sincronizado.

### 2026-05-02 - Fix recorte superior en graficas de evolucion

- `EvolucionChart` calcula un dominio Y explicito con padding superior/inferior segun rango y magnitud de datos.
- Se evita que la serie de saldo quede pegada al borde superior cuando el saldo se aproxima al tick maximo.
- El fix aplica a todas las vistas que reutilizan `EvolucionChart`.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK y `wwwroot` sincronizado.

### 2026-05-02 - Reticula fija en tabla de extractos

- Corregida la desalineacion visual de la vista `Extractos` con muchas columnas visibles.
- Cabecera, cuerpo virtualizado y filas comparten el mismo ancho total de hoja.
- Las columnas pasan a anchos fijos por tipo para mantener lectura tipo Excel.
- Se elimina el offset vertical negativo de las filas virtualizadas.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK, Playwright headless OK y `wwwroot` sincronizado.

### 2026-05-02 - Fix overflow KPIs dashboard principal

- Corregido el corte de importes largos en `Ingresos periodo` y `Egresos periodo`.
- Reequilibrado el dashboard principal para que `Saldos por divisa` ocupe menos ancho y los KPIs principales tengan mas presencia.
- Los importes de KPIs escalan segun el ancho real de su tarjeta mediante container queries CSS.
- Se mantiene una sola linea para cifras financieras, sin truncado.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK, Playwright headless OK y `wwwroot` sincronizado.

### 2026-05-02 - Apertura de version V-01.06

- `version_actual.md` pasa a declarar `V-01.06` como version activa.
- Fuentes runtime actualizadas a `V-01.06` / `1.6.0`.
- `V-01.05` queda documentada como base anterior.
- Rama de trabajo prevista: `V-01.06`.
- Verificacion: `git status --short --branch` confirma rama activa `V-01.06`; fuentes runtime y documentacion apuntan a `V-01.06` / `1.6.0`.
