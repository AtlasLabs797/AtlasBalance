# V-01.05

## Estado

Version actual activa.

## Base

- Version anterior publicada: `V-01.05`.
- Rama base local: `V-01.05`.
- Rama de trabajo actual: `V-01.05`.

## Alcance

- Rama de trabajo para nuevas funciones y correccion de errores posteriores a `V-01.05`.
- Aplicacion Atlas Balance empaquetable para Windows x64.
- Backend ASP.NET Core 8, frontend React/Vite y Watchdog.
- Paquetes de release en `Atlas Balance/Atlas Balance Release`.
- Documentacion centralizada en `Documentacion`.

## Prioridad

- Corregir bugs antes de sumar funciones vistosas.
- Mantener cada cambio trazado en tickets, bugs o documentacion tecnica.
- No publicar release sin build limpio y verificaciones relevantes.
- No meter artefactos de release en commits normales salvo que el objetivo sea publicar paquete.

## Instrucciones para cambios

- Asociar cualquier cambio nuevo a `V-01.05`.
- Actualizar `Documentacion/DOCUMENTACION_CAMBIOS.md` en cada sesion.
- Actualizar `Documentacion/DOCUMENTACION_TECNICA.md` si cambia codigo, estructura, scripts o configuracion.
- Actualizar `Documentacion/DOCUMENTACION_USUARIO.md` si cambia instalacion, uso, configuracion o ubicacion de paquetes.
- Consultar `Documentacion/LOG_ERRORES_INCIDENCIAS.md` antes de resolver errores.
- Mantener `Documentacion/REGISTRO_BUGS.md` al dia.
- No documentar contrasenas, tokens ni datos privados.

## Cambios registrados

### 2026-05-02 - Correccion de CI GitHub por lockfile npm

- GitHub Actions fallaba en `npm ci` porque `package-lock.json` apuntaba a `once@1.5.0`, version inexistente en npm.
- `package.json` fija `overrides.once = 1.4.0` y `package-lock.json` resuelve `once` al tarball publicado `once-1.4.0.tgz`.
- No cambia codigo runtime; es una correccion de reproducibilidad de dependencias frontend.
- Verificacion: `npm.cmd ci` OK, `npm.cmd audit --audit-level=moderate` 0 vulnerabilidades, `npm.cmd run lint` OK y `npm.cmd run build` OK.
- Pendiente: confirmar CI remoto tras push.

### 2026-05-02 - Paquete release Windows x64 regenerado

- Generado `AtlasBalance-V-01.05-win-x64.zip` en `Atlas Balance/Atlas Balance Release`.
- El empaquetado recompila frontend, sincroniza `wwwroot`, publica API y Watchdog self-contained para `win-x64` y crea carpeta + ZIP de release.
- SHA256 del ZIP: `3E7A3ED22EFC4D18A161EA9D8D15CD9C12B3D51BDEF9AE38863767EC5CEAE299`.
- Tamano ZIP: `102350978` bytes.
- Verificacion: `Build-Release.ps1 -Version V-01.05` OK.
- Pendiente: falta `.zip.sig` porque no estaba definido `ATLAS_RELEASE_SIGNING_PRIVATE_KEY_PEM`; no publicar como latest final sin firma.

### 2026-05-02 - Cierre de hallazgos residuales del escaneo repo-wide

- Cerrados ocho hallazgos de seguridad/bug residuales: credenciales one-shot de instalacion/reset con ACL fail-open, bypass de columnas `flagged`/`flagged_nota`, scope global `dashboard-only`, auditoria OpenClaw sobre extractos eliminados, `returnTo` externo en importacion, politica RLS de exportaciones con permiso de lectura y tag Docker mutable de PostgreSQL.
- Las credenciales iniciales pasan a `C:\AtlasBalance\config\INSTALL_CREDENTIALS_ONCE.txt`; el directorio `config` se restringe antes de escribir secretos y los scripts abortan si no pueden aplicar ACL.
- `Reset-AdminPassword.ps1` exige ejecucion como Administrador antes de tocar credenciales o escribir el archivo temporal.
- `backend/src/GestionCaja.API/wwwroot` queda sincronizado con el build frontend actualizado.
- Verificacion: tests focalizados `ExtractosControllerTests|DashboardServiceTests|IntegrationOpenClawControllerTests|RowLevelSecurityTests` 20/20 OK, `npm.cmd run lint` OK, `npm.cmd run build` OK, parser PowerShell OK y `robocopy` OK.

### 2026-05-02 - Regresion MFA cada 90 dias

- Agregada cobertura para confirmar que una cookie `mfa_trusted` expirada vuelve a pedir Google Authenticator.
- Confirmado que el recuerdo MFA dura 90 dias y no se renueva silenciosamente en cada login.
- Verificacion: `AuthServiceTests` Release 13/13 OK.

### 2026-05-02 - Escaneo de seguridad completo y hardening

- Escaneo repo-wide con `codex-security` y subagentes sobre auth/MFA, autorizacion, integraciones, importacion, frontend, CI, dependencias, Watchdog y update path.
- Corregidos lockout password/MFA, redaccion de auditoria de integraciones, limites de columnas extra, permisos dashboard-only, restore de extractos, visibilidad de cuenta referencia de plazo fijo y firma de paquetes online.
- El actualizador online ahora requiere `.zip.sig` RSA/SHA-256 y clave publica configurada por `UpdateSecurity:ReleaseSigningPublicKeyPem` o `ATLAS_RELEASE_SIGNING_PUBLIC_KEY_PEM`.
- Verificacion: tests focalizados 72/72 OK, NuGet sin vulnerabilidades, npm audit 0 vulnerabilidades, parser PowerShell OK. Suite backend completa 127/128 por permisos locales de `__EFMigrationsHistory` en el harness RLS.

### 2026-05-02 - Alineacion dinamica de todas las graficas de evolucion

- `EvolucionChart` calcula el ancho del eje Y segun las etiquetas compactas de cada serie.
- El eje queda limitado entre `44px` y `72px`, reduciendo el hueco izquierdo en importes pequenos.
- El ajuste aplica a dashboard principal, dashboard por titular, `Titulares` y `Cuentas`.
- `wwwroot` queda sincronizado con el build frontend actualizado.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK, `robocopy` OK y Playwright headless confirma `gridStartX=45px` en las cuatro rutas con evolucion.

### 2026-05-02 - Saldo total del dashboard sin salto de linea

- El numero destacado de `Saldo total` baja de escala en el resumen superior del dashboard.
- La grilla de KPIs superiores da mas ancho relativo al KPI principal.
- Los importes KPI evitan salto de linea para que `1.000.000,00 €` entre completo.
- `wwwroot` queda sincronizado con el build frontend actualizado.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK, Playwright headless confirma `wraps=false` y `overflows=false` con `total_convertido=1000000`, y `robocopy` OK.

### 2026-05-02 - Listado de cuentas en tres columnas

- `CuentasPage` agrega clase de pagina para acotar estilos del listado inferior.
- El listado de tarjetas de cuentas pasa a tres columnas en desktop, dos en tablet y una en mobile.
- Las tarjetas ajustan titulo, badges, metadatos, saldo, notas y acciones para que el ancho menor no genere overflow ni tarjetas torpes.
- `wwwroot` queda sincronizado con el build frontend actualizado.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK, Playwright headless con APIs mockeadas confirma desktop `3`, tablet `2`, mobile `1` y sin overflow horizontal, y `robocopy` OK.

### 2026-05-02 - Divisa base primero en saldos por divisa

- `Saldos por divisa` muestra siempre primero la divisa base del dashboard.
- Las divisas secundarias conservan el orden recibido de la API.
- No cambian endpoints, permisos ni calculos; es un ajuste de jerarquia visual en frontend.
- `wwwroot` queda sincronizado con el build frontend actualizado.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK, Playwright headless confirma que `EUR` aparece primero aunque la API devuelva `USD` antes, y `robocopy` OK.

### 2026-05-02 - Reorden de plazos fijos y saldos por titular en dashboard principal

- `Plazos fijos` pasa al resumen superior del dashboard, debajo de `Saldo total`, `Ingresos periodo` y `Egresos periodo`.
- `Saldos por titular` ocupa toda la parte inferior de la pantalla.
- Los titulares se muestran en tres columnas fijas en desktop: Empresa, Autonomo y Particular.
- No cambian endpoints, permisos ni calculos; es un ajuste de jerarquia visual y CSS.
- `wwwroot` queda sincronizado con el build frontend actualizado.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK, Playwright headless con APIs mockeadas confirma plazos bajo KPIs, titulares a ancho completo, tres columnas y sin overflow horizontal, y `robocopy` OK.

### 2026-05-02 - Listado de titulares en tres columnas

- `TitularesPage` agrega clase de pagina para acotar estilos del listado inferior.
- El listado de tarjetas de titulares pasa a tres columnas en desktop, dos en tablet y una en mobile.
- Las tarjetas ajustan titulo, notas, saldo y acciones para evitar tarjetas torpes o overflow al reducir el ancho.
- `wwwroot` queda sincronizado con el build frontend actualizado.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK, Playwright headless con APIs mockeadas confirma desktop `3`, tablet `2`, mobile `1` y sin overflow horizontal, y `robocopy` OK.

### 2026-05-02 - Formato de importacion en cuentas de efectivo

- Las cuentas `EFECTIVO` pueden seleccionar y conservar `formato_id`.
- La UI muestra el selector de formato para `NORMAL` y `EFECTIVO`; solo `PLAZO_FIJO` lo oculta.
- El backend valida el formato para cuentas normales y de efectivo, manteniendo datos bancarios nulos en efectivo.
- `ImportacionPage` aclara que efectivo usa formato de importacion igual que una cuenta normal.
- `wwwroot` queda sincronizado con el build frontend actualizado.
- Verificacion: `CuentasControllerTests` 5/5 OK, `npm.cmd run lint` OK, `npm.cmd run build` OK y `robocopy` OK.

### 2026-05-02 - Alineacion de graficas en dashboards de cuentas y titulares

- Las graficas de barras en `Cuentas` y `Titulares` reducen el carril del eje Y de `120px` a `72px`.
- Los ticks del eje Y usan formato compacto para no desplazar el area de trazado.
- Se mantienen importes completos en tooltip y no se cambian contratos de API.
- `wwwroot` queda sincronizado con el build frontend actualizado.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK, `robocopy` OK y Playwright headless confirma `gridStartX=72px` en `/titulares` y `/cuentas`.

### 2026-05-01 - Rediseño del dashboard principal con grafica a ancho completo

- El dashboard principal deja de encajar `Evolucion` en una grilla lateral estrecha.
- KPIs y saldos por divisa quedan como resumen superior compacto.
- La grafica de evolucion pasa a una tarjeta propia de ancho completo y `EvolucionChart` acepta altura configurable.
- `wwwroot` queda sincronizado con el build frontend actualizado.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK, Playwright headless con APIs mockeadas (`chartWidthRatio=0.960`, `svgHeight=420`, sin overflow horizontal) y `robocopy` OK.

### 2026-05-01 - Alineacion de grafica Evolucion en dashboard principal

- `EvolucionChart` reduce el ancho reservado por el eje Y y declara margenes explicitos en `LineChart`.
- La grafica queda alineada visualmente dentro de la tarjeta sin tocar contratos de API ni layout global del dashboard.
- `wwwroot` queda sincronizado con el build frontend actualizado.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK, `robocopy` OK y Playwright headless con APIs mockeadas confirma `plotInsetFromLegend=72px`.

### 2026-05-01 - MFA recordado 90 dias y QR de enrolamiento

- El MFA TOTP queda recordado por navegador durante 90 dias mediante cookie `mfa_trusted` firmada, `HttpOnly` y ligada a usuario + `security_stamp`.
- Si la cookie es valida, el login no vuelve a pedir Google Authenticator hasta que expire o cambie el estado de seguridad del usuario.
- El primer enrolamiento muestra QR escaneable generado desde `mfa_otp_auth_uri`, con clave manual como fallback.
- Agregada dependencia frontend `qrcode` para generar el QR localmente.
- `wwwroot` queda sincronizado con el build frontend actualizado.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK, `AuthServiceTests` Release 11/11 OK y `robocopy` OK.

### 2026-05-01 - Alineacion del logo en login

- El bloque de marca superior del login queda alineado con la misma columna visual del formulario.
- `.auth-logo-container` deja de usar el ancho general de `1120px`, pasa a ancho de login responsive y centra el bloque de marca completo.
- `wwwroot` queda sincronizado con el build frontend actualizado.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK, `robocopy` OK y Edge headless confirma `brandDeltaCard=0px`.

### 2026-05-01 - Aplicacion UI/UX en shell y dashboard

- Aplicadas las reglas de `Documentacion/Diseno/DESIGN.md` al shell principal y dashboard.
- La navegacion queda agrupada por intencion: Operacion, Control y Sistema.
- El menu movil queda limitado a Inicio, Titulares, Cuentas, Importar y Mas, con el resto agrupado en el sheet `Mas`.
- La navegacion pasa a iconos `lucide-react` con peso consistente.
- El dashboard principal reordena su primera lectura: KPIs, saldos por divisa y evolucion.
- `Saldos por divisa` muestra total, disponible e inmovilizado con estructura semantica.
- Se elimina la carga CSS de Geist y se corrige el token tipografico de MFA.
- `wwwroot` queda sincronizado con el build frontend actualizado.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK, Playwright desktop/mobile con APIs mockeadas sin overflow horizontal y `robocopy` OK.
- Pendiente: aplicar la guia pantalla por pantalla en el resto de vistas.

### 2026-05-01 - Guia UI/UX Atlas Balance

- Reescrito `Documentacion/Diseno/DESIGN.md` como sistema de diseno operativo adaptado a Atlas Balance.
- Se mantienen los tokens de color actuales (`--bg-app`, `--accent-primary`, estados, charts y dark mode) y se documenta como usarlos sin convertir la app en un dashboard SaaS generico.
- Se agregan reglas concretas para arquitectura de menus, sidebar, topbar, bottom nav, iconografia, tablas financieras, charts, formularios, responsive, motion, accesibilidad y anti-patrones.
- Cambio documental; no requiere build ni tests.
- Pendiente: aplicar la guia en codigo pantalla por pantalla.

### 2026-05-01 - Checklist general de seguridad aplicado

- Revisado `C:\Proyectos\CHECKLIST DE SEGURIDAD PARA APP.md` contra Atlas Balance.
- Implementado MFA TOTP obligatorio para usuarios web antes de emitir JWT/cookies.
- Agregada migracion `RequireWebUserMfa` con campos MFA en `USUARIOS`.
- Los cambios de permisos, email o perfil de usuario rotan `security_stamp` y revocan refresh tokens.
- Las actualizaciones descargadas desde GitHub Release verifican digest SHA-256 del asset antes de extraer.
- CI agrega escaneo de secretos de alta confianza.
- Creados `Documentacion/SEGURIDAD_CHECKLIST_APP_V-01.05_2026-05-01.md` y `Documentacion/SEGURIDAD_RESPUESTA_INCIDENTES.md`.
- Corregido test fragil de dashboard que usaba fechas futuras al ejecutarse al inicio de mes.
- Verificacion: backend Release build OK, tests focalizados seguridad/auth/update 24/24 OK, backend sin Testcontainers 115/115 OK, frontend lint/build OK, npm audit 0 vulnerabilidades, NuGet vulnerable sin hallazgos y secret scan local sin hallazgos.
- Pendiente operativo: firma de binarios/instaladores, firma detached de releases, cifrado real de backups en disco y pentest.

### 2026-05-01 - Row Level Security activo en PostgreSQL

- Agregada migracion `EnableRowLevelSecurity` con RLS forzado y politicas sobre tablas sensibles de titulares, cuentas, extractos, plazos fijos, exportaciones, auditoria, backups y notificaciones.
- Agregada migracion `SignRowLevelSecurityContext` para exigir firma HMAC del contexto RLS y bloquear `SET atlas.*` falsificado.
- Agregado interceptor EF Core que fija contexto PostgreSQL `atlas.*` por comando para usuario, integracion, admin, sistema y dashboard, incluyendo `atlas.context_signature`.
- Ajustado middleware de integraciones para que las escrituras de auditoria/rate limit tengan contexto de token validado.
- Docker e instalador separan rol owner/migracion y rol runtime; el runtime queda sin superusuario, sin ownership de tablas y sin `BYPASSRLS` en instalaciones nuevas.
- Agregado `RowLevelSecurityTests` con PostgreSQL real para validar catalogo, rol runtime, runtime no owner, rechazo de contexto sin firma y aislamiento anonimo/usuario/integracion/admin.
- Base local `atlas_balance_db` migrada: 11 tablas objetivo con RLS/FORCE RLS, 20 politicas, dos migraciones RLS aplicadas, secreto RLS sembrado, contexto invalido rechazado, contexto firmado aceptado y `app_user` sin `rolsuper` ni `rolbypassrls`.
- Verificacion: `dotnet build GestionCaja.API -c Release --no-restore` OK, `RowLevelSecurityTests` OK, tests focalizados RLS/permisos/integraciones 15/15 OK, `dotnet ef database update` OK y `git diff --check` sin errores.

### 2026-04-30 - Referencias UI locales en Skills\Diseno

- Clonados `tailwindlabs/headlessui` y `radix-ui/themes` en `Skills\Diseno\headlessui` y `Skills\Diseno\radix-ui-themes`.
- Cambio de soporte/documentacion local; no modifica codigo de Atlas Balance ni su stack canonico.
- Verificacion: `git clone` completado para ambos repositorios.

### 2026-04-26 - Vista de extractos tipo hoja de calculo

- `ExtractoTable` pasa a tener cabecera y filas dentro de un viewport comun para mantener alineado el scroll horizontal.
- `extractos.css` refuerza formato de hoja: rejilla de celda, cabecera sticky, primera columna sticky, foco de celda, numeros tabulares y modo compacto mas denso.
- Las columnas visibles muestran etiquetas legibles sin cambiar los nombres reales del contrato API.
- `wwwroot` queda sincronizado con el build frontend actualizado.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK y `robocopy dist ..\\backend\\src\\GestionCaja.API\\wwwroot /MIR` OK.
- Prueba visual/funcional Playwright con app real y APIs mockeadas en `/extractos`: OK; 120 filas, scroll horizontal/vertical, cabecera y primera columna sticky, foco de celda, filtros, panel de columnas y consola sin errores.

### 2026-04-26 - Reorden en dashboard de titulares (evolucion antes del listado)

- En `CuentasPage`, la tarjeta `Evolucion` pasa a mostrarse antes del listado de cuentas/titulares dentro del bloque de dashboard.
- Cambio exclusivamente visual de orden; no hay cambios en logica de negocio, permisos ni endpoints.
- Verificacion: `npm.cmd run lint` OK y `npm.cmd run build` OK.

### 2026-04-26 - Reorden de dashboard principal (grafica antes de saldos)

- `DashboardPage` muestra ahora la tarjeta `EvoluciÃƒÂ³n` antes del bloque con `Saldo por divisa` y `Saldos por titular`.
- Cambio exclusivamente visual en el orden de render; sin cambios de contratos ni logica de datos.
- `wwwroot` sincronizado con el build frontend actualizado.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK y `robocopy dist ..\\backend\\src\\GestionCaja.API\\wwwroot /MIR` OK (codigo `1` esperado).

### 2026-04-26 - Importacion sin reordenar lineas por fecha

- `ImportacionService.ConfirmarAsync` ya no ordena las filas por fecha antes de guardarlas.
- La asignacion de `fila_numero` recorre las filas desde abajo hacia arriba, dejando la linea superior del pegado como la ultima/mas alta del lote.
- Las lineas informativas quedan junto a su movimiento cuando comparten fecha o fecha heredada; el backend no intenta reinterpretar el orden del banco.
- Verificacion: `ImportacionServiceTests` 26/26 OK y `dotnet build GestionCaja.API -c Release --no-restore` OK.

### 2026-04-26 - Borrado multiple de extractos en dashboard de cuenta

- `CuentaDetailPage` permite seleccionar varias lineas del desglose con checkbox por fila y seleccion global.
- Se agrega accion `Eliminar seleccionadas` con confirmacion unica y vista previa de filas.
- El borrado multiple reutiliza `DELETE /api/extractos/{id}`, manteniendo permisos y auditoria por linea.
- `wwwroot` sincronizado con el build frontend actualizado.
- Verificacion: `npm.cmd run build` OK, `npm.cmd run lint` OK y `robocopy dist ..\\backend\\src\\GestionCaja.API\\wwwroot /MIR` OK (codigo `1` esperado).

### 2026-04-26 - Importacion con avisos para filas con saldo y sin fecha/monto

- Las filas con concepto, saldo, fecha vacia e importe vacio ya no bloquean la importacion.
- Se importan como avisos seleccionables: monto `0`, fecha heredada de la ultima fila valida anterior y saldo informado conservado.
- Se mantiene el error para saldos no numericos, importes ambiguos o filas sin fecha cuando no existe fecha anterior usable.
- Verificacion: `ImportacionServiceTests` 26/26 OK y `dotnet build GestionCaja.API -c Release` OK.

### 2026-04-26 - Actualizacion automatica desde GitHub Release oficial

- `Configuracion > Sistema` usa el repo oficial `https://github.com/AtlasLabs797/AtlasBalance` como origen de actualizaciones.
- `ActualizacionService` consulta el ultimo GitHub Release, localiza el asset `AtlasBalance-*-win-x64.zip`, lo descarga y lo extrae bajo `UpdateSourceRoot`.
- El asset descargado debe pertenecer al repo oficial y contener `VERSION`, `api/GestionCaja.API.exe` y `watchdog/GestionCaja.Watchdog.exe`.
- La comparacion de versiones soporta tags de release tipo `V-01.05-win-x64`.
- Watchdog crea backup PostgreSQL previo antes de aplicar actualizacion automatica y aborta si no puede hacerlo.
- Watchdog crea copia rollback de binarios y la restaura si falla la sincronizacion o el health check posterior.
- `wwwroot` sincronizado con el build frontend actualizado.
- Verificacion: tests focalizados `ActualizacionServiceTests|WatchdogOperationsServiceTests|ConfiguracionControllerTests` 14/14 OK, parser PowerShell de instalador OK, `npm.cmd run lint` OK, `npm.cmd run build` OK y `robocopy` OK.

### 2026-04-26 - Fix de banner de alerta sobredimensionado en shell

- Corregido `frontend/src/styles/layout/shell.css` para que `app-main` use tres filas (`topbar`, `alert-banner`, `app-content`) en lugar de dejar el banner ocupando la fila flexible `1fr`.
- Asignadas filas explicitas para `topbar`, `alert-banner` y `app-content`, con `min-height: 0` en contenido para mantener scroll correcto.
- Ajustado `.alert-banner` con `align-self: start` para evitar estirado vertical residual detectado en dashboards.
- Ajustado `.app-main > .alert-banner` con guard rails de grid (`align-self: start`, `min-height: 0`, `height: auto`).
- Barrido de frontend: `AlertBanner` solo se renderiza en `Layout`, sin variantes por pagina.
- Ajuste replicado en mobile (`max-width: 768px`) para evitar regresion de altura en dispositivos pequenos.
- `wwwroot` sincronizado con el nuevo build frontend.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK y `robocopy dist ..\\backend\\src\\GestionCaja.API\\wwwroot /MIR` OK (codigo `1` esperado).

### 2026-04-26 - Actualizacion post-instalacion endurecida

- `scripts\update.ps1` deja de reenviar `-InstallPath` como argumento residual y declara explicitamente `InstallPath`/`SkipBackup`.
- El wrapper `update.cmd -InstallPath C:\AtlasBalance` queda alineado con el contrato real del actualizador.
- `SeedData` comprueba primero los IDs fijos de formatos de importacion antes de insertar defaults por banco/divisa, evitando el fallo `23505 pk_formatos_importacion` en bases actualizadas desde versiones anteriores.
- Agregado test de regresion para formato legacy con ID fijo ya existente.
- Documentados incidente, causa y mitigacion en log de errores, registro de bugs, documentacion tecnica y bitacora.
- Regenerado paquete `AtlasBalance-V-01.05-win-x64.zip` con la correccion incluida.
- Verificacion: parser PowerShell OK para scripts de actualizacion, `SeedDataTests` 5/5 OK y `Build-Release.ps1 -Version V-01.05` OK.
- SHA256 del ZIP corregido: `482189BB4B6F731CEB02ECA214A550B1CE9DB33C71F0DBF4E057761E8FD002C3`.

### 2026-04-26 - Limpieza de artefactos locales

- Eliminados artefactos generados fuera del codigo fuente: `.codex-runlogs/`, `output/`, logs de API y paquetes locales antiguos en `Atlas Balance/Atlas Balance Release/`.
- Conservado `Atlas Balance/Atlas Balance Release/.gitkeep`; los paquetes release siguen fuera de Git y se regeneran/publican como assets.
- Eliminados directorios frontend vacios `src/lib/` y `src/components/ui/`.
- `.gitignore` ahora cubre `.codex-runlogs/` y `output/`.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK y backend filtrado sin Testcontainers 107/107 OK. La suite completa queda en 107/108 porque Docker no esta disponible para `ExtractosConcurrencyTests`.

### 2026-04-25 - Publicacion release

- Regenerado paquete final `AtlasBalance-V-01.05-win-x64.zip`.
- SHA256 final: `B5ABC5525CBD49F2BD0A5ADC5B930A2113AF323F99C1337087B8E0D7875E6A10`.
- Tamano ZIP: `102360418` bytes.
- Verificacion: `scripts\Build-Release.ps1 -Version V-01.05` OK, `npm.cmd run lint` OK, `npm.cmd audit --audit-level=moderate` 0 vulnerabilidades, backend tests Release 108/108 OK y NuGet vulnerable sin hallazgos.
- El paquete se publica como asset de GitHub Release; no se versiona dentro del repositorio.

### 2026-04-25 - Correccion de hallazgos de auditoria

- Eliminados Tailwind/shadcn del frontend: dependencias, plugin Vite, imports CSS, `components.json`, boton shadcn y utilidades asociadas.
- El frontend vuelve a quedar sobre React/Vite, CSS variables propias y `lucide-react`, sin doble sistema visual.
- `CuentasController.Resumen` queda alineado con el resumen rico: incluye titular, nombre de cuenta, divisa, tipo de cuenta, notas, ultima actualizacion y bloque `plazo_fijo`.
- Agregada cobertura de test para que el resumen de cuenta exponga metadatos de plazo fijo.
- `DatePickerField` mejora etiquetas accesibles y navegacion de calendario con flechas/Home/End.
- `ConfirmDialog` atrapa Tab dentro del modal y conserva cierre con Escape/restauracion de foco.
- `AppSelect` abre/cierra con Enter y Espacio.
- Reducidos fondos decorativos con radial/linear gradients en superficies de UI; se conservan solo degradados funcionales.
- Sincronizado `backend/src/GestionCaja.API/wwwroot` con el build frontend corregido.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK, `npm.cmd audit --audit-level=moderate` 0 vulnerabilidades, NuGet vulnerable sin hallazgos y backend tests 108/108 OK.

### 2026-04-25 - Auditoria de uso, bugs y seguridad

- Creada `Documentacion/AUDITORIA_USO_BUGS_SEGURIDAD_V-01.05_2026-04-25.md`.
- Verificadas dependencias frontend/NuGet, lint, build frontend, tests backend completos, parser PowerShell, headers/auth/permisos/rutas/CI y superficie de secretos.
- Resultado: sin P0 ni vulnerabilidades criticas/altas detectadas; `npm audit` 0 vulnerabilidades, NuGet vulnerable sin hallazgos y backend tests 107/107 OK.
- Hallazgos detectados: Tailwind/shadcn reintroducidos contra el stack canonico, contrato duplicado de resumen de cuenta sin plazo fijo y gaps de accesibilidad en controles propios.
- Estado posterior: corregidos en la misma version antes de cierre.
- Actualizado `Documentacion/REGISTRO_BUGS.md` con los bugs/deuda abiertos.

### 2026-04-25 - Vaciado operativo de datos de tesoreria local

- Se genero un backup SQL local previo al vaciado de la base de datos de desarrollo.
- Se vaciaron `TITULARES`, `CUENTAS` y `EXTRACTOS` con `TRUNCATE ... CASCADE`, limpiando tambien datos dependientes como `PLAZOS_FIJOS`, columnas extra, permisos/preferencias asociados a cuentas/titulares, alertas, exportaciones y permisos de integracion relacionados.
- Se conservaron usuarios, configuracion y migraciones.
- Verificacion: conteos posteriores a cero en las tablas objetivo y dependientes; `USUARIOS` y `CONFIGURACION` conservan registros.

### 2026-04-25 - Auditoria general de bugs y seguridad

- Revisadas las incidencias abiertas/cerradas en `REGISTRO_BUGS.md`, `LOG_ERRORES_INCIDENCIAS.md`, auditorias de seguridad V-01.02/V-01.03 e incidencias Windows Server 2019.
- Confirmadas verificaciones de auth, CSRF, cookies, cabeceras, permisos, OpenClaw, rutas de exportacion/backup, secretos versionables, CI/CD y dependencias.
- Actualizado el manifiesto frontend para no declarar minimos antiguos: `axios ^1.15.2` y `react-router-dom ^6.30.3`.
- Creada `Documentacion/SEGURIDAD_AUDITORIA_V-01.05.md`.
- Verificacion: `npm.cmd audit --audit-level=moderate` OK, `npm.cmd run lint` OK, `npm.cmd run build` OK, backend tests 107/107 OK, NuGet vulnerable sin hallazgos y `wwwroot` sincronizado.

### 2026-04-25 - Selector de fecha propio alineado con Atlas

- Se sustituye el popup nativo de `input type="date"` por `DatePickerField`, un calendario React propio con estilos del sistema visual.
- El selector se aplica a fechas de plazo fijo, importacion de movimientos de plazo fijo, alta manual de extractos y filtros de auditoria.
- El popover muestra mes, navegacion, dias, estado seleccionado/hoy, acciones `Hoy` y `Limpiar`, y abre hacia arriba si no cabe debajo del campo.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK, `wwwroot` sincronizado y comprobacion visual en navegador de `/cuentas` sin errores de consola.

### 2026-04-25 - Vencimiento visible en detalle de cuenta de plazo fijo

- `GET /api/extractos/cuentas/{id}/resumen` devuelve ahora `tipo_cuenta` y el bloque `plazo_fijo` cuando la cuenta es de tipo `PLAZO_FIJO`.
- El dashboard de cuenta muestra, bajo el nombre de la cuenta, la fecha de vencimiento, dias restantes/vencido y estado del plazo fijo.
- Se sincroniza el bundle frontend servido desde `backend/src/GestionCaja.API/wwwroot`.
- Verificacion: backend Release build OK, `npm.cmd run lint` OK, `npm.cmd run build` OK y `wwwroot` sincronizado.

### 2026-04-25 - Importacion simple de plazo fijo y resumen dashboard

- Las cuentas `PLAZO_FIJO` dejan de usar formatos de importacion bancarios.
- Importacion muestra para plazo fijo solo entradas/salidas de dinero con fecha, monto y concepto.
- Backend bloquea importaciones con mapeo para plazo fijo y agrega `POST /api/importacion/plazo-fijo/movimiento`.
- El dashboard principal muestra monto total en plazos fijos, intereses previstos aproximados y dias hasta el proximo vencimiento.
- Verificacion: `ImportacionServiceTests|DashboardServiceTests` 28/28 OK, backend Release build OK, `npm.cmd run lint` OK, `npm.cmd run build` OK y `wwwroot` sincronizado.

### 2026-04-25 - Coherencia UI/UX del frontend

- Se unifica la capa visual comun del frontend: tokens de controles, superficies, sombras, foco, cards, tablas, tabs y navegacion.
- El boton compartido `components/ui/button.tsx` queda alineado con los tokens propios de Atlas Balance y deja de usar el aspecto generico shadcn por defecto.
- Los tokens shadcn/Tailwind se mapean a las variables propias para evitar dos sistemas visuales compitiendo.
- Login, formularios, selects, tabs de configuracion, tarjetas, tablas y estados hover/focus comparten radios, bordes, alturas y feedback tactil.
- Los campos `type="date"` usados en plazo fijo adoptan el mismo alto, foco, icono de calendario tintado y `color-scheme` claro/oscuro del resto de inputs.
- Se elimina el indicador lateral fuerte en filas marcadas del dashboard y se sustituye por tratamiento de fila completo.
- `layout.css` queda reducido a indice de imports y los estilos se separan por dominio en `frontend/src/styles/layout/`.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK, `robocopy dist ..\\backend\\src\\GestionCaja.API\\wwwroot /MIR` OK y screenshots manuales de `/login` desktop/mobile con Playwright.

### 2026-04-25 - Plazo fijo, autonomos, alertas por tipo y dashboard inmovilizado

- `AUTONOMO` queda disponible como tipo de titular.
- `TipoCuenta` queda disponible con `NORMAL`, `EFECTIVO` y `PLAZO_FIJO`.
- Se crea `PLAZOS_FIJOS` para vencimiento, estado, renovacion manual, cuenta de referencia, notas y auditoria.
- `GET /api/titulares` filtra por `tipoTitular`.
- `GET /api/cuentas` filtra por `tipoTitular` y `tipoCuenta`.
- Cuentas de plazo fijo pueden crearse, editarse y renovarse manualmente desde API/UI.
- Job diario marca plazos fijos proximos a vencer y vencidos, crea notificacion admin y envia email si SMTP esta configurado.
- Alertas de saldo bajo soportan alcance por tipo de titular con prioridad cuenta > tipo titular > global.
- Dashboard separa saldo disponible, inmovilizado y total por divisa.
- Saldos por titular se agrupan por Empresa, Autonomo y Particular.
- Verificacion: backend Release build OK, tests focalizados 12/12 OK, backend sin Testcontainers 103/103 OK, frontend lint/build OK y `wwwroot` sincronizado.

### 2026-04-25 - Planning nuevas funciones de tesoreria

- Creado `Documentacion/Versiones/v-01.05-nuevas-funciones-plazo-fijo-autonomos-alertas.md` como documento operativo para implementar plazo fijo, tipo de titular autonomo, filtros por tipo, alertas por tipo de titular y dashboard agrupado.
- Se fija que `AUTONOMO` sera tipo de titular propio.
- Se fija que `PLAZO_FIJO` sera tipo de cuenta con vencimiento, notificacion a 14 dias, marcado de vencido y renovacion manual.
- Se fija que los plazos fijos cuentan en saldo total, pero el dashboard de divisas debe separar saldo disponible, saldo inmovilizado y total.
- Se fija que las alertas de saldo bajo podran configurarse por cuenta, por tipo de titular o global, con prioridad cuenta > tipo titular > global.

### 2026-04-25 - Permiso global para ver todas las cuentas

- Se agrega `puede_ver_cuentas` a `PERMISOS_USUARIO` mediante migracion EF.
- Los usuarios pueden recibir acceso global de lectura a todas las cuentas sin activar permisos de edicion/importacion.
- La pantalla de usuarios incorpora `Acceso a todas las cuentas` y el checkbox `Ver cuentas`.
- `puede_ver_dashboard` sigue sin conceder acceso global a datos; se conserva el cierre de la fuga de V-01.03.
- Verificacion: tests focalizados de usuarios/permisos/extractos 12/12 OK, backend tests filtrados sin Testcontainers 97/97 OK, backend Release build OK, `npm.cmd run lint` OK, `npm.cmd run build` OK y `wwwroot` sincronizado.

### 2026-04-25 - Actualizaciones post-instalacion

- `update.cmd` y `Actualizar Atlas Balance.cmd` devuelven codigo de salida.
- `scripts\update.ps1` valida paquete antes de autoelevar y soporta `-PackagePath` para actualizar desde una instalacion ya existente apuntando a un paquete descomprimido.
- `scripts\Actualizar-AtlasBalance.ps1` valida paquete antes de tocar la instalacion, actualiza API y Watchdog conservando configuracion, copia scripts/wrappers operativos nuevos a `C:\AtlasBalance`, actualiza `VERSION` y `atlas-balance.runtime.json`, y verifica `/api/health` con `curl.exe -k`.
- Documentacion actualizada con el flujo recomendado: ejecutar `.\update.cmd -InstallPath C:\AtlasBalance` desde el paquete nuevo, o `C:\AtlasBalance\update.cmd -PackagePath C:\Temp\AtlasBalance-V-XX-win-x64 -InstallPath C:\AtlasBalance` cuando la instalacion ya tenga scripts nuevos.
- Regenerado `AtlasBalance-V-01.05-win-x64.zip`; SHA256 `42994915A8AFD014EF807D99E6335944302662FAA21927206ACAF1B8FDE46304`.
- Verificacion: parser PowerShell OK para scripts fuente/empaquetados; backend tests filtrados sin Testcontainers 95/95 OK; paquete sin artefactos `Development`, plantillas, `.env`, `node_modules` ni `.bak-iframe-fix`.

### 2026-04-25 - Cierre de incidencias instalacion Windows Server 2019

- El instalador valida al inicio que se ejecuta desde un paquete release con `api\GestionCaja.API.exe` y `watchdog\GestionCaja.Watchdog.exe`; desde carpeta fuente falla con mensaje claro.
- `install.cmd`/`scripts\install.ps1` preservan mejor argumentos y devuelven codigo de salida.
- `Instalar-AtlasBalance.ps1` mantiene autodeteccion de PostgreSQL 17 antes que 16, da instrucciones concretas si `winget` falla y usa `curl.exe -k` como health check primario.
- En reinstalacion sobre BD existente, el instalador detecta usuarios previos y no escribe una password admin inicial falsa.
- Se agrega `scripts\Reset-AdminPassword.ps1` para resetear un admin: hash bcrypt 12, `primer_login = true`, desbloqueo de login, rotacion de `security_stamp` y revocacion de refresh tokens.
- `Build-Release.ps1` incluye `Reset-AdminPassword.ps1` e `install-cert-client.ps1` en el paquete.
- Documentacion de usuario/release actualizada: ZIP `main` no sirve como instalador, PostgreSQL 17 es valido, PostgreSQL manual es recomendado en Windows Server 2019, `curl.exe -k` manda para health check y el certificado cliente debe instalarse como raiz confiable.
- Generado `AtlasBalance-V-01.05-win-x64.zip`; SHA256 `42994915A8AFD014EF807D99E6335944302662FAA21927206ACAF1B8FDE46304`.
- Verificacion: parser PowerShell OK para scripts tocados y empaquetados; ejecucion desde carpeta fuente falla con mensaje de paquete invalido; backend tests filtrados sin Testcontainers 95/95 OK; paquete sin artefactos `Development`, plantillas, `.env`, `node_modules` ni `.bak-iframe-fix`.

### 2026-04-25 - Importacion con advertencias para filas informativas

- Las filas con solo concepto y sin fecha/monto/saldo ya no bloquean la importacion.
- Se muestran como avisos importables; se heredan fecha y saldo de la ultima fila valida anterior y se guarda monto `0`.
- Se mantiene el bloqueo para filas ambiguas, parcialmente rotas o con valores no numericos.
- Verificacion: `ImportacionServiceTests` 21/21 OK, `npm.cmd run build` OK y `wwwroot` sincronizado desde `frontend/dist`.

### 2026-04-25 - Apertura de version

- Creada la rama local `V-01.05` desde `V-01.03`.
- Marcada `V-01.05` como version actual del proyecto.
- Actualizada version runtime backend a `1.5.0` con `InformationalVersion` `V-01.05`.
- Actualizada version frontend a `1.5.0` y `appVersion` `V-01.05`.
- Actualizados scripts de build/instalacion para usar `V-01.05` por defecto.
- Cerrada `V-01.03` como version publicada y base anterior.
- Verificaciones de apertura: `git diff --check` OK, backend build Release OK y frontend build OK.
