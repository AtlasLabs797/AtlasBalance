# V-01.07

## Estado

Version actual activa.

## Base

- Version anterior publicada: `V-01.06`.
- Rama base local: `main`.
- Rama de trabajo actual: `V-01.07`.

## Alcance

- Rama de trabajo para nuevas funciones y correccion de errores posteriores a `V-01.06`.
- Aplicacion Atlas Balance empaquetable para Windows x64.
- Backend ASP.NET Core 8, frontend React/Vite y Watchdog.
- Paquetes de release en `Atlas Balance/Atlas Balance Release`.
- Documentacion centralizada en `Documentacion`.

## Prioridad

- Corregir bugs antes de sumar funciones vistosas.
- Mantener cada cambio trazado en documentacion tecnica, bugs o bitacora.
- No publicar release sin build limpio y verificaciones relevantes.
- No meter artefactos de release en commits normales salvo que el objetivo sea publicar paquete.

## Instrucciones para cambios

- Asociar cualquier cambio nuevo a `V-01.07`.
- Actualizar `Documentacion/DOCUMENTACION_CAMBIOS.md` en cada sesion.
- Actualizar `Documentacion/DOCUMENTACION_TECNICA.md` si cambia codigo, estructura, scripts o configuracion.
- Actualizar `Documentacion/DOCUMENTACION_USUARIO.md` si cambia instalacion, uso, configuracion o ubicacion de paquetes.
- Consultar `Documentacion/LOG_ERRORES_INCIDENCIAS.md` antes de resolver errores.
- Mantener `Documentacion/REGISTRO_BUGS.md` al dia.
- No documentar contrasenas, tokens ni datos privados.

## Cambios registrados

### 2026-05-20 - Fix updater ZIP con entrada raiz './'

- Se corrigio una regresion funcional en `TryExtractPackageSafely`: entradas de directorio raiz del ZIP (`./` o equivalente) ya no se rechazan como Zip Slip.
- Se mantiene la proteccion de path traversal: solo se permite igualdad exacta con la raiz para entradas de directorio; el resto debe quedar bajo `packageRoot`.
- Impacto: paquetes validos generados por herramientas comunes de ZIP vuelven a actualizar correctamente sin debilitar la validacion de seguridad.
- Verificacion: intento de test focalizado bloqueado porque `dotnet` no esta disponible en este entorno.

### 2026-05-19 - Jerarquia visual y pesos de accion UI/UX pre-entrega

- Auditoria UI/UX con skills de Build Web Apps, skills locales de `Skills Curated` y dos subagentes read-only.
- Headers, cards, modales y formularios reciben jerarquia visual mas clara para que saldos, estados y acciones criticas destaquen.
- Tablas operativas reducen peso visual frente a modales; headers sticky se limitan a wrappers de tabla concretos.
- Acciones primarias/destructivas se diferencian: crear/confirmar usa `button-primary`, eliminar/revocar/restaurar backup usa `button-danger` y actualizar app usa `button-warning`.
- Importacion muestra metricas de validacion en bloque; Backups muestra resumen de ultima copia correcta en pagina principal y rutas truncadas.
- Cuenta detalle destaca `Saldo total`; Dashboard marca vencimientos de plazo fijo cercanos.
- Verificacion: `npm.cmd run lint`, `npm.cmd exec tsc -- --noEmit`, `npm.cmd run build` y `git diff --check` OK.
- Pendiente: QA visual/E2E autenticado real antes de declarar entrega final a clientes.

### 2026-05-19 - Skills Curated pre-release y hardening Git

- Ejecutadas las skills locales relevantes una por una: seguridad, debugging/tests, inspeccion de codebase, TDD, dogfood web QA, harden, polish y humanizalo.
- `Skills Curated/` queda fuera de Git, alineado con la regla de no subir `Skills/` ni tooling local de skills.
- `.gitignore` raiz y `Atlas Balance/.gitignore` ignoran certificados/keystores/dumps y `backend/**/TestResults/`.
- El CI de secretos excluye `Skills Curated/` junto a `Otros/` y `Skills/`.
- Verificacion: secret scan local 0 hallazgos, `npm audit` 0 vulnerabilidades, NuGet vulnerable audit 0 paquetes vulnerables, frontend lint/TypeScript/build OK, backend sin Docker/Testcontainers 254/254 OK.
- Bloqueo: Docker/Testcontainers no esta disponible en esta maquina; E2E visual/autenticado real sigue pendiente.

### 2026-05-19 - Refinamiento UI/UX pre-entrega

- `AppSelect` nativo queda sin doble flecha visual.
- Tablas tipo hoja: cuenta fija columnas de seleccion/fila y Extractos deja de prometer semantica `grid` sin teclado de grid.
- Extractos aclara que los filtros de columna aplican a la pagina actual y muestra total del servidor.
- Configuracion completa tabs ARIA con paneles y navegacion de teclado.
- Modal de token muestra errores dentro del modal; metricas de tokens dejan de mostrar ceros falsos cuando falla la carga.
- Errores visibles se anuncian con `role="alert"` y acciones repetidas reciben labels contextuales.
- Backups anuncia restauracion como estado critico con foco; EvolucionChart agrega tabla accesible.
- Verificacion: `npm.cmd run lint`, `npm.cmd exec tsc -- --noEmit`, `npm.cmd run build` y `git diff --check` OK.
- Pendiente: validacion visual/E2E autenticada real antes de llamar la entrega "lista para clientes".

### 2026-05-19 - Auditoria integral y hardening pre-release

- Corregidos saldos actuales inconsistentes en resumen de cuenta y API OpenClaw: ahora usan `fila_numero` como fuente de verdad.
- Corregida la idempotencia de importacion cuando cambian los indices de fila por cabeceras o lineas extra.
- Las transacciones de importacion y movimientos de plazo fijo se limpian en `finally`.
- Configuracion rechaza textos `null` y body nulo en `smtp/test` con `400`.
- UI/UX: select nativo, modal de importacion con foco controlado, errores de celda persistentes/anunciados, estados textuales de importacion, labels asociados y grafica con alternativa accesible.
- PostgreSQL 16+ queda reflejado en documentacion viva.
- Verificacion: tests focalizados 52/52 OK, `ConfiguracionControllerTests` 8/8 OK, backend sin Docker/Testcontainers 254/254 OK, frontend lint/TypeScript/build OK.
- Auditorias externas npm/NuGet ejecutadas despues con aprobacion: 0 vulnerabilidades.
- Bloqueo: suite completa 254/256 por Docker/Testcontainers no disponible; siguen pendientes E2E autenticado con PostgreSQL real, ZIP firmado y backup/restore real antes de release final.

### 2026-05-18 - Verificacion backend post-Codex Security

- Se instalo SDK .NET 8.0.419 local en `C:\tmp\dotnet-sdk-8.0.419`, respetando `global.json`.
- Restore/build backend OK.
- Suite backend sin Docker/Testcontainers 249/249 OK.
- Suite backend completa 249/251: solo fallan `ExtractosConcurrencyTests` y `RowLevelSecurityTests` por Docker/Testcontainers no disponible/configurado.
- NuGet vulnerable audit: 0 paquetes vulnerables en API, Watchdog y tests.

### 2026-05-17 - Revision Codex Security y hardening

- Revision de seguridad con Codex Security y subagentes por auth/RLS, frontend, scripts/update, IA/export/import y supply chain.
- No-admins ya no ven cuentas/extractos de titulares soft-deleted; auditoria de celdas de extractos eliminados queda oculta para no-admins.
- Exportacion manual exige permiso operativo de cuenta; descarga mantiene lectura.
- OpenRouter fuerza `zdr=true` y `data_collection=deny` en todas las llamadas.
- Actualizacion desde la app rechaza `sourcePath` manual y solo aplica assets oficiales descargados, con digest y firma.
- Backups/Watchdog no ejecutan `pg_dump`, `pg_restore` ni `docker` por PATH; se documenta `DockerCliPath`.
- Importacion limita celdas a 4096 caracteres; exportacion XLSX endurece escape de formulas.
- `package-lock.json` queda sin dependencia raiz huerfana.
- Verificacion: `npm audit` 0 vulnerabilidades, NuGet vulnerable audit 0 paquetes vulnerables, `npm ls` sin extraneous, parse PowerShell OK, `git diff --check` OK, SDK .NET 8.0.419 local instalado, restore/build backend OK, suite backend sin Docker/Testcontainers 249/249 OK. Suite completa 249/251: solo fallan `ExtractosConcurrencyTests` y `RowLevelSecurityTests` por Docker/Testcontainers no disponible/configurado.

### 2026-05-17 - Autoactualizacion segura desde GitHub Release

- `AutoUpdateJob` revisa actualizaciones una vez al dia desde la hora UTC configurada cuando `app_update_auto_enabled=true`.
- `Configuracion > Sistema` permite activar el modo automatico, definir hora UTC y consultar ultimo resultado automatico.
- El modo automatico usa el flujo existente de GitHub Release oficial, digest SHA-256, firma `.zip.sig`, carpeta segura `UpdateSourceRoot` y aplicacion via Watchdog.
- `ActualizacionService` limita ZIP descargado a 300 MiB, contenido extraido a 1 GiB, entrada individual a 512 MiB y 10000 entradas maximas.
- El automatico queda desactivado por defecto; activarlo en produccion exige elegir una ventana real de mantenimiento.
- Verificacion: tests backend focalizados 25/25 OK con SDK local, frontend lint OK, TypeScript OK, build OK y `wwwroot` sincronizado 65/65.

### 2026-05-17 - IA: recibos/facturas sin cargos de tarjeta

- `RECIBOS/FACTURAS DETECTADOS` excluye tarjeta/TPV/datáfono y prestamos/leasing cuando la coincidencia viene por terminos genericos como `cargo`.
- Verificacion: test focalizado OK y suite backend sin Testcontainers 242/242 OK.

### 2026-05-17 - IA: contexto financiero y errores de proveedor afinados

- El contexto IA deja de usar terminos flojos de comision (`cuota`, `servicio`, `tarjeta`, `transferencia`) como disparadores directos.
- La seccion de seguros en contexto IA queda limitada a cargos negativos y excluye Seguridad Social/TGSS, Generalitat, transferencias, anulaciones, devoluciones y reembolsos.
- Los errores de red de OpenRouter/OpenAI muestran diagnostico tecnico saneado sin exponer prompt, respuesta ni API key.
- Se agregan regresiones en `AtlasAiServiceTests` para que esos falsos positivos no inflen los totales enviados al proveedor.
- Verificacion: `git diff --check` OK; frontend lint, TypeScript y build OK; tests backend pendientes porque esta maquina no tiene SDK .NET.

### 2026-05-17 - Revision bancaria afinada con capturas reales

- Comisiones deja de detectar `tarjeta` como comision por si sola.
- Seguros solo revisa cargos negativos.
- Seguros excluye Seguridad Social/TGSS en plural, Generalitat, transferencias, anulaciones, devoluciones y reembolsos.
- Se agregan pruebas de regresion con los conceptos reportados en capturas de comisiones y seguros.
- Verificacion: `git diff --check` OK; tests backend pendientes porque `dotnet` no esta disponible en esta maquina.

### 2026-05-16 - Importacion, revision y MFA

- Importacion acepta columnas extra vacias o ausentes en las filas pegadas; quedan en blanco y no bloquean la importacion.
- Revision deja de clasificar transferencias normales como comisiones.
- Revision excluye Seguridad Social, Seguro Social, TGSS y Tesoreria General de la deteccion de seguros.
- El recuerdo MFA pasa a 90 dias y el logout ya no borra el dispositivo recordado.
- Administracion de usuarios permite revocar Authenticator: limpia MFA, rota `security_stamp`, revoca sesiones activas y audita `MFA_REVOKED`.
- `Reset-AdminPassword.ps1` tambien limpia MFA para recuperar admins que hayan perdido el Authenticator.
- Verificacion: frontend lint OK, TypeScript OK y build OK. Tests backend focalizados bloqueados porque `dotnet` no esta disponible en esta maquina.

### 2026-05-16 - Apertura de version V-01.07

- `version_actual.md` pasa a declarar `V-01.07` como version activa.
- Fuentes runtime actualizadas a `V-01.07` / `1.7.0`.
- `V-01.06` queda documentada como base anterior.
- Rama de trabajo creada: `V-01.07`.
- Verificacion: barrido de referencias activas confirma `V-01.07` / `1.7.0` en fuentes runtime, scripts y documentacion viva.

### 2026-05-16 - Auditoria correctiva de seguridad y estabilidad

- Se corrige el riesgo de dejar la app sin administrador activo desde gestion de usuarios.
- Se endurece la comunicacion API -> Watchdog para permitir solo BaseUrl local/loopback y no filtrar `X-Watchdog-Secret`.
- Watchdog valida bodies y rutas invalidas con `400` controlado.
- Backups, restauraciones y actualizaciones usan timeout defensivo de 30 minutos para procesos externos.
- `AuthService` reduce coste de armado de permisos/preferencias con lookup por diccionario.
- El frontend corrige timeout de sesion y reactividad de permisos.
- Se agregan tests de regresion para usuarios y Watchdog.
- Verificacion: lint frontend OK, typecheck frontend OK, build frontend OK, tests focalizados 14/14 OK, suite backend sin Testcontainers 229/229 OK, npm audit 0 vulnerabilidades, NuGet vulnerable 0 vulnerabilidades.
- Pendiente para release: suite completa con Docker/Testcontainers y revision separada de importacion, saldo actual, configuracion nula y cooldown SMTP.
