# V-01.02

## Estado

Version publicada y cerrada. Base anterior de `V-01.03`.

## Alcance

- Rama de trabajo para nuevas funciones y correccion de errores posteriores a `V-01.01`.
- Aplicacion Atlas Balance empaquetable para Windows x64.
- Backend ASP.NET Core 8, frontend React/Vite y Watchdog.
- Paquetes de release en `Atlas Balance/Atlas Balance Release`.
- Documentacion centralizada en `Documentacion`.

## Prioridad

- Corregir bugs antes de sumar funciones vistosas.
- Mantener cada cambio trazado en tickets, bugs o documentacion tecnica.
- No publicar release sin build limpio y verificaciones relevantes.
- No meter artefactos de release en commits normales salvo que el objetivo sea publicar paquete.

## Instrucciones para cambios

- No asociar cambios nuevos a `V-01.02`; cualquier trabajo posterior pertenece a `V-01.03` o superior.
- Actualizar `Documentacion/DOCUMENTACION_CAMBIOS.md` en cada sesion.
- Actualizar `Documentacion/DOCUMENTACION_TECNICA.md` si cambia codigo, estructura, scripts o configuracion.
- Actualizar `Documentacion/DOCUMENTACION_USUARIO.md` si cambia instalacion, uso, configuracion o ubicacion de paquetes.
- Consultar `Documentacion/LOG_ERRORES_INCIDENCIAS.md` antes de resolver errores.
- Mantener `Documentacion/REGISTRO_BUGS.md` al dia.
- No documentar contrasenas, tokens ni datos privados.

## Cambios registrados

### 2026-04-20 - Publicacion GitHub Windows x64

- Version preparada para publicacion en la rama `V-01.02`.
- Tag de distribucion previsto: `V-01.02-win-x64`, evitando colision semantica con la rama de version.
- Asset oficial: `AtlasBalance-V-01.02-win-x64.zip`.
- SHA256 del asset: `F2BDC7BAF0168631C6E11E2E802B4019A5A88BA944CA8426CFD3B5353D865386`.
- Release notes basadas en este documento y en la bitacora `Documentacion/DOCUMENTACION_CAMBIOS.md`.
- Verificaciones: build de release OK, frontend lint OK, backend filtrado sin Testcontainers 82/82 OK; suite completa 82/83 por Docker/Testcontainers no disponible.

### 2026-04-20 - Release autonoma con scripts one-click

- Paquete de release ampliado con `install.cmd`, `update.cmd`, `uninstall.cmd` y `start.cmd`.
- Instalador autoelevado con preparacion de PostgreSQL 16 gestionado mediante `winget` cuando no se usa una instancia existente.
- Runtime instalado registra PostgreSQL gestionado para que `start` y `update` arranquen la base antes de backend/frontend.
- Desinstalador completo para servicios, firewall, atajos, claves de Data Protection, carpeta instalada y base gestionada.
- Documentacion de usuario y tecnica actualizada para el nuevo flujo.

### 2026-04-20 - Auditoria tecnica profunda y hardening

- Secretos SMTP/API key en BD protegidos con Data Protection y migracion automatica de valores legacy en claro.
- Keyring Data Protection persistido fuera de rutas publicas en produccion y protegido con DPAPI en Windows.
- Corregido bug de autorizacion donde `PuedeVerDashboard` global podia abrir alcance global de datos.
- Descargas de exportacion restringidas a `.xlsx` dentro de `export_path`.
- Watchdog forzado a `localhost:5001`.
- Wildcards en `AllowedHosts` bloqueados fuera de Development; produccion exige hosts explicitos.
- Scripts de backup/restore/servicios endurecidos y alineados con `AtlasBalance.*` / `atlas_balance_app`.
- Eliminados logs y artefactos auxiliares con informacion de login/sesion.
- Verificaciones: backend build Release 0 warnings, backend tests completos 83/83, frontend lint/build OK, NuGet/npm audit sin vulnerabilidades.

### 2026-04-20 - Auditoria de seguridad y bugs

- Limpieza de secretos/defaults de desarrollo en configuracion versionable.
- Plantillas de configuracion API/Watchdog y `.env.example` sin secretos reales.
- Correccion de version runtime residual `V-01.01` en seed/check de actualizaciones.
- Correccion de textos mojibake en importacion y SMTP.
- CI endurecido con GitHub Actions fijadas por SHA.
- Verificaciones: scanner de secretos 0 hallazgos, NuGet/npm audit sin vulnerabilidades, backend tests Release sin Docker 81/81, frontend lint/build OK.

### 2026-04-20 - Cierre de bugs reportados

- Corregidos restos `atlasbalnace` / `atlas-blance` en configuracion, scripts, UI, tests y documentacion activa.
- Corregido `Instalar-AtlasBalance.ps1`, que seguia escribiendo runtime `V-01.01`.
- Centralizado el evento interno de importacion en `IMPORTACION_COMPLETADA_EVENT`.
- Frontend recompilado y sincronizado con `backend/src/GestionCaja.API/wwwroot`.
- Verificaciones: backend tests Release completos 82/82, frontend lint/build OK, `atlas_balance_db` activo en Docker.
