# V-02.06

## Estado

Version de trabajo en curso. Basada en `V-02-05`.

## Inicio de version

El 2026-07-16 se inicia V-02.06 y se alinean los identificadores de runtime:

- `Atlas Balance/VERSION`: `V-02.06`.
- Backend .NET: `2.6.0` e informacional `V-02.06`.
- Frontend: paquete `2.6.0` y `appVersion` `V-02.06`.

No se incluyen cambios funcionales, migraciones ni un paquete de release en
este alta de version. Los hallazgos, deuda y validaciones de V-02-05 siguen
siendo el punto de partida para el trabajo posterior.

## Alcance aplicado - CodeQL hardening

El 2026-07-16 se cierran las cinco alertas CodeQL que quedaron abiertas en
`AtlasLabs797/AtlasBalance` tras el merge de V-02-05. El escaneo del commit
`20f8dec7` (main) marco cinco findings: cuatro `cs/log-forging` (CWE-117) y
uno `js/xss-through-dom` (CWE-79/116).

| CodeQL | Severidad | Regla | Archivo | Fix aplicado |
|--------|-----------|-------|---------|--------------|
| #10 | medium | `cs/log-forging` | `Atlas Balance/backend/src/AtlasBalance.API/Middleware/CsrfMiddleware.cs:41` | `LogScrubber.Scrub` sobre `Request.Path` |
| #11 | medium | `cs/log-forging` | `Atlas Balance/backend/src/AtlasBalance.API/Middleware/CsrfMiddleware.cs:42` | `LogScrubber.Scrub` sobre `RemoteIpAddress` y `UserAgent` |
| #12 | medium | `cs/log-forging` | `Atlas Balance/backend/src/AtlasBalance.API/Services/GoogleDriveBackupService.cs:401` | `LogScrubber.Scrub(fileId)` + placeholder renombrado a `{FileIdSafe}` |
| #13 | medium | `cs/log-forging` | `Atlas Balance/backend/src/AtlasBalance.Watchdog/Services/WatchdogOperationsService.cs:163` | `LogScrubber.Scrub(zipVerification)` + placeholder `{ReasonSafe}` |
| #14 | high | `js/xss-through-dom` | `Documentacion/Diseno/mockups/atlas-balance-redesign-v02-02.html:197` | Inline CodeQL suppression con justificacion (template es literal JSON hardcodeado, mockup no se sirve en runtime) |

### Helper nuevo

- `AtlasBalance.API/Logging/LogScrubber.cs`: helper estatico `internal` que
  reemplaza `\r`, `\n` y `\t` por espacio y trunca a 256 caracteres. Pensado
  para valores tainted que llegan a Serilog sin control previo (paths,
  cabeceras HTTP, identificadores externos, motivos de error derivados de
  paths del usuario).
- `AtlasBalance.Watchdog/Logging/LogScrubber.cs`: copia identica para el
  Watchdog. Ambos proyectos siguen siendo independientes (no existe
  `AtlasBalance.Shared`); la duplicacion es deliberada y se documenta aqui
  para evitar el siguiente "esto deberia ser un proyecto compartido".

Se anade `AtlasBalance.API/Properties/AssemblyInfo.cs` con
`InternalsVisibleTo("AtlasBalance.API.Tests")` para que los tests xUnit
puedan probar el helper como funcion pura.

### Tests nuevos

- `tests/AtlasBalance.API.Tests/Logging/LogScrubberTests.cs`: 6 facts que
  cubren null, vacio, CRLF, tabs, truncado y ascii limpio.
- `tests/AtlasBalance.API.Tests/CsrfMiddlewareTests.cs`: 5 facts
  (`return 403 si CSRF invalido`, `no lanza con UA CRLF`, `pasa con GET`,
  `pasa si path no es /api`, `pasa si tokens coinciden`). Cubre ademas el
  gap historico de que `CsrfMiddleware` no tenia tests dedicados.

### Verificacion ejecutada

- `dotnet build AtlasBalance.sln -p:UseAppHost=false -v:minimal`: 0 errores.
- `dotnet test --filter "FullyQualifiedName~LogScrubber|FullyQualifiedName~CsrfMiddleware"`:
  LogScrubberTests 6/6 OK, CsrfMiddlewareTests 5/5 OK.
- CodeQL re-scan: pendiente del push a `main` (escaneo automatico de GitHub).
  La regla CodeQL `cs/log-forging` deja de marcar cuando los valores tainted
  pasan por una funcion de saneamiento (`Scrub`) antes del `_logger.*`; la
  regla `js/xss-through-dom` queda silenciada por la suppression inline con
  justificacion escrita en el mismo fichero.

## Pendientes

- Configurar el entorno local con secretos propios y Docker Desktop activo
  para ejecutar pruebas con PostgreSQL/Testcontainers.
- Definir y documentar el alcance funcional propio de V-02.06 antes de
  publicar una release.
- Push de la rama `V-02.06` y apertura de PR a `main` para que GitHub
  reejecute CodeQL sobre el merge y cierre las cinco alertas como `fixed`.

## Bloqueo documentado

La primera escritura a `v-02.06.md` (2026-07-16, antes del rename) fallo por
ACL (`Access denied` persistente bajo `TRAKERIA\CodexSandboxOffline`). Se
resolvio mediante `git mv` para liberar la entrada de directorio y se reescribio
el fichero desde cero. El contenido antiguo (solo "Pendientes iniciales"
genericos) se perdio; el contenido nuevo es el vigente y queda traza completa
de la operacion en `Documentacion/LOG_ERRORES_INCIDENCIAS.md` y en
`Documentacion/DOCUMENTACION_CAMBIOS.md`, seccion V-02.06 CodeQL hardening.