# V-02.09

## Estado

**En preparacion.** Version abierta el 2026-08-05 desde `V-02.08` para ejecutar
el plan de 12 fases que reescribe el asistente IA financiero sobre Atlas
Balance. La rama `V-02.09` parte de `V-02.08` (commit `c040d1a`).

## Inicio de version

V-02.09 arranca el ciclo de estabilizacion + planificacion del asistente IA.
Esta primera entrada cubre la **Fase 1: Estabilizar la IA actual**, el primer
paso del Bloque A (consultas esenciales) del plan. Las once fases restantes
se iran anadiendo bajo el mismo paraguas de V-02.09.

## Fase 1: Estabilizar la IA actual

Antes de anadir capacidades, se cierran seis agujeros del flujo actual:

### 1.1 DbContext concurrente en `BuildFinancialContextAsync`

`AtlasAiService.BuildFinancialContextAsync` ejecutaba los `AppendPeriodSummary`
con `Task.WhenAll` contra el mismo `AppDbContext`. Ademas, la condicion del
resumen anual se anadia a `periodTasks` **despues** del `await`, asi que la
consulta anual se perdia silenciosamente y nunca llegaba al proveedor.

- `Atlas Balance/backend/src/AtlasBalance.API/Services/AtlasAiService.cs`:
  - `BuildFinancialContextAsync` reemplaza el `Task.WhenAll` por awaits
    secuenciales. Cada bloque se ejecuta, se espera y se concatena en orden.
  - La condicion del resumen anual se evalua junto al resto, ya no despues.
  - `rollingMonthStart` se elimina: la variable ya no se usa tras el cambio
    de Fase 1.2.

### 1.2 "ultimo mes" pasa a ser el mes natural anterior

Antes, "ultimo mes", "ultimos 30 dias" y "ultimas 4 semanas" mapeaban a una
ventana rodante de 30 dias (`today.AddDays(-30) -> today`). En meses de 31
dias, esa ventana se solapaba con el mes en curso y dejaba fuera los dias
1-2 del mes anterior. Ademas, no coincidia con la intencion natural del
usuario cuando dice "ultimo mes".

- `AtlasBalance.API/Services/AtlasAiService.cs`:
  - `BuildFinancialContextAsync`: el bloque de "ultimo mes" ahora se envia a
    `AppendPeriodSummaryAsync(previousMonthStart, previousMonthEnd)`.
  - `TryResolveFinancialRankingIntent`: misma correccion. "ultimo mes" y
    "mes anterior" / "mes pasado" producen el mismo rango (mes natural
    anterior), asi que el texto de la respuesta es consistente.

### 1.3 Timeout frontend 15s -> 45s para `/ia/chat`

El frontend axios tenia un timeout defensivo de 15s para todo. El backend
tiene un HttpClient de 45s para OpenRouter/OpenAI/MiniMax (`Program.cs`).
Cualquier consulta medianamente larga al proveedor se cancelaba a los 15s
antes de recibir respuesta.

- `Atlas Balance/frontend/src/components/ia/AiChatPanel.tsx`: la llamada a
  `/ia/chat` lleva ahora `timeout: 45_000` solo en esa peticion. El resto
  mantiene el timeout defensivo de 15s.

### 1.4 Sin texto libre del proveedor en logs ni auditoria

El campo `provider_error` se serializaba en `LogProviderErrorAsync` y en el
`extra` de la auditoria. El regex de redaction era fragil: si el formato
del error cambiaba (p.ej. "API key provided: sk-..." con la palabra
"provided:" entre la clave y el valor) el regex fallaba y la credencial
llegaba a la BD. La clasificacion del error (data policy, ZDR, model
restrictions, etc.) sigue funcionando sobre el payload en memoria, pero NO
se escribe.

- `AtlasBalance.API/Services/AtlasAiService.cs`:
  - `LogProviderErrorAsync`: se quitan `provider_error` del `extra` en las
    dos rutas de error (HTTP no-2xx y respuesta con error top-level). El
    clasificador (`IsOpenRouterDataPolicyError`,
    `IsOpenRouterModelRestrictionError`) sigue intacto.
  - El log del servidor pasa a registrar `runtime_model` ademas de
    `model`, para que el operador pueda distinguir el modelo que el
    operador configuro del que OpenRouter realmente uso en la llamada
    final (importante para auditoria de costes y ZDR).

### 1.5 Catalogo OpenRouter filtrado a solo permitidos

`/ia/modelos` con `provider=OPENROUTER` devolvia hasta 80 modelos del
catalogo de OpenRouter. El backend solo permite 7 modelos en
`AiConfiguration.SuggestedOpenRouterModels`, asi que el usuario podia
seleccionar uno que luego seria rechazado con 400. La diferencia entre lo
mostrado y lo permitido era confusa.

- `AtlasBalance.API/Services/AtlasAiService.cs`: `GetModelsAsync` filtra
  por proveedor con `IsModelAllowedForProvider` antes del `Take(80)`. El
  helper se apoya en `AiConfiguration.IsAllowedOpenRouterModel` /
  `IsAllowedOpenAiModel` / `IsAllowedMiniMaxModel` (mismas allowlist que
  usa `IsAllowedModel` en `AskAsync`, asi que el catalogo y la ejecucion
  nunca divergen).
- `AtlasBalance.API/DTOs/IaDtos.cs`: nuevo campo `Permitido` (bool) en
  `IaModelResponse`. Marcado `true` para los catalogos de OpenAI y
  MiniMax (siempre fueron estaticos) y para los OpenRouter que pasan el
  filtro. Si en el futuro se decide enviar el catalogo completo al
  frontend, este campo permite etiquetar la entrada.
- `Atlas Balance/frontend/src/types/index.ts`: la interfaz `IaModel` gana
  el campo `permitido: boolean`.

### 1.6 Chat unico: el flotante y la pagina /ia comparten conversacion

Antes, el componente `AiChatPanel` se montaba en dos sitios (el boton
flotante de la `TopBar` y la pagina `/ia`) y cada uno tenia su propio
`useState` local para mensajes, modelo seleccionado, errores, etc.
Resultado: lo que preguntabas en el flotante desaparecia al cerrar y
abrir; lo que seleccionabas en uno no se reflejaba en el otro. La unica
memoria realmente compartida era el `ConversationContext` estructurado
del backend, que el usuario nunca ve.

- `Atlas Balance/frontend/src/stores/aiChatStore.ts` (nuevo): store
  Zustand singleton con el estado de la conversacion (`messages`,
  `loading`, `error`, `lastFailedPrompt`), la eleccion de modelo
  (`selectedModel`, `openRouterModels`), la `IaConfig` cacheada con TTL
  de 30s para evitar dobles `GET /ia/config`, y las acciones
  `ensureConfig`, `loadOpenRouterModels`, `ask`, `reset`, `clear`. Las
  deduplicaciones de peticiones en vuelo siguen el mismo patron que
  `iaAvailabilityStore` (guard `openRouterInflight`).
- `Atlas Balance/frontend/src/components/ia/AiChatPanel.tsx`: pasa a
  consumir el store via selectores. `messages`, `loading`, `error`,
  `lastFailedPrompt`, `selectedModel`, `openRouterModels` y `config`
  dejan de ser `useState` locales. El `input` (texto a medio escribir)
  sigue siendo local por instancia, igual que los `useRef` del DOM
  (`scrollRef`, `inputRef`) y los `useEffect` que dependen del DOM
  (auto-scroll, focus). El boton "Nueva conversacion" llama a
  `useAiChatStore.getState().reset()`, que encapsula el
  `POST /ia/conversacion/nueva` y el `setMessages([])`.
- `Atlas Balance/frontend/src/types/index.ts`: se reexportan
  `ChatMessage`, `AssistantMessageMeta`, `AssistantLink` y
  `AssistantClarificationOption` desde el store para que el resto del
  frontend pueda seguir importandolos de `@/types`.
- `Atlas Balance/frontend/src/services/api.ts`: en `clearSessionState`
  (logout, 419/440, refresh fallido) se añade
  `useAiChatStore.getState().clear()` para que el siguiente usuario en
  el mismo navegador no vea la conversacion del anterior al montar el
  panel.

Lo que se conserva: la variante visual `compact` del flotante y la
full de `/ia`, las dos rutas siguen montando `<AiChatPanel />` sin
cambios en `TopBar.tsx` ni en `pages/IaPage.tsx`. El backend no se
toca: su `IConversationMemory` ya era compartido por `userId`.

## Tests nuevos y modificados

- **Nuevo** `backend/tests/AtlasBalance.API.Tests/AtlasAiServiceStabilizationTests.cs`
  con 8 tests cubriendo los seis arreglos:
  - `AskAsync_DeterministicRanking_LastMonth_Should_Use_Previous_Calendar_Month`
    (Theory, 2 casos): valida Fase 1.2.
  - `AskAsync_Context_For_Annual_Question_Should_Include_Annual_Summary`:
    valida Fase 1.1 (resumen anual llega al proveedor).
  - `AskAsync_Context_Should_Include_All_Matched_Periods_In_Order`: blinda
    contra una regresion a paralelizar (DbContext no es thread-safe).
  - `AskAsync_Audit_Should_Not_Contain_Pii_From_Prompt` (Theory, 3 casos):
    PII en la pregunta del usuario (email, IBAN, DNI, tarjeta) no aparece
    en la auditoria.
  - `GetModelsAsync_OpenRouter_Should_Only_Return_Allowed_Models`: Fase
    1.5. Verifica que el catalogo solo incluye los 7 permitidos y que
    los 3 modelos de control (Claude 3.5, GPT-4o, Llama 3.1) NO aparecen.
  - `GetModelsAsync_OpenAi_And_MiniMax_Should_Mark_All_Models_Allowed`:
    OpenAI y MiniMax siempre llevan `Permitido=true`.
  - `AskAsync_ProviderHttpError_Audit_Should_Not_Contain_Provider_Free_Text`:
    un error 500 con credenciales y hostnames internos no llega ni a la
    auditoria ni al mensaje que ve el usuario.
  - `AskAsync_ProviderHttpError_Audit_Extra_Should_Not_Have_ProviderError_Key`:
    la clave `provider_error` no se persiste; `retry_after_seconds` si
    (es campo estructurado, no texto libre).

- **Modificado** `backend/tests/AtlasBalance.API.Tests/AtlasAiServiceTests.cs`:
  seis tests existentes que asertaban que el texto del proveedor estaba en
  la auditoria. Se invierten: ahora se asegura que NO esta y que los
  campos estructurados (status, kind, retry_after_seconds, model,
  runtime_model) si lo estan.
  - `AskAsync_Should_Handle_Provider_Model_Not_Found_Without_Fallback`
  - `AskAsync_Should_Report_OpenRouter_Data_Policy_404_As_Privacy_Routing_Error`
  - `AskAsync_Should_Report_OpenRouter_Model_Restrictions_404_Clearly`
  - `AskAsync_Should_Surface_OpenRouter_Provider_Error_Without_Fallback_Array`
  - `AskAsync_Should_Report_OpenRouter_Rate_Limit_Retry_After_Clearly`
  - `AskAsync_Should_Handle_Top_Level_Provider_Error_With_Http_200`

## Verificacion local

- `dotnet build` (Release, output redirigido): **0 errores, 7 warnings
  preexistentes** (deprecaciones V-02.04 de `UseXminAsConcurrencyToken` y
  `PostgreSqlStorage`).
- `npm.cmd run lint --max-warnings 0` (V-02.09 / Fase 1.6): **OK**.
  Ninguna referencia muerta en el panel tras mover el estado al store.
- `npx.cmd tsc --noEmit` (V-02.09 / Fase 1.6): **OK**. Las reexportaciones
  desde `@/stores/aiChatStore` resuelven sin acoplamiento circular con
  `api.ts` ni `paisScopeStore`.
- `npm.cmd run test:unit` (V-02.09 / Fase 1.6): **57/57 PASS**. No hay
  tests automaticos para el store todavia (la cobertura del helper
  `friendlyIaError` se mantiene verde, que es lo que cubre la accion
  `ask`). El smoke manual entre el flotante y `/ia` queda pendiente
  para validar la persistencia visual de mensajes y modelo.
- `dotnet test --filter 'Category!=Postgres'`: **671/690** pasan. Los 19
  fallos restantes son tests `Category=Postgres` que dependen de
  Testcontainers (Docker no disponible local). Es la misma baseline que
  V-02.08 reporto (660/679, mismo patron). Los 11 tests anadidos por
  esta fase pasan.
- `npx tsc --noEmit`: **OK**.
- `npm.cmd run lint --max-warnings 0`: **OK**.
- `npm.cmd run test:unit`: **46/46 PASS** (sin cambios en el frontend de
  este modulo de tests).
- `git diff --check`: **OK** (solo avisos de CRLF, normales en Windows).
- `Check-VersionAlignment.ps1 -ExpectedVersion V-02.09`: **OK**,
  `V-02.09` / `2.9.0` alineado en VERSION, Directory.Build.props,
  package.json, package-lock.json, SeedData.app_version, release.default,
  Build-Release.default, Instalar.default, install.default.

## Pendientes de Fase 1

- El gate `Category=Postgres` (Testcontainers) no se ha ejecutado en
  este host por ausencia de Docker. Las nuevas aserciones de Fase 1.1
  ("se anade el resumen anual al contexto") corren sobre InMemory y solo
  verifican la forma del payload HTTP; el test real contra PostgreSQL
  con 50k filas queda pendiente para CI.
- `npm.cmd run build` (Vite) **BLOQUEADO** por `EPERM` de Vite/Rolldown
  al copiar `public/fonts/*.ttf` a `dist/fonts/`. Mismo bloqueo conocido
  del sandbox documentado en AGENTS.md seccion 8. La compilacion
  `tsc --noEmit` y el lint pasan en limpio. La build de Vite se hara en
  host con permisos sobre `dist/`.
- En esta fase no se ha anadido test automatico para el timeout
  frontend de 45s (cambio puramente declarativo en `AxiosRequestConfig`).

## Estado tras auditoria posterior

La documentacion de cierre previa afirmaba incorrectamente que las doce fases
estaban cerradas. La auditoria del 2026-08-06 encontro que los servicios de
planificacion, herramientas, memoria y ayuda se habian creado pero no estaban
conectados al endpoint real. Se ha conectado el flujo local, las aclaraciones,
la ayuda documental, la memoria, la frontera DLP y el planificador semantico
sin contexto financiero. Los planes compuestos validan ahora dependencias de
orden. Quedan pendientes la ejecucion de la bateria PostgreSQL de 50.000
movimientos y las pruebas de aceptacion integradas en CI antes de poder
declarar cerradas las fases 5, 11 y 12. Un rechazo semantico nunca debe
degradar a una peticion externa con contexto financiero.

## Pendientes del plan V-02.09

- Fases 2 a 12, agrupadas en Bloque B (flexibilidad), C (analisis) y
  D (privacidad, conversacion, UX final).
- Publicacion del paquete `V-02.09-win-x64` cuando todas las fases
  esten cerradas y los gates de CI (incluido `Category=Postgres`) en
  verde.

## Fase UI (2026-08-07): composer, mensajes y modo de pensamiento

Reescritura visual del chat IA para alinear la composicion con el
Design System (DESIGN.md / design-tokens.css). Aplica por igual a la
burbuja flotante del `TopBar` y a la pagina `/ia`, que ya comparten
el mismo componente desde la Fase 1.6.

### Frontend

- `frontend/src/utils/aiModels.ts` anade `ThinkingMode`,
  `ThinkingModeOption`, `getThinkingModeOptions(provider)` y
  `normalizeThinkingMode(provider, value)`. La normalizacion cae a
  `auto` si el valor no encaja con el provider activo.
- `frontend/src/types/index.ts` amplia `IaConfig` con
  `thinking_modes?: IaThinkingModeOption[]` y `IaChatResponse` con
  `thinking_mode_aplicado?: string`.
- `frontend/src/stores/aiChatStore.ts` incorpora el estado
  `thinkingMode` (default `auto`), accion `setThinkingMode`, envio
  del campo en `POST /ia/chat` y persistencia del modo aplicado en
  la meta del mensaje. `reset()` ya no toca el modo (es preferencia
  del usuario). Se elimina el override de modelo en el chat: el
  modelo activo es siempre el de `config.model` (se elige en
  Configuracion, no en el chat).
- `frontend/src/components/ia/AiChatPanel.tsx`:
  - Cabecera: titulo "Analisis IA" + chip con el provider activo +
    boton "Nueva conversacion" (`RotateCcw`) + boton cerrar.
  - Lista de mensajes: divisor `.ai-chat-day-divider` ("Hoy" /
    "Ayer" / "DD MMM") cuando el dia cambia; asistente en texto
    plano a la izquierda con pie `HH:mm · modelo · modo`;
    usuario en burbuja a la derecha con radio asimetrico (mas
    cerrado en la esquina inferior derecha). El bloque tecnico
    (`<details>` con origen, tokens, coste, periodo, divisa)
    queda plegado bajo cada mensaje del asistente.
  - Composer: tarjeta `.ai-chat-composer` con textarea arriba y
    footer (selector de modo de pensamiento a la izquierda, chip
    con el modelo activo + boton enviar `ArrowUp` a la derecha).
    Icono de microfono fuera del composer.
- `frontend/src/styles/layout/revision-ai.css` se reescribe para
  reflejar las clases nuevas (`.ai-chat-message--user`,
  `.ai-chat-message--assistant`, `.ai-chat-message-group`,
  `.ai-chat-day-divider`, `.ai-chat-composer`,
  `.ai-chat-composer-footer`, `.ai-chat-composer-trigger`,
  `.ai-chat-composer-send`, `.ai-chat-composer-model`) y limpia
  las clases del antiguo `ai-chat-toolbar` con `AppSelect` de
  modelo. Movil <768 px oculta el chip de modelo del composer.

### Backend

- `AtlasBalance.API/Constants/AiConfiguration.cs` declara los modos
  admitidos por provider (`ThinkingModesOpenAi`,
  `ThinkingModesMiniMax`, `ThinkingModesOpenRouter`) y los helpers
  `GetThinkingModesForProvider`, `IsAllowedThinkingMode` y
  `NormalizeThinkingMode`. El mapeo por provider:
  - `OPENAI`: `auto | low | medium | high` -> `reasoning_effort`.
  - `MINIMAX`: `auto | on | off` -> `thinking.type`
    (`enabled | disabled`). Solo MiniMax-M3 acepta el campo;
    MiniMax-M2.7 lo ignora o devuelve 400, asi que el servicio lo
    omite en ese modelo.
  - `OPENROUTER`: `auto` por defecto. Modelos concretos aceptan
    `reasoning.effort`; el `openrouter/auto` lo ignora
    silenciosamente.
- `AtlasBalance.API/DTOs/IaDtos.cs`: `IaChatRequest.ThinkingMode`
  (request), `IaConfigResponse.ThinkingModes` (response), y
  `IaChatResponse.ThinkingModeAplicado` (response).
- `AtlasBalance.API/Services/AtlasAiService.cs`:
  - `AskAsync` acepta `requestedThinkingMode`. Valores no admitidos
    por el provider (p.ej. `low` para MiniMax) se rechazan con
    auditoria `rejected_thinking_mode` y degradan a `auto`.
  - `BuildProviderRequest` envia el parametro al proveedor
    siguiendo el mapeo arriba. Para `MINIMAX` sin `thinking`
    explicito se mantiene el default anterior
    (`thinking.type = disabled`).
  - `BuildConfigResponse` publica `IaConfigResponse.ThinkingModes`
    a partir de `AiConfiguration.GetThinkingModesForProvider`.
- `AtlasBalance.API/Controllers/IaController.cs` y
  `ConfiguracionController.cs` propagan el campo en el request y
  en la respuesta.
- `AtlasBalance.API/Services/IaPlanner/IaAuditSchema.cs` anade
  `requested_thinking_mode` al set de campos permitidos en
  `IaConsultaBloqueada`, y `LogBlockedAsync` propaga las
  propiedades del objeto `extra` al payload de auditoria.

### Tests

Nuevo archivo `backend/tests/AtlasBalance.API.Tests/AtlasAiServiceThinkingModeTests.cs`
con 11 tests:

- MiniMax `on` -> payload con `thinking.type=enabled`.
- MiniMax `off` -> payload con `thinking.type=disabled`.
- MiniMax `high` invalido -> degrada a `auto` + auditoria
  `rejected_thinking_mode` con `requested_thinking_mode=high`.
- OpenAI `high` / `low` -> payload con `reasoning_effort`.
- OpenAI `on` invalido -> degrada a `auto`, sin
  `reasoning_effort`.
- OpenRouter `high` -> payload con `reasoning.effort=high`.
- OpenRouter `auto` -> payload con `reasoning.exclude=true`.
- OpenRouter `on` invalido -> degrada a `auto` con
  `reasoning.exclude=true`.
- Tres tests unitarios sobre
  `AiConfiguration.GetThinkingModesForProvider` /
  `IsAllowedThinkingMode` / `NormalizeThinkingMode`.

### Verificacion

- `npx.cmd tsc --noEmit` (frontend): **OK** (0 errores).
- `npm.cmd run lint` (frontend): **OK** (0 warnings,
  `--max-warnings 0`).
- `npm.cmd run test:unit` (frontend): **57/57 PASS**.
- `dotnet build` (`AtlasBalance.API`, `AtlasBalance.Watchdog`,
  `AtlasBalance.API.Tests` en `Release` con
  `-p:UseAppHost=false -p:GenerateDepsFile=false` por el bloqueo
  de `bin/Release/` y `bin/Debug/` documentado en AGENTS.md
  seccion 8): **OK** (0 errores; warnings preexistentes no
  introducidos por esta fase).
- `dotnet test` queda **BLOQUEADO** en este host por el sandbox
  de permisos sobre
  `bin\Debug\net8.0\TestResults\*.log`. La bateria completa
  (incluyendo los 11 tests nuevos) queda pendiente de CI o de
  un host sin bloqueo. El codigo compila limpio.
- `npm.cmd run build` (Vite) sigue **BLOQUEADO** por `EPERM` al
  copiar `public/fonts/*.ttf` a `dist/fonts/`. Mismo bloqueo
  conocido.

### Pendientes

- Bateria xUnit completa en host sin bloqueo de `bin/Debug/`.
- Build de Vite en host con acceso a `dist/fonts/`.
- Smoke visual en `/ia` y en el flotante con los tres
  providers (OPENAI, OpenRouter, MiniMax) y en ambos temas
  (claro/oscuro).
