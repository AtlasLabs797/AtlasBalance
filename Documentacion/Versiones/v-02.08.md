# V-02.08

## Estado

**En preparacion.** Version abierta el 2026-08-04 desde `V-02.07`. La rama
`V-02.08` ya esta publicada en GitHub y se ha hecho push de todos los
bloques del plan.

## Inicio de version

V-02.08 inicia el nuevo ciclo de trabajo de Atlas Balance. Esta apertura no
incluye cambios funcionales ni migraciones: sincroniza la version visible de
la aplicacion y sus superficies de documentacion para que el siguiente trabajo
se registre bajo una unica version.

## Superficies sincronizadas

- Runtime backend: `Atlas Balance/Directory.Build.props` -> `2.8.0` / `V-02.08`.
- Runtime frontend: `Atlas Balance/frontend/package.json` y
  `package-lock.json` -> `2.8.0` / `V-02.08`.
- Trazabilidad de paquete: `Atlas Balance/VERSION` -> `V-02.08`.
- Scripts de release e instalacion y workflow de release -> `V-02.08`.
- Documentacion vigente: `version_actual.md`, README, instalacion,
  documentacion tecnica, documentacion de usuario y bitacora.

Las referencias a V-02.07 y versiones anteriores que describen cambios
historicos se conservan deliberadamente para no perder trazabilidad.

## Correccion del reset de administrador

- `Reset-AdminPassword.ps1` ya no intenta cargar la DLL .NET 8 de BCrypt desde
  Windows PowerShell 5.1.
- El hash bcrypt de coste 12 se genera con `pgcrypto`, extension ya instalada
  por las migraciones de Atlas Balance.
- La password temporal se envia a `psql` por stdin y no forma parte de los
  argumentos del proceso.

## Correccion del login auditado bajo RLS

- `AuditService` evita `RETURNING` solo al insertar auditorias desde el flujo
  auth anonimo. PostgreSQL sigue asignando la secuencia y aplicando la policy
  INSERT, RLS forzado y el trigger append-only.
- No se concede lectura de `AUDITORIAS` al contexto auth. El fallo era que
  PostgreSQL exige policy SELECT para las columnas de `RETURNING`, no que la
  password o el secreto RLS fueran incorrectos.
- Se anade una regresion con rol runtime/PostgreSQL y un despliegue con backup y
  rollback. El hotfix quedo validado en la instalacion V-02.07 real: login HTTP
  200 y transicion al desafio MFA.
- Se corrige tambien el segundo `INSERT ... RETURNING` generado por
  `AuditSaveChangesInterceptor` al persistir usuario y refresh token despues de
  verificar MFA. Las rutas auth anonimas conservan sus eventos explicitos y no
  generan el diff automatico duplicado; RLS no se relaja.

## Smoke test post-instalacion

`scripts/Smoke-Test-AtlasBalance.ps1` automatiza la validacion end-to-end que
el informe del incidente V-02.07 dejo pendiente tras los dos hotfix:
ejecuta login + verificacion MFA, confirma que las cookies de sesion se
emiten y que `/api/auth/me` responde 200, y consulta `AUDITORIAS` por la
ventana del smoke para asegurar que `LOGIN_MFA_REQUIRED`, `MFA_VERIFIED` y
`LOGIN` quedaron firmados y persistidos.

Parametros:

- `-ApiBaseUrl` (URL base HTTPS, normalmente `https://<host>:8443`).
- `-AdminEmail` / `-AdminPassword` (cuenta seed del instalador).
- `-PostgresConnectionString` (cadena Npgsql del rol owner, con BYPASSRLS).
- `-PostgresBinPath` (ruta a `psql.exe`; opcional si esta en PATH).
- `-SkipCertificateCheck` (acepta el cert self-signed de primera instalacion).

Los helpers TOTP se extrajeron a `scripts/Mfa-Totp.ps1` con cobertura
independiente (`scripts/Mfa-Totp.Tests.ps1`) que valida los vectores
RFC 6238 contra `GEZDGNBVGY3TQOJQGEZDGNBVGY3TQOJQ`: T=59s -> `287082`,
T=1111111109s -> `081804` (truncados a 6 digitos, los del TotpService.cs).

## Tres niveles de health check

El incidente V-02.07 demostro que `/api/health` (literal `{status:"healthy"}`)
devolvia 200 con login 500. V-02.08 introduce dos endpoints anonimos para
que instalador y actualizador sepan cuando el sistema esta realmente funcional:

- `GET /api/health` (liveness): 200 si el proceso responde. Sin cambios.
- `GET /api/health/ready` (readiness): ejecuta `AppHealthService.ComprobarAsync`
  (BD, disco, pool). 200 si todo SANO, 503 si NO_SANO.
- `GET /api/health/functional` (functional readiness): ejecuta
  `atlas_security.context_is_valid()` y un `INSERT ... ROLLBACK` firmado
  en `AUDITORIAS`. Devuelve 200 si pasa; 503 si falla. Es el endpoint que
  `Actualizar-AtlasBalance.ps1` y `Deploy-RlsHotfix.ps1` golpean antes de
  declarar OK: si el RLS no esta alineado o el INSERT da 42501, devuelven
  503 y hacen rollback.

El rate limiter global exime cualquier ruta bajo `/api/health/*` (antes solo
`/api/health` exacto estaba exento).

## Jerarquia de permisos Pais > Titular > Cuenta

La matriz de permisos del modal de Usuarios se reforzaba para que la jerarquia
`Pais > Titular > Cuenta` fuera explicita y consistente en frontend y backend.

**Backend** (`Atlas Balance/backend/src/AtlasBalance.API/Controllers/UsuariosController.cs`):

- `ValidatePermisosAsync` rechaza filas con `cuenta_id` y sin `titular_id` ni
  `pais_id` (400 con mensaje claro). Antes pasaba silenciosamente y dejaba el
  alcance sin dimensiones amplias.
- La validacion titular-cuenta y pais-cuenta sale del bloque anidado: ahora se
  ejecutan de forma independiente. Una fila con `cuenta_id + titular_id` sin
  `pais_id` tambien valida que la cuenta pertenece al titular.
- `GuardarPermisos` detecta redundancias antes de persistir: dos filas con los
  mismos flags y una que cubre a la otra en todas las dimensiones devuelven
  `409 Conflict` con un body `{ error, redundantes: [{ scope, cubierta_por }] }`
  para que el admin decida. Politica explicita en
  `DetectPermisosRedundantes` y `ScopeCubiertoPor`: cada dimension cubre si es
  `null` o igual; mismo flagset es obligatorio. Cobertura estricta (no exacta),
  por lo que tambien detecta duplicados literales.

**Frontend** (`Atlas Balance/frontend/src/components/usuarios/UsuarioModal.tsx`):

- Helpers puros extraidos a `src/utils/permisosFormUtils.ts`
  (`computeCoherence`, `computeTitularesParaPermiso`,
  `computeCuentasParaPermiso`, `computeAlcance`,
  `corregirTitularYPaisDesdeCuenta`, placeholders dinamicos) cubiertos por 21
  tests en `tests/permisosFormUtils.test.ts`.
- Coherencia Pais/Titular/Cuenta en vivo: tres estados (`coherent`,
  `partial`, `dangling`). Cuando es `partial`, aparece una barra de aviso con
  tres botones: "Mantener solo la cuenta", "Mantener pais y titular" y
  "Corregir a la realidad de la cuenta". Si es `dangling`, un aviso impide
  guardar.
- Dropdown Titular: placeholder dinamico. Si no hay pais: "Todos los
  titulares". Si hay pais: "Todos los titulares del pais" (antes era
  "Global o por cuenta", confuso).
- Preview de alcance en cada fila: "Afecta a N cuenta(s)" o "Sin cuentas en
  este alcance" calculado sobre el catalogo en memoria. Sin requests
  adicionales.
- `parseRedundanciasDedup` extrae el 409 del backend y muestra las
  redundancias como texto entendible antes de cerrar el modal.

**Tests**:

- `backend/tests/AtlasBalance.API.Tests/UserAccessScopeMatrixTests.cs`:
  matriz 3x3 de las dimensiones para `ApplyCuentaScope` y `GetScopeAsync`.
  Titular con cuentas en dos paises ejercita el caso real por el que el admin
  pedia el refuerzo.
- `backend/tests/AtlasBalance.API.Tests/UsuariosControllerValidatePermisosTests.cs`:
  cobertura de `cuenta sin titular/país`, cuenta con titular que no la posee,
  cuenta con pais que no la posee, y combinaciones validas (cuenta + titular +
  pais, cuenta + titular sin pais, cuenta + pais sin titular).
- `backend/tests/AtlasBalance.API.Tests/UsuariosControllerDeduplicacionTests.cs`:
  409 cuando un global cubre un permiso por pais, cuando `pais+titular`
  cubre `pais+titular+cuenta`, y casos donde NO se dedup (flags distintos,
  dimensiones disjuntas).
- `frontend/tests/permisosFormUtils.test.ts`: 21 tests para los helpers
  extraidos.

## CI con Testcontainers como gate obligatorio

`[Trait("Category","Postgres")]` en las cinco clases que requieren
Testcontainers. `ci.yml` y `release.yml` separan el test en dos steps:

- `dotnet test --filter-trait "Category!=Postgres"` -> gate rapido.
- `dotnet test --filter-trait "Category=Postgres"` -> gate PostgreSQL real.

Ambos jobs deben pasar para que la release avance. Sin este gate, la
regresion de `INSERT ... RETURNING` que mordio al cliente V-02.07 volveria a
pasar en silencio.

Tests anadidos en `AuditoriaAppendOnlyPostgresTests`:

- `Auth_Audit_Should_Insert_Without_Requiring_Select_Policy`: inserta una
  auditoria bajo rol runtime con contexto anonimo firmado y verifica que
  el INSERT no requiere policy SELECT.
- `Mfa_Verify_SaveChanges_Should_Not_Throw_And_Should_Keep_Audit_Trail`:
  reproduce el batch de `IssueTokensAsync` (UPDATE USUARIOS + INSERT
  REFRESH_TOKENS + INSERT AUDITORIAS explicito) bajo rol runtime y policy
  RLS estricta.

## Endurecimiento del instalador

Cierra los 12 huecos operativos que el informe V-02.07 senalo:

- `Test-AtlasPreflight` (nuevo en `Instalar-AtlasBalance.ps1`): carpeta
  escribible, espacio libre >= 2 GB, puertos no ocupados por otra
  aplicacion, binarios no bloqueados, .tmp huerfanos. ABORTA antes de
  escribir si cualquier condicion falla.
- `Test-PostgresPreflight` (nuevo): version >= 16, extension `pgcrypto`,
  atributos del rol owner (NOSUPERUSER + BYPASSRLS), smoke-test del
  contexto RLS firmado. ABORTA antes de migrar.
- `Test-AtlasSmtp.ps1` (nuevo): distingue "SMTP no configurado" (no
  bloqueante) de "endpoint no responde" (error).
- `Test-VolumeEncryption`: distingue "BitLocker no instalado" (sin cmdlet)
  de "volumen sin cifrar".
- `New-AtlasCertificate`: post-check de SAN contra los DNS solicitados.
- `INSTALL_CREDENTIALS_ONCE.txt`: vuelve a escribirse con `Write-SecretFile`
  (ACL Administrators+SYSTEM, herencia desactivada) y la tarea
  `AtlasBalance.DeleteInstallCredentialsOnce` a 24h.
- `Sync-DirectoryPreserveConfig`: copia atomica con staging + rollback
  automatico. Cualquier fallo a mitad restaura el backup del delta.
- `Build-Release.ps1`, `Actualizar-AtlasBalance.ps1` y
  `Instalar-AtlasBalance.ps1` anaden al paquete los scripts de soporte:
  `Repair-RlsContext.ps1`, `Deploy-RlsHotfix.ps1`,
  `Grant-OwnerBypassRls.ps1`, `Test-BackupRestore.ps1`,
  `Test-AtlasSecrets.ps1`, `Test-AtlasSmtp.ps1`,
  `Smoke-Test-AtlasBalance.ps1`, `Mfa-Totp.ps1`, `Mfa-Totp.Tests.ps1`.
- `Actualizar-AtlasBalance.ps1` y `Deploy-RlsHotfix.ps1` golpean
  `/api/health/functional` antes de declarar OK. Si falla, hacen rollback
  del DLL o de la actualizacion.

## Verificacion local

- `Check-VersionAlignment.ps1 -ExpectedVersion V-02.08`: **OK**, `V-02.08`
  / `2.8.0` alineado.
- `git diff --check`: **OK**.
- Parser PowerShell de los scripts modificados: **OK**.
- `npm.cmd exec tsc --noEmit`: **OK**.
- `npm.cmd run lint -- --max-warnings 0`: **OK**.
- `npm.cmd run test:unit`: **46/46 PASS** (22 previos + 21 nuevos de
  `permisosFormUtils.test.ts` + 3 de variantes deprecadas deprecadas
  actualizadas).
- `npm.cmd run build`: **BLOQUEADO** por `EPERM` de Vite/Rolldown al
  copiar `public/fonts/*.ttf` a `dist/fonts/`. Mismo bloqueo conocido
  del sandbox documentado en AGENTS.md seccion 8. La compilacion
  `tsc --noEmit` y el lint pasan en limpio. La build de Vite se hara en
  host con permisos sobre `dist/`.
- `dotnet test` con Postgres no disponible: el gate `Category!=Postgres`
  pasa 660/679 tests en este host. Los 19 fallos restantes son los
  tests `Category=Postgres` que dependen de Docker (no disponible local).
- `Test-AtlasSecrets.Tests.ps1`: **OK**. El escaneo general encuentra ademas
  el fixture positivo intencionado `scripts/.scanner-fixtures/positive.txt`;
  no es un secreto runtime, pero impide declarar el scanner general limpio sin
  cambiar su alcance.
- Rama publicada: `origin/V-02.08`, commits `5be8c46` a `81acf96`.
- `Mfa-Totp.ps1` / `Mfa-Totp.Tests.ps1`: **OK**. Vectores RFC 6238 contra
  el secreto de prueba y contra caracteres no base32 + padding.
- `dotnet build Release` con `BaseIntermediateOutputPath` redirigido: **0
  errores, 7 warnings preexistentes** (deprecaciones V-02.04 de
  `UseXminAsConcurrencyToken` y `PostgreSqlStorage`).
- `Mfa_Verify_SaveChanges_Should_Not_Throw_And_Should_Keep_Audit_Trail`:
  test de regresion dentro de `AuditoriaAppendOnlyPostgresTests`. El test
  compila en este host; queda bloqueado para corrida local por ausencia
  de Docker/Testcontainers. El run se hara en `ci.yml` (GitHub Actions
  `ubuntu-24.04` con daemon).

## Pendientes de release

- Ejecutar los gates completos de CI, incluyendo PostgreSQL/Testcontainers y
  el scanner general sin el falso positivo del fixture.
- Generar, inspeccionar y firmar un paquete `V-02.08-win-x64` independiente.
- Validar el paquete y el entorno Windows real antes de declarar la release.
- Validacion end-to-end del hotfix MFA en cliente real con `Smoke-Test.ps1`
  contra un challenge y un TOTP nuevos.
- Confirmar copia atomica con rollback bajo fallo real (no solo el caso
  feliz del parser). Hoy esta implementado y parsea, pero el ultimo
  smoke contra una primera instalacion completa queda en manos del
  operador.

## 2026-08-17: CI reparado + regresion descubierta en AtlasAiService (80 tests)

Informe de handoff para continuar en otra sesion. Contexto: el bloqueo de
`dotnet test` en host local (ACL sobre `bin/`, ver seccion 8 de
`AGENTS.md`) significaba que **la bateria completa de tests nunca se habia
corrido de verdad**, ni en local ni en CI, desde hace varias fases de
trabajo de IA (que llegaron a esta rama via commits `V-02.09: IA ...`
mezclados en `V-02.08` antes del cierre de version). CI en GitHub Actions
tambien estaba roto (bug distinto, ver abajo), asi que el gate llevaba
tiempo sin proteger nada. Al arreglar CI, salio a la luz una regresion
real preexistente.

### Parte 1 (RESUELTA): CI no ejecutaba ningun test

**Sintoma:** el job "Test backend (sin Postgres)" fallaba en GitHub Actions
con `Total: 0` tests ejecutados, en ~120-400ms (demasiado rapido para ser
una ejecucion real).

**Causa raiz:** `.github/workflows/ci.yml` y `release.yml` invocaban
`dotnet test ... -- --filter-trait "Category!=Postgres"`. El proyecto de
tests corre sobre **xUnit v3 + Microsoft Testing Platform** (ver comentario
en `AtlasBalance.API.Tests.csproj`), que tiene **dos CLIs distintas y
NO intercambiables**:

- Invocando el `.exe` de tests directamente: sintaxis antigua de un solo
  guion (`-trait "name=value"`, `-trait- "name=value"`).
- Invocando via `dotnet test -- <args>` (el bridge
  `TestingPlatformDotnetTestSupport`, que es como lo hace CI): sintaxis
  nueva de doble guion (`--filter-trait "name=value"`,
  `--filter-not-trait "name=value"`), **sin soporte para el operador
  `!=`** (solo `name=value`, siempre igualdad).

El workflow original mezclaba ambas cosas mal: doble guion (correcto para
`dotnet test`) pero con `!=` (invalido en ese parser), lo que hacia que el
runner abortara en el parseo de argumentos antes de descubrir ningun test.

**Iteracion del fix (dejar constancia para no repetir el error):**
1. Primer intento: cambiar a `-trait`/`-trait-` (commit `c7b08f9`).
   **Incorrecto** — es la sintaxis del exe standalone, no la de
   `dotnet test --`. Fallaba en CI real con
   `Unknown option '--trait-'`.
2. Fix correcto (commit `6e40176`): `--filter-not-trait "Category=Postgres"`
   (excluye Postgres) y `--filter-trait "Category=Postgres"` (solo
   Postgres). Verificado reproduciendo el job de CI en un contenedor
   `mcr.microsoft.com/dotnet/sdk:8.0` (Ubuntu, igual que el runner de
   GitHub Actions) — con el flag corregido la suite discovers y corre los
   842 tests reales en vez de abortar.

**Como reproducir el entorno de CI en local** (util si vuelve a bloquearse
el host por ACL en `bin/`/`obj/`, ver seccion 8 de `AGENTS.md`):

```bash
docker volume create atlasbalance_nuget   # cache de paquetes, opcional
docker volume create atlasbalance_work    # copia de trabajo del repo

docker run --rm \
  -v "//c/Proyectos/Atlas Balance Dev:/repo:ro" \
  -v atlasbalance_nuget:/root/.nuget/packages \
  -v atlasbalance_work:/work \
  -w /work mcr.microsoft.com/dotnet/sdk:8.0 bash -c '
    cp -r /repo/. /work/
    dotnet restore "./Atlas Balance/backend/src/AtlasBalance.API/AtlasBalance.API.csproj" -r linux-x64
    dotnet restore "./Atlas Balance/backend/src/AtlasBalance.Watchdog/AtlasBalance.Watchdog.csproj" -r linux-x64
    dotnet restore "./Atlas Balance/backend/tests/AtlasBalance.API.Tests/AtlasBalance.API.Tests.csproj"
    dotnet test "./Atlas Balance/backend/tests/AtlasBalance.API.Tests/AtlasBalance.API.Tests.csproj" \
      -c Release --no-restore -- --filter-not-trait "Category=Postgres"
  '
```

(En Git Bash en Windows, anteponer `MSYS_NO_PATHCONV=1` para que Docker no
reescriba la ruta `//c/...`. El log xUnit completo queda en
`.../bin/Release/net8.0/TestResults/AtlasBalance.API.Tests_net8.0_x64.log`
en UTF-16LE; decodificar con
`iconv -f UTF-16LE -t UTF-8 archivo.log`.)

Esto evita depender del host local (bloqueado por los servicios Windows
`AtlasBalance.API`/`AtlasBalance.Watchdog` corriendo, que tienen los `.exe`
de `bin/Release/` con lock) y reproduce exactamente lo que corre GitHub
Actions.

### Parte 2 (SIN RESOLVER): regresion en AtlasAiService — 80 tests fallando

Con el flag de CI ya corregido, la suite corre de verdad y reporta
`Failed: 80, Passed: 762, Total: 842`. Confirmado identico en el
contenedor local y en el run real de GitHub Actions (run id
`31989471616`).

**Los 80 fallos se concentran en exactamente 3 clases de test**, todas
sobre `AtlasAiService.AskAsync`:

- `AtlasAiServiceTests`
- `AtlasAiServiceStabilizationTests`
- `AtlasAiServiceThinkingModeTests`

**Mecanismo (confirmado leyendo el codigo, no es especulacion):**

`AtlasAiService.AskAsync` (`Atlas Balance/backend/src/AtlasBalance.API/Services/AtlasAiService.cs:249`)
llama primero a `TryAnswerPlannedAsync` (linea 885), que enruta la
pregunta por el nuevo planificador de intenciones de 3 niveles
(`IntentPlanner`, del trabajo de IA "Fase 4"). Ese metodo devuelve
**no-null en todos los casos salvo un borde interno muy estrecho**, asi
que en la practica el viejo flujo de chat directo con el proveedor
(lineas 261+) ha quedado inalcanzable para casi cualquier pregunta.

Para llegar al flujo viejo, `IntentPlanner` tiene que fallar en:
1. **Nivel 1** (patron local): solo ~7 regex fijos (`"cual es mi saldo"`,
   `"ultimo gasto"`, etc. — ver `IntentPlanner.cs:238-342`).
2. **Nivel 2** (semantico): pedirle al proveedor que reduzca la pregunta a
   JSON `{operacion, metrica, filtros}`.

**En los 80 tests, el Nivel 2 nunca puede tener exito.** El constructor
de 6 argumentos de `AtlasAiService` (mantenido explicitamente "para las
pruebas unitarias existentes", `AtlasAiService.cs:45-46`) inyecta por
defecto `new IntentPlanner(new NullSemanticPlannerClient(), ...)`
(linea 61), y `NullSemanticPlannerClient.PlanToJsonAsync` **siempre
devuelve null**. Resultado: cualquier pregunta libre (p.ej.
`"Resumen de gastos"`) es rechazada al instante por el planificador,
`httpFactory` (el mock HTTP de los tests) nunca se toca, y toda
asercion sobre `Provider`, `Model`, payload enviado, etc. falla.

**No es solo un problema de mocks desactualizados.** Revisando produccion:

- El DI real (`Program.cs:393-394`) SI usa el `SemanticPlannerClient`
  real, que si llama al proveedor configurado.
- Pero incluso ahi, el rol de la IA cambio de "componer la respuesta
  final" a "clasificar la pregunta en 1 de 9 operaciones rigidas como
  JSON". Cualquier pregunta que no se reduzca a ese esquema recibe el
  mismo mensaje generico de rechazo ("El proveedor no devolvio un plan
  valido.") **en vez de una respuesta real**.
- Hay un guard explicito que bloquea la caida al flujo viejo
  (`AtlasAiService.cs:922-923`): *"No convertimos un fallo del
  planificador en una llamada legacy con contexto financiero: esa
  degradacion romperia la frontera de datos."* — parece una decision de
  DLP deliberada.
- **Pero contradice el propio test suite**: uno de los 80 tests que
  fallan se llama literalmente
  `AskAsync_Should_Keep_OpenRouter_For_Uncovered_Financial_Questions`,
  que documenta el contrato contrario (preguntas no cubiertas deberian
  seguir llegando al proveedor).
- Ademas, el flujo "legacy" que el guard bloquea **no es un leak de
  datos sin proteccion**: ya aplica seudonimizacion/DLP completa
  (`DlpScrubber`, `BuildPseudonymMapAsync`, trabajo de V-02-07) antes de
  salir a un proveedor externo. Varios de los 80 tests que fallan
  verifican exactamente esa proteccion
  (`AskAsync_Should_Pseudonymize_Titular_Name_Before_Sending_To_Provider`,
  `AskAsync_Audit_Should_Not_Contain_Pii_From_Prompt`).

La combinacion (nombre de test que documenta el contrato contrario +
guard que bloquea un camino que ya tenia su propio DLP + cero tests
actualizados) apunta a una **migracion incompleta**, no a una decision de
producto revisada y deliberada. Pero eso hay que confirmarlo con quien
llevo el trabajo del planificador antes de tocar nada — podria haber
contexto de producto que no esta en el codigo.

**Opciones de solucion:**

1. **(Recomendada) Restaurar el fallback.** En `TryAnswerPlannedAsync`,
   cuando el planificador rechaza especificamente en el **paso semantico**
   (no en match local, no en aclaracion), caer al flujo de chat legacy en
   vez de devolver el mensaje enlatado. Cambio acotado (probablemente solo
   tocar el bloque `if (resolution.Origen is PlanResolutionSource.Rejected)`
   en `AtlasAiService.cs:920-931`, quiza distinguiendo rechazo-por-Nivel1
   de rechazo-por-Nivel2). Muy probablemente los 80 tests vuelven a pasar
   casi sin tocarlos, porque restaura exactamente el comportamiento que
   ya afirman.

2. **Tratarlo como intencional y jubilar los tests viejos.** Si el
   estrechamiento fue una decision de producto/seguridad deliberada
   (confirmar con el dueno del trabajo de IA), entonces los 80 tests estan
   obsoletos y hay que borrarlos/reescribirlos contra el nuevo contrato
   basado en planes. Ya existe cobertura del flujo nuevo a nivel de
   componente (`IntentPlannerTests.cs`, `PlanExecutorTests.cs`,
   `FinancialToolsServiceTests.cs`); faltaria cobertura de integracion a
   nivel de `AtlasAiService` (que `Provider="LOCAL"`, `Model="planificador"`
   / `"planificador-semantico"`, y sobre todo, donde queda la proteccion
   PII/DLP en el nuevo flujo si las respuestas de plan se generan
   localmente sin pasar por el scrubber).

### Como retomar en otra sesion

1. Confirmar con quien impulso el trabajo del planificador (IntentPlanner)
   si el guard de `AtlasAiService.cs:920-931` es una decision de producto
   deliberada o un efecto colateral no revisado de la migracion.
2. Si es efecto colateral -> aplicar Opcion 1 arriba, correr la suite con
   el comando Docker de la Parte 1, confirmar que los 80 tests vuelven a
   pasar (o ajustar los que dependan de detalles finos del mensaje/flujo).
3. Si es deliberado -> aplicar Opcion 2: escribir tests de integracion
   nuevos para `AtlasAiService` sobre el flujo de planificador, decidir
   donde vive la proteccion DLP para respuestas basadas en plan, y borrar
   o reescribir los tests de las 3 clases afectadas.
4. En cualquier caso, una vez resuelto, correr la suite completa (no solo
   los 3 archivos afectados) via el contenedor Docker de la Parte 1 antes
   de cerrar, porque el host local sigue bloqueado (ver "Pendientes de
   release" arriba) y es facil dar por bueno un fix que solo compila pero
   no se ejecuto.

**Commits de la sesion anterior:** `c7b08f9` (fix incorrecto, ver Parte 1),
`6e40176` (fix correcto de CI). Ambos ya en `origin/V-02.08`.

### Parte 2 (RESUELTA): restaurar fallback al flujo legacy

**Diagnostico confirmado:** el guard en `TryAnswerPlannedAsync`
(AtlasAiService.cs:920-931) era una migracion incompleta, no una decision
de producto. El comentario sobre "frontera de datos" era incorrecto: el
flujo legacy ya aplica DLP completo (`DlpScrubber` + `BuildPseudonymMapAsync`)
antes de enviar datos al proveedor. El test
`AskAsync_Should_Keep_OpenRouter_For_Uncovered_Financial_Questions` documenta
el contrato correcto.

**Fix aplicado (Opcion 1 del informe):**

1. `AtlasAiService.cs`: el bloque `if (resolution.Origen is
   PlanResolutionSource.Rejected)` ahora devuelve `null` en vez de una
   respuesta local enlatada. Las preguntas que el planificador no puede
   clasificar caen al camino deterministico o al proveedor con DLP.
2. `IaAcceptanceTests.cs`: 6 tests de aceptacion anadieron
   `catch (IaProviderException) { }` para cubrir el caso donde la pregunta
   llega al proveedor sin red (sandbox). Consistente con los catches
   existentes de `IaConfigurationException`.

**Pendiente:** correr la suite completa en Docker para confirmar 0 fallos.

## Devolucion automatica de comisiones (2026-08-23)

Nueva columna `Devolucion` en `Revision > Comisiones` con emparejamiento
automatico entre comisiones (cargos) y sus bonificaciones.

### Reglas de emparejamiento

- La lista de comisiones solo muestra cargos (`monto < -umbral`); los
  positivos dejan de listarse porque son devoluciones.
- Candidato de abono por comision: misma cuenta, importe exacto opuesto,
  fecha >= fecha de la comision, concepto tipo comision/bonificacion
  (`ComisionSearchTerms`), no descartado previamente como comision y sin
  emparejar todavia con otra revision activa.
- Un abono se asigna a una sola comision: siempre la mas antigua que encaje
  (orden `fecha ASC, fila_numero ASC`). Se aplica tanto a la sugerencia del
  listado como al endpoint de verificar.
- Al desmarcar una linea (PENDIENTE o DESCARTADA) se limpia
  `extracto_devolucion_id` y el abono vuelve a quedar libre.
- Los seguros no cambian.

### Backend

- Migracion `20260823120000_AddExtractoDevolucionToRevisionEstados`: columna
  `extracto_devolucion_id uuid NULL` en `REVISION_EXTRACTO_ESTADOS`, FK a
  `EXTRACTOS` ON DELETE RESTRICT e indice unico parcial
  `(extracto_devolucion_id) WHERE deleted_at IS NULL AND extracto_devolucion_id IS NOT NULL`
  (garantia anti-carrera contra doble asignacion). Snapshot actualizado; el
  drift preexistente de `deleted_at` en ese bloque se conserva tal cual.
- `RevisionService`: `GetComisionesAsync` lista solo negativos y resuelve
  sugerencias por lotes (sin N+1); `SetEstadoAsync` limpia la referencia al
  salir de DEVUELTA; nuevo `VerificarDevolucionAsync` revalida elegibilidad y
  antiguedad dentro del mismo guardado.
- Endpoint nuevo: `POST /api/revision/comision/{id}/verificar-devolucion`.
  Sin candidata o con abono reclamable por una comision mas antigua ->
  409 con mensaje; permisos via `CanReviewCuentaAsync`; `DbUpdateException`
  del indice unico -> 409.
- DTOs: `RevisionComisionItemResponse` += `DevolucionExtractoId`,
  `DevolucionFecha`; nuevo `VerificarDevolucionResponse`.

### Frontend

- `RevisionPage.tsx`: columna `Devolucion` (fecha formateada o em-dash),
  boton verde de verificar con icono Check cuando hay candidata y permiso,
  update optimista + invalidacion `['revision']`, resincroniza la lista si el
  servidor responde 409/sin candidata.
- Tipos en `types/index.ts` (`devolucion_extracto_id`, `devolucion_fecha`,
  `VerificarDevolucionResponse`); estilos `.revision-verify-button` en
  `styles/layout/revision-ai.css`, responsive por data-label ya existente.

### Tests

- `RevisionServiceTests`: reescrito el test de umbral (solo negativos) y 8
  tests nuevos (sugerencia, antiguedad, persistencia, sin candidato, rechazo
  de mas reciente, positivo descartado excluido, limpieza al desmarcar,
  solo lectura denegado) + traduccion Npgsql de la query de candidatos.
- `RevisionControllerTests`: stub de `IRevisionService` actualizado.

## Fixes de la auditoria integral (2026-08-23)

Aplicacion de los fixes priorizados del informe del mismo dia (ver
`REGISTRO_BUGS.md` entrada "Auditoria integral" y
`LOG_ERRORES_INCIDENCIAS.md`). Resumen de lo tocado:

**Backend** (`AtlasBalance.API`): exencion CSRF para `/api/integration/*`
(`CsrfMiddleware`); invalidacion de cache en PUT/Rotar de tokens de
integracion (`IntegrationTokenService.InvalidateActiveTokensCache` +
`IntegracionesController`); advisory lock fail-closed relacional
(`ImportacionService`); validacion estricta de emails de usuario y alertas
(`UsuariosController`) + try/catch en `EvaluateSaldoPostAsync`
(`ExtractosController`, logger nuevo); titular activo exigido en
conciliacion lectura y escritura no-ADMIN (`ConciliacionService`).

**Frontend:** `CuentaDetailPage` separa `loadError`/`error`, banner no
destructivo con cerrar, EmptyState con Reintentar, rollback por refetch y
resync 409; guards request-id en `ExtractosPage` (3 loaders),
`RevisionPage` e `ImportacionPage.loadLotes`; `ConciliacionPage` gestiona
errores de carga y resincroniza tras 409; salida del paso MFA en
`LoginPage`; bloqueo de reapertura de lotes cerrados y guard anti doble-click
en plazo fijo (`ImportacionPage`); tope de toasts (`uiStore`); CSS nuevo en
`auth.css` y `entities.css`.

**Scripts:** here-string corrupto reparado en `Instalar-AtlasBalance.ps1`
(check BitLocker era un no-op); preflight BYPASSRLS + owner por defecto +
exit codes en `backup-manual.ps1`; try/catch global con rollback automatico,
stderr de curl neutralizado, fallback PS 5.1 del healthcheck y reintento de
copias en `Actualizar-AtlasBalance.ps1`; exit codes honestos + owner por
defecto en `restore-backup.ps1`; `-Depth` condicionado a PS6+ en
`Smoke-Test-AtlasBalance.ps1`; patron stderr/EAP corregido en helpers psql
del instalador y `Grant-OwnerBypassRls.ps1`.

**Tests:** 2 nuevos (`CsrfMiddlewareTests.InvokeAsync_Should_SkipCsrf_For_
Integration_Paths`, `UsuariosControllerTests.Crear_Should_Reject_Malformed_
Alert_Email`) + stub actualizado en `IntegrationAuthMiddlewareTests`.

**Verificacion local:** suite backend completa **853/853 PASS** ejecutada en
este host por primera vez desde 2026-08-17, compilando con
`dotnet test -p:OutputPath=<temp>` para esquivar el bloqueo ACL de bin/
(los servicios Windows siguen corriendo; Docker sigue sin daemon). Esto
confirma ademas que el fix de AtlasAiService (`31f4529`) deja la suite en
verde. Frontend: tsc OK, lint OK, test:unit 57/57. Parser PowerShell OK en
los 6 scripts modificados.

**Deliberadamente NO aplicado:** ventana de gracia en refresh concurrente:
el test `RefreshToken_Should_Revoke_Active_Sessions_When_Rotated_Token_Is_
Reused` documenta revocacion inmediata como contrato; cambiarlo requiere
decision de producto. Resto de pendientes listados en REGISTRO_BUGS.
