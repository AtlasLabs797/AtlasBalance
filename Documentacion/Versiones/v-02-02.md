# V-02-02

## Estado

Version actual activa.

## Base

- Version anterior publicada: `V-01.09`.
- Rama base local: `main`.
- Rama de trabajo actual: `V-02-02`.
- Documentacion historica inmediata: `Documentacion/Versiones/v-01.09.md`.

## Alcance

- Rama de trabajo para nuevas funciones, correcciones y preparacion de release posteriores a `V-01.09`.
- Aplicacion Atlas Balance empaquetable para Windows x64.
- Backend ASP.NET Core 8, frontend React/Vite y Watchdog.
- Paquetes de release en `Atlas Balance/Atlas Balance Release`.
- Documentacion centralizada en `Documentacion`.

## Prioridad

- Corregir bugs antes de sumar funciones vistosas.
- Mantener cada cambio trazado en documentacion tecnica, bugs o bitacora.
- No publicar release sin build limpio y verificaciones relevantes.
- No meter artefactos de release en commits normales salvo que el objetivo sea publicar paquete.

## Instrucciones para cambios

- Asociar cualquier cambio nuevo a `V-02-02`.
- Actualizar `Documentacion/DOCUMENTACION_CAMBIOS.md` en cada sesion.
- Actualizar `Documentacion/DOCUMENTACION_TECNICA.md` si cambia codigo, estructura, scripts o configuracion.
- Actualizar `Documentacion/DOCUMENTACION_USUARIO.md` si cambia instalacion, uso, configuracion o ubicacion de paquetes.
- Consultar `Documentacion/LOG_ERRORES_INCIDENCIAS.md` antes de resolver errores.
- Mantener `Documentacion/REGISTRO_BUGS.md` al dia.
- No documentar contrasenas, tokens ni datos privados.

## Cambios registrados

### 2026-06-26 - Dashboard alineado con referencia bancaria

- El dashboard principal se ajusta a la referencia aportada: saldo consolidado dominante, panel superior unico, mini tarjetas por divisa, grafica de area azul y KPIs secundarios mas compactos.
- `EvolucionChart` incorpora variante `saldoArea`, con eje Y basado solo en saldo y animacion desactivada.
- `Saldos por titular` y `Plazos fijos` quedan juntos en desktop; pais y concentracion bajan de prioridad.
- Verificacion: frontend lint OK, TypeScript OK, build Vite OK con salida temporal dentro del workspace y QA Playwright mockeada desktop/mobile sin overflow ni errores de consola.
- Incidencia: build temporal en `C:\tmp` fallo por `EPERM`; se documento y se uso salida temporal dentro del workspace.

### 2026-06-23 - Rediseño completo con nuevo sistema visual

- Se implemento el sistema visual de `Documentacion/Diseno/design.md` en tokens, componentes base, shell, login, dashboard, extractos y pantallas operativas/admin.
- El rail lateral queda oscuro de forma permanente, con marca `Atlas Balance by Atlas Labs`, navegacion agrupada, iconografia mas fina y topbar sticky con usuario en formato pill.
- La pantalla de login pasa a layout partido: panel de marca oscuro, tarjeta de acceso, toggle de tema y conservacion completa de login, MFA, QR, recordar dispositivo, retorno seguro y mensajes post-update.
- El dashboard principal reorganiza saldo consolidado, saldos por divisa y grafica de evolucion en una hero card; mantiene KPIs, plazos fijos, saldos por pais, concentracion y enlaces por titular.
- `PeriodoSelector` pasa de desplegable a control segmentado para los periodos del dashboard sin cambiar query params ni endpoints.
- Extractos conserva tabla virtualizada, edicion inline, auditoria, filtros, columnas visibles y paginacion, pero adopta header/filtros y tabla acorde al nuevo sistema.
- Importacion, revision, IA, entidades, formatos, usuarios, auditoria, exportaciones, papelera, configuracion y backups reciben la capa visual coordinada sin cambiar rutas ni contratos API.
- Se versiono el mockup aceptado en `Documentacion/Diseno/mockups/atlas-balance-redesign-v02-02.html`.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd exec tsc -- --noEmit` OK, `git diff --check` OK con avisos CRLF preexistentes, build Vite OK en `C:\tmp\atlas-balance-vite-build-redesign-v02-02`.
- Incidencia: `npm.cmd run build` estandar queda bloqueado por `EPERM` al limpiar `frontend/dist/assets`; no es fallo de compilacion del rediseño.

### 2026-06-23 - QA posterior del rediseno y cierre responsive

- Se audito la cobertura del rediseno con subagentes y se cerraron huecos en `ChangePasswordPage`, selects reutilizables, backups, sidebar, dashboard, extractos y formatos de importacion.
- `AppSelect` pasa de select nativo a popover visual del sistema manteniendo fallback `noscript`.
- `Backups` usa selects redisenados para frecuencia, dia y destino; el resumen aclara que la ultima copia correcta se calcula en la pagina actual y el OAuth de Drive limpia codigos expirados/fallidos.
- `PeriodoSelector` queda como `radiogroup` con roving tabIndex; `ExtractoTable` evita que los botones internos rompan el foco roving de la grilla.
- El sidebar muestra version `V-02-02` y reloj; `dashboard.css` define clases que ya usaba el JSX y ajusta la hero al token `surface-highlight`.
- Se corrigio overflow horizontal mobile causado por tablas `.sr-only` de charts: el contenido accesible queda clippeado y desplazado fuera del eje visible.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd exec tsc -- --noEmit` OK, `git diff --check` OK, build Vite OK en `C:\tmp\atlas-balance-vite-build-redesign-v02-02-qa3`.
- QA Playwright con Chrome local y API mock: login, cambio obligatorio de password, dashboard, tabs de periodo, extractos, backups/Google Drive y dashboard mobile OK; consola sin errores; capturas en `output/playwright/`.
- Pendiente de release: `npm.cmd run build` estandar sigue condicionado por liberar `frontend/dist/assets`.

### 2026-06-23 - Interactividad responsive y accesibilidad operativa

- Se anadio registro global de overlays bloqueantes para modales, sheets y alertdialogs, con bloqueo de scroll y cierre/ocultacion del chat IA cuando procede.
- El chat IA flotante deja de competir con modales y queda oculto en movil; en movil la IA se usa desde la ruta/menu `IA`.
- `DatePickerField` usa selector nativo en tactil/movil y calendario custom en escritorio.
- La navegacion inferior movil prioriza Dashboard si procede, Cuentas, Extractos, Importar y Mas; sin Dashboard, Extractos entra primero.
- `ExtractoTable` pasa a `role="grid"` interactivo con celda activa, atributos ARIA y navegacion por teclado.
- `Revision` se renderiza como tarjetas etiquetadas en movil y mantiene tabla densa en pantallas mayores.
- Se reforzo scroll tactil local en tablas administrativas, dashboard, importacion, revision y extractos.
- Verificacion: frontend lint OK, TypeScript OK y build OK. No se hizo QA visual con servidor/navegador por la regla anti-encallamiento.

### 2026-06-22 - Copias programables y Google Drive

- La pagina `Backups` permite configurar copias automaticas por horas, diarias, semanales o mensuales.
- El destino puede ser solo local o local mas Google Drive.
- Google Drive se vincula con OAuth device flow; el refresh token y el client secret quedan protegidos.
- Las copias subidas a Drive se cifran antes de salir del servidor con AES-GCM y clave protegida localmente.
- Se agregan `BACKUP_CLOUD_CONNECTIONS`, `BACKUP_CLOUD_COPIES`, migracion, RLS admin/system y auditoria de eventos de nube.
- Se permite listar copias creadas en Drive, importar una copia cifrada y reintentar subidas fallidas.
- Verificacion: backend build OK desde `C:\tmp`, tests focalizados 9/9 OK, frontend lint OK, TypeScript OK y build OK. No se hizo subida real a Drive por falta de credenciales OAuth/cuenta vinculada.

### 2026-06-21 - Proveedor IA MiniMax M3/M2.7

- MiniMax queda incorporado como proveedor IA propio, separado de OpenRouter y OpenAI.
- Backend admite `MINIMAX`, normaliza modelos a `MiniMax-M3` por defecto y limita la seleccion a `MiniMax-M3` y `MiniMax-M2.7`.
- La API key se guarda en `CONFIGURACION.minimax_api_key`, protegida con `ISecretProtector`, sin exponerse en respuestas ni auditoria.
- `AtlasAiService` usa el endpoint OpenAI-compatible de MiniMax (`/v1/chat/completions`) con `max_completion_tokens`; `MiniMax-M3` desactiva `thinking` y `MiniMax-M2.7` no intenta desactivarlo porque MiniMax no lo soporta en M2.x.
- Frontend de Configuracion y chat muestran MiniMax y sus dos modelos en selector cerrado.
- Verificacion: tests focalizados MiniMax 3/3 OK, frontend lint OK, TypeScript OK, build OK, `git diff --check` OK. La suite focalizada amplia IA/Configuracion sigue roja por deuda ajena y un test sensible a fecha; no se marca como verde.

### 2026-06-09 - Mockup HTML post-auditoria UI/UX

- Se creo `Documentacion/Diseno/mockups/atlas-balance-post-uiux-v02-02.html` como mockup estatico de la app tras aplicar las mejoras propuestas.
- El mockup muestra dashboard con jerarquia financiera mas fuerte, scope de pais con error explicito, navegacion sin puerta falsa, tooltip de chart propio, estados de importacion diferenciados, tabla financiera densa y mobile sin solape IA/sheet.
- No modifica la app real ni introduce dependencias nuevas.
- Verificacion: archivo creado, sin referencias externas/dependencias prohibidas y `git diff --check` OK; captura Playwright bloqueada por navegador local no instalado.

### 2026-06-09 - Auditoria UI/UX premium por skills

- Se creo `PRODUCT.md` como contexto raiz de producto para aplicar Impeccable/craft sin inventar direccion visual.
- Se genero informe por cada skill solicitada en `Documentacion/Diseno/AUDITORIA_UI_UX_SKILLS_V-02-02.md`.
- Se genero informe global y plan de mejora coordinado en `Documentacion/Diseno/AUDITORIA_UI_UX_GLOBAL_V-02-02.md`.
- Se corrigio `Documentacion/SKILLS_LOCALES.md` para reflejar que las skills de diseno actuales viven bajo `Skills/02_Design-UI-UX/...`.
- Verificacion: `git diff --check` OK con avisos CRLF preexistentes en cambios ajenos; frontend `npm.cmd run lint` OK.

### 2026-06-09 - Datos demo de desarrollo

- `SeedData` carga datos sinteticos en entorno `Development` para revisar la app con contenido realista.
- Incluye paises, titulares, cuentas multi-divisa, extractos, plazo fijo, alertas, permiso global admin y auditoria demo.
- La carga es idempotente y no corre en `Production`, aunque `DemoData:Enabled=true`.
- `appsettings.Development.json.template` deja `DemoData.Enabled=true` para instalaciones locales nuevas.
- Verificacion: `SeedDataTests` 8/8 OK usando el workaround desde `C:\tmp` por el SDK fijado en `global.json`.

### 2026-06-09 - Autorizacion por pais en permisos/RLS

- `pais_id` pasa a ser dimension real de autorizacion en permisos de usuario e integraciones.
- Backend, RLS y frontend aplican scopes por interseccion de `pais_id`, `titular_id` y `cuenta_id`.
- RLS/FORCE RLS se activa en `PERMISOS_USUARIO` e `INTEGRATION_PERMISSIONS`.
- Preferencias de columnas incorporan pais/titular para no mezclar restricciones entre scopes.
- Extractos resuelve columnas editables solo desde permisos que conceden edicion; una preferencia visual no puede abrir columnas editables.
- `RowLevelSecurityTests` incorpora escenarios usuario/integracion por `pais_id + titular_id` y checks FORCE RLS para tablas de paises, MFA y permisos.
- Exportacion manual, dashboard, importacion, extractos, alertas e integraciones quedan alineados con el nuevo modelo.
- Verificacion: backend build OK, tests focalizados no Docker 32/32 OK, frontend lint/build OK.
- Pendiente de release: validar `RowLevelSecurityTests` con PostgreSQL real/Testcontainers; Docker no esta disponible en esta maquina.

### 2026-06-09 - App shell nativo y scope global por pais

- Shell recreado con React/Zustand/lucide/CSS propio. No se agrego shadcn, Tailwind ni `@efferd/app-shell-7`.
- Nuevo selector global `Organizacion`: `General` incluye todo; un pais activo filtra cuentas, saldos, movimientos, titulares, importacion, revision, exportaciones, alertas activas, auditoria e IA.
- Store frontend de pais con persistencia local y validacion contra paises activos.
- Endpoints ampliados con `paisId`/`pais_id` para titulares, extractos, importacion, revision, exportaciones, alertas activas, auditoria y chat IA.
- Tests focalizados del cambio: 161/161 OK. Frontend lint, TypeScript y build OK. Backend build OK usando SDK `8.0.421` desde `C:\tmp`.
- Suite backend no Docker completa: 288/290 OK; quedan fallos ajenos en `ConfiguracionControllerTests`.
- Evidencia visual en `output/playwright/`: desktop expandido, sidebar colapsado, tablet y movil.

### 2026-06-08 - Actualizador, MFA recordado, OpenRouter y paises

- Actualizador GitHub: `GET /api/sistema/version-disponible` informa `instalable`, `bloqueos`, ZIP detectado, firma, digest, clave publica y disponibilidad real de Watchdog. `Actualizar ahora` queda bloqueado si hay update pero no es instalable.
- Seguridad de update: se mantiene el repositorio oficial `AtlasLabs797/AtlasBalance`; no se aceptan URLs arbitrarias para instalar paquetes.
- MFA recordado: nueva tabla `MFA_TRUSTED_DEVICES` con token opaco hasheado, `security_stamp`, expiracion, revocacion, user-agent/IP resumidos y expiracion de 90 dias. Logout ya no borra `mfa_trusted`; revocar MFA, cambiar password o rotar `security_stamp` invalida dispositivos.
- IA/OpenRouter: OpenRouter acepta cualquier model id sintacticamente valido, incluido `openrouter/auto`, y envia el modelo exacto al proveedor. Nuevo `GET /api/ia/modelos` consulta `/api/v1/models` con cache corta.
- Render IA: el frontend renderiza texto/Markdown seguro con listas, tablas, bloques de codigo y JSON simple sin HTML crudo.
- Paises: nueva tabla `PAISES`, CRUD `/api/paises`, `CUENTAS.pais_id` nullable, filtros por pais en cuentas/dashboard y agregado `saldos_por_pais`.
- Frontend: selectores de pais en cuentas y dashboard, entrada libre con sugerencias para modelos OpenRouter, preflight visible de actualizacion y boton de update condicionado a `instalable`.
- Tests agregados/actualizados para preflight de updater, MFA recordado/revocado/90 dias, modelos OpenRouter arbitrarios y dashboard por pais.
- Verificacion: frontend `npm run build` OK; backend build OK con SDK `8.0.419`; tests focalizados backend 122/122 OK; `git diff --check` OK con avisos CRLF.

### 2026-06-08 - Apertura de version V-02-02

- `version_actual.md` pasa a declarar `V-02-02` como version activa.
- Fuentes runtime actualizadas a `V-02-02` / `2.2.0`.
- `V-01.09` queda documentada como base anterior.
- Rama de trabajo creada: `V-02-02`.
- Verificacion: revision estatica de referencias activas y estado Git.
