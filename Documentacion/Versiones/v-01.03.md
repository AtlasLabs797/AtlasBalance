# V-01.03

## Estado

Version publicada y cerrada. Base anterior de `V-01.04`.

## Base

- Version anterior publicada: `V-01.02`.
- Rama base local: `V-01.02`.
- Rama publicada: `V-01.03`.
- Siguiente rama activa: `V-01.04`.

## Alcance

- Rama de trabajo para nuevas funciones y correccion de errores posteriores a `V-01.02`.
- Aplicacion Atlas Balance empaquetable para Windows x64.
- Backend ASP.NET Core 8, frontend React/Vite y Watchdog.
- Paquetes de release en `Atlas Balance/Atlas Balance Release`.
- Documentacion centralizada en `Documentacion`.

## Prioridad

- Corregir bugs antes de sumar funciones vistosas.
- Mantener cada cambio trazado en tickets, bugs o documentacion tecnica.
- No publicar release sin build limpio y verificaciones relevantes.
- No meter artefactos de release en commits normales salvo que el objetivo sea publicar paquete.

## Instrucciones para cambios

- No asociar cambios nuevos a `V-01.03`; cualquier trabajo posterior pertenece a `V-01.04` o superior.
- Actualizar `Documentacion/DOCUMENTACION_CAMBIOS.md` en cada sesion.
- Actualizar `Documentacion/DOCUMENTACION_TECNICA.md` si cambia codigo, estructura, scripts o configuracion.
- Actualizar `Documentacion/DOCUMENTACION_USUARIO.md` si cambia instalacion, uso, configuracion o ubicacion de paquetes.
- Consultar `Documentacion/LOG_ERRORES_INCIDENCIAS.md` antes de resolver errores.
- Mantener `Documentacion/REGISTRO_BUGS.md` al dia.
- No documentar contrasenas, tokens ni datos privados.

## Cambios registrados

### 2026-04-25 - Cierre para apertura de V-01.04

- `V-01.03` queda como version publicada y base anterior.
- La nueva linea activa pasa a `V-01.04`.
- Los cambios futuros deben documentarse en `Documentacion/Versiones/v-01.04.md`.

### 2026-04-25 - Publicacion GitHub

- Publicado el contenido versionable de `V-01.03` en la rama remota `origin/V-01.03`.
- Commit principal: `1155bac` (`Publica V-01.03`).
- Excluidos `Otros/`, `Skills/` y paquetes generados de `Atlas Balance/Atlas Balance Release`.
- Verificacion pre-push: backend Release tests 94/94 OK, frontend lint/build OK, `npm audit` 0 vulnerabilidades y NuGet sin vulnerabilidades.
- `gh` no estaba autenticado durante la publicacion de codigo; no se creo PR desde esa sesion.
- Asset de release publicado posteriormente en `V-01.03-win-x64`.

### 2026-04-25 - Publicacion de asset GitHub Release

- Creado release publico `V-01.03-win-x64` en `AtlasLabs797/AtlasBalance`.
- Publicado `AtlasBalance-V-01.03-win-x64.zip` como asset del release, no como archivo versionado.
- Tag remoto `V-01.03-win-x64` asociado al commit `8df640d86912eb39b900a59ea0fd8ba769cacc96` (`origin/V-01.03`).
- SHA256 local/GitHub: `71E51F49CF740D358E056F256B70B3352EE23E61BD6FFFF0F048627AA07FDFA2`.
- URL: `https://github.com/AtlasLabs797/AtlasBalance/releases/tag/V-01.03-win-x64`.

### 2026-04-25 - Generacion de release Windows x64

- Generado paquete `AtlasBalance-V-01.03-win-x64` en `Atlas Balance/Atlas Balance Release`.
- Generado ZIP `AtlasBalance-V-01.03-win-x64.zip`.
- Frontend recompilado y sincronizado en `GestionCaja.API/wwwroot`.
- API y Watchdog publicados como self-contained `win-x64`.
- Verificacion: build de release OK, manifiesto `version.json` y `VERSION` empaquetados en `V-01.03`, y paquete sin archivos `Development`, plantillas ni `.env` dentro de `api`.

### 2026-04-25 - Hardening de seguridad post-auditoria

- Cerrados hallazgos de sesion: `SecurityStamp`, `PasswordChangedAt`, migracion `UserSessionHardening`, invalidacion de access tokens antiguos y revocacion de refresh tokens en reset/cambio/delete/reuse.
- Login endurecido con throttle por cliente/email y respuesta externa generica para cuentas bloqueadas.
- Integracion OpenClaw endurecida con rate limit para bearer invalido antes de consultar tokens.
- `app_update_check_url` restringida a HTTPS del repositorio oficial `AtlasLabs797/AtlasBalance`.
- Backups, exportaciones, descargas y Watchdog validan rutas absolutas antes de normalizarlas.
- Password minimo sube a 12 caracteres y se bloquean passwords comunes en backend; frontend actualizado.
- `INSTALL_CREDENTIALS_ONCE.txt` se programa para borrado automatico a las 24 horas.
- `postcss` actualizado a `8.5.10`.
- Verificacion: backend Release build OK, suite backend completa 94/94 OK, frontend lint/build OK, `npm audit` 0 vulnerabilidades, NuGet sin vulnerabilidades y parser PowerShell OK.

### 2026-04-20 - Apertura de version

- Creada la rama local `V-01.03` desde `V-01.02`.
- Marcada `V-01.03` como version actual del proyecto.
- Actualizada version runtime backend a `1.3.0` con `InformationalVersion` `V-01.03`.
- Actualizada version frontend a `1.3.0` y `appVersion` `V-01.03`.
- Actualizados scripts de build/instalacion para usar `V-01.03` por defecto.
- Cerrada `V-01.02` como version publicada y base anterior.
- Verificaciones de apertura: `git diff --check`, backend build Release y frontend build OK.

### 2026-04-23 - Cierre de vulnerabilidad de autorizacion en extractos

- Corregida divergencia de autorizacion en `ExtractosController`: `PuedeVerDashboard` global ya no concede acceso global de datos.
- Ajustadas rutas `GetAllowedAccountIds` y `CanViewTitular` para usar solo permisos de datos (`agregar`, `editar`, `eliminar`, `importar`) en el criterio global.
- Agregado test de regresion `Listar_Should_Return_Empty_For_DashboardOnly_GlobalPermission`.
- Verificacion: tests de autorizacion relacionados 8/8 OK.

### 2026-04-24 - Alineacion frontend de permisos de cuenta

- Corregida desalineacion en `frontend/src/stores/permisosStore.ts`: un permiso global `dashboard-only` ya no habilita acceso de cuenta ni reglas de columnas en frontend.
- `frontend/src/pages/CuentasPage.tsx` ya no muestra accesos operativos a dashboards de cuenta cuando el usuario no tiene alcance real; ahora marca `Sin acceso`.
- `frontend/src/pages/CuentaDetailPage.tsx` redirige a `/dashboard` si el backend rechaza la carga con `403`.
- Verificacion: `npm.cmd run lint` OK, `npm.cmd run build` OK y sincronizacion de `dist` a `wwwroot` completada.

### 2026-04-25 - Auditoria de seguridad manual inicial

- Ejecutado analisis manual de seguridad inicial.
- Creado `Documentacion/SEGURIDAD_AUDITORIA_V-01.03.md`.
- Registrados bugs de seguridad que quedaron cerrados en el bloque posterior de hardening del mismo dia.
- Verificacion: `npm audit` 0 vulnerabilidades, NuGet sin paquetes vulnerables y tests focalizados 22/22 OK.
